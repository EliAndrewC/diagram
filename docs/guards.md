# The guards: what is enforced, where, and why

*Project reference, moved out of [`../CLAUDE.md`](../CLAUDE.md) on 2026-09-15 so it is loaded on
demand rather than in every session's context. CLAUDE.md keeps the one-row-per-guard table; this
file is the full record - the rule, the mechanism, the measurement that motivated it and the GM's
ruling - and the doctrine for writing a guard.*

**Load this file when:** a guard blocked or rewrote a command and its message did not settle the
question, you are adding or changing a guard, or you are auditing what the guards cost
(`make audit`). The economics of the whole set are in [`efficiency-tooling.md`](efficiency-tooling.md).

---

**WHAT IS ENFORCED, AND WHERE.** Every guard has a test companion that
`make hooks-test` runs as a gate phase - a guard without one turns the gate red (constitution XVIII).

| rule | mechanism |
|---|---|
| never `git push --force`; never rewrite history (rebase, pull --rebase, merge --squash, commit --amend) | [`scripts/repo-safety-hooks.sh`](scripts/repo-safety-hooks.sh) - no escape; "never" stops meaning never the moment one exists |
| no git writes to `/host-l7r-repo` | same script; EDITS there stay legal, the intake workflow needs them |
| the GM's SOURCE blocks are not editable (V) | [`scripts/source-block-hooks.sh`](scripts/source-block-hooks.sh) - checks containment against the file on disk |
| hyphens only; American spellings | [`scripts/house-style-hooks.sh`](scripts/house-style-hooks.sh) - **CORRECTS the text rather than refusing the edit** (feature 164): both rules are exact substitutions from this file's own table, so the fix is applied and the session is told. Two things it never does: correct a word inside a backtick span (a token being NAMED, not used - it refused feature 164's own plan for one) and correct the GM's verbatim `specs/*/request.md`, which stays on the refusal path so a person decides (Principle V). Still exempts the GM's writing and the files that must quote the rule |
| a README is the GM's to write (XVII) | [`scripts/readme-hooks.sh`](scripts/readme-hooks.sh) |
| everything runs through `make` | [`scripts/make-only-hooks.sh`](scripts/make-only-hooks.sh) + `l7r/diagram/_invocation.py`. **A TARGETED bare pytest is REWRITTEN to `make test-file FILE=<files, a dir, a node id> [K="<-k expr>"]`** (feature 164 took one file; feature 212, GM 2026-09-07, *"targeting a specific module or even a specific test case"*, takes the rest: `-k` becomes `K=`, output flags are dropped, and the `2>&1 \| tail` after the pytest segment - which every one of the eleven targeted runs in the record carried, and which 164 declined - is kept verbatim); a marker, a deselect, a collection, a coverage or plugin flag still refuses, and the refusal names the token that stopped it. **An engine entry point a make target wraps is rewritten to that target** (`python3 -m l7r.diagram.ci engine-key worktree` -> `make engine-key REF=worktree`), the table DERIVED from the Makefile's one-line `$(RUN).<module>` recipes at hook time. `gate-hooks` counts `K=` and a `::` node id as the subset a bare `-k` is. Its message also lost a false reason: the floors are held by the gate targets, not by `test-file`, which runs `--no-cov` |
| guard files are not edited casually | [`scripts/guard-file-hooks.sh`](scripts/guard-file-hooks.sh) - and it says so when you OPEN one (feature 164): a Read of a guard file carries the marker line, because 28 of its 29 recorded firings were the same edit again with the marker added. Since feature 212 the marker is judged only on a GUARD file (a spec that mentioned the token was being refused for a missing reason) |
| **a recipe comment must not RUN** (feature 212, the GM's ruling relayed 2026-09-07 after feature 207's `: "... \`make test-full\` ..."` comment ran `make test-full` from inside test-full and recursed 914 levels to the container's 2,048-process limit; feature 185 had met the same hazard: *"just commenting, saying not to do it again is not a good way to reliably make sure that the problem does not recur"*): an Edit or Write to ANY Makefile whose `: "..."` recipe comment holds an unescaped backtick or `$$(` is refused BEFORE the marker escape and has no escape token - a shell fact, not a policy; `\`` and `\$$(` pass | the same script, `recipe-comment-substitution`; the detector is `_hm_make.py recipe_comment_hazards`, shared with the gate-phase backstop `tests/tooling/test_makefile_recipe_comments.py`, which scans every Makefile in the tree and proves the detector fires; `scripts/test-guard-file-hooks.sh` section 5 |
| a feature in progress lands NOTHING, on either route (feature 133, GM 2026-08-25): the active feature is derived from any `specs/NNN-*/tasks.md` with an open task that the delta touches or the pointer names; the one exception is a push of that feature's `specs/` directory alone (the number claim); no flag | [`scripts/sync-with-main.sh`](scripts/sync-with-main.sh) at PUSH time; `scripts/test-sync-with-main.sh` 7d proves it fires on both routes and stays quiet on the claim and on a finished feature |
| a spec is reviewed before implementation (XVI); a Mode B map before it ships | [`scripts/review-gate.sh`](scripts/review-gate.sh), run by `sync-with-main.sh` at PUSH time |
| nothing is pushed that a green gate did not see (XIII): engine Python needs a green `make done`, a guard script (`scripts/*.sh`, `scripts/*.py`) a green `make hooks-test` | [`scripts/gate-stamp.py`](scripts/gate-stamp.py), stamped by each gate on success and checked by `sync-with-main.sh` at PUSH time; `scripts/test-gate-stamp.sh` proves both refusals. Added 2026-08-25 after a red `hooks-test` chained with `;` landed on main - the stamp only covered engine Python |
| both perf bookends exist; a seed >5% slower must be DIAGNOSED and a total >10% BLOCKS as a regression (VI) | `make perf-gate`, a phase of `make done FULL=1`; the two bands are in `tools/perf_snapshot.py` with `tests/tools/test_perf_snapshot.py` proving each fires |
| no `-k` subset before the gate; no branches; no polling; batching | the `gate`/`no-branch`/`no-poll`/`batching` hooks. Feature 164 converted the two that could be: **`no-poll` CORRECTS a self-matching process pattern** into the bracket form it used to recommend in prose (and runs that correction LAST, after the busy-wait refusals - in first position it rewrote the original 2026-07-25 command and let the 10.9-minute busy-wait through, which its own suite caught; **and since 2026-09-08 the correction runs on the ESCAPED path too** - a `POLL_OK` waiter on a detached page-check carried `! pgrep -f "make page-check"`, the token exited the guard before the correction, and the loop ran for hours on a gate that had finished in 7 s: the escape permits the wait, it does not make the pattern correct), and **`batching` warns one turn BEFORE it blocks**, free, because at 127 firings in six days it was the loudest guard in the repository and every firing spends the round trip it exists to save - **and since feature 249 (GM 2026-09-14) that notice speaks on ANY single-call turn at one below the bar**, a folded or backgrounded command included: the window counts a turn serial by ROUND TRIP whatever its shape, while the notice carried the block's shape test, so a session reconning in folded commands - the playbook's own advice - reached the bar with no notice and was blocked cold; the block keeps its shape test, because a folded command is never the right thing to refuse. **Feature 212 (GM 2026-09-07) fixed `no-poll`'s file-wait qualifier and backgrounds the foreground form**: the one wait the GM permitted in feature 165 - a backgrounded loop reading a file - was being REFUSED on seven of its eight real uses because the qualifier read the condition raw (a `\|` inside the quoted regex counted as a pipeline, `2>/dev/null` as an output file, `$S/done.log` as no path, `[ -s f ] && grep` as none of the three forms); it now reads the condition in five stated steps (`_hm_shape.py file_watching_loop`; the substitution test stays on the RAW condition, so nothing quoted can hide a `$(curl ...)`), and a FOREGROUND loop of the permitted shape is returned with `run_in_background` set and told, instead of refused. The boundary itself did not move. **Feature 227 (GM 2026-09-12) ADDS THE PROOF OF LIFE to a permitted wait that has none**, because a wait that never asks whether the thing writing the file is still there waits forever when it is not: a detached `make` run wrote everything it had to write and was then killed before flushing the line the waiter watched for - by the kernel's OOM killer, `oom_kill 36` in this container's `/proc/vmstat`, so yes, a `make` run here does get killed. The GM's rule, *"the hook should add the second proof of life check to what is being waited for"* and *"simply telling you to set a watch properly next time is bad engineering practice because that's just another version of making you remember to do something"*, is performed: the clause names the file the loop itself watches and asks the kernel's open-file table and the file's mtime (`scripts/_writer-alive.sh`), never a process pattern - a literal `pgrep -f` pattern matches the searching shell, which is this guard's own 2026-07-25 fault. And the same guard stops REFUSING the shape it now writes: a liveness clause was not one of the three permitted file forms, so a wait that asked both questions was blocked as a busy-wait. A liveness part can only end a loop sooner, a condition must still read a real file, and the `>/dev/null` forgiveness is per PART, so `until grep -q x /tmp/a.log >/dev/null` is refused exactly as before |
| a repeated EXPENSIVE MEASUREMENT with nothing changed between (GM 2026-08-28, feature 146: *"that seems really inefficient since you know what lines need to be covered"*) - the **SECOND** `make test-full` / `done FULL=1` in a row with no engine edit and no commit is refused (feature 162, GM 2026-08-30: *"should we make it so we start blocking at 2 in a row instead of 3 in a row?"*), and the FIRST one carries the reminder, so a session is told before it is ever refused - a `additionalContext` on an allowed call costs no round trip, a block costs one. The cheap loop is never blocked; an engine edit or a commit resets it; escape `MEASURE_OK` with a reason. Measured on the feature that prompted it: 20 measurements, about an hour, almost all redundant | [`scripts/measure-hooks.sh`](scripts/measure-hooks.sh); `scripts/test-measure-hooks.sh` (28 cases, including the cheap loop staying free, the known mention false-positive, and a log that cannot be written not taking the guard down) |
| what a guard actually COSTS is a total, not an impression (feature 162, GM 2026-08-30: *"I notice I've been seeing a lot of this in the output of my claude code sessions over time"*) - `measure-hooks.sh` and `gate-hooks.sh` record every block, rewrite, reminder and escape to `~/.claude/guard-log/`, one file per entry, and `make audit` prints the census with the escape rate. Answering that question before this existed took a replay of 715 MB of transcripts (`specs/162-guard-block-economics/measure/replay.py`), and the answer retired a refusal that was being escaped 62% of the time. **Since feature 168 (GM 2026-08-30: *"the firing log should record more data for us to be able to use to make improvements in the future"*) EVERY acting branch of every guard records, and an entry names the RULE** - `no-poll` distinguishes busy-wait from disguised-sleep, `clone-sync` its five refusals, `make-only` its five verdicts - because the unit a future improvement acts on is a rule, not a script, and "no-poll fired 32 times" cannot say which one is carrying the cost. **An escape is a branch, and the one that matters most**: all nine escape tokens record now, where only `MEASURE_OK` did before. `idle-tests` is deliberately out of class (it refuses a session nothing - it is a runner, and it keeps `dev/idle-log/`) | [`scripts/_guardlog.sh`](scripts/_guardlog.sh) + [`scripts/_gatecost.py`](scripts/_gatecost.py); every guard's own suite, plus `tests/tooling/test_guard_firing_log.py`, which drives each guard with a REAL payload and asserts the `(event, rule)` on the entry that lands (a grep proves a call site exists, not that it fires), and `tests/tooling/test_guard_message_durations.py`, which fails the gate if any guard message states how long a command takes |
| a background agent that stalls (transcript ends on a tool_result, unchanged 5 min) is REPORTED at the next prompt and, under a `Monitor`, mid-turn (feature 143, GM 2026-08-28) | [`scripts/agent-stall-hooks.sh`](scripts/agent-stall-hooks.sh) `prompt` / `watch`; `scripts/test-agent-stall-hooks.sh` (12 cases) |
| the gate and the independent settlement-review run TOGETHER, and neither alone - and since feature 164 a plain `make done` with a review owed is **REWRITTEN to `make verify`** rather than refused, carrying the line that says to dispatch the review in the same turn (7 of its 13 firings had been escaped in the very next call) (feature 151, GM 2026-08-29: *"is there a scripted way to have them both happen at the same time and that's the only way you can do either of them without some kind of override?"*): `make verify` starts both; **every OTHER gate shape** (`make maps`, a detached `make done`, `FULL=1`, flags) **is RECORDED AND PERMITTED with the DISPATCH NOW context** rather than refused (feature 212, GM 2026-09-07: the census found the rewrite fired once while every real refusal - eight `make maps`, three detached gates - was a shape `verify` cannot take, against 98 `PAIR_OK` waivers; a detached run overlaps the review as under verify, a FOREGROUND run is told the review will follow it and how to overlap next time - spec 212 D7, the one place this changes what 151 delivers; the GM accepted it on 2026-09-08 over restoring the refusal for that shape, with a detached `verify` variant for `maps` as the lever if sessions keep sweeping in the foreground); a `settlement-review` dispatch with no gate running or freshly green is refused, and a turn may not end on a half-open pairing. Escape `PAIR_OK` with a reason, logged to `dev/bypass-log/`. Measured cause: the review's 17 minutes sat on the critical path of feature 150's T55 because it is dispatched from memory rather than by the tooling. **And since feature 231 (GM 2026-09-12) a review is owed only when a pool map's LAYOUT moved** (*"if there are no changes to the actual way that the settlement is laid out, then we should not need to re review the settlement"*): `scripts/_review_owed.py` names every map whose MANIFEST differs from the merge base with main - the one scripted answer, asked FRESH at every decision point because the gate's own pool phase can move one - and with no map named the gate runs as typed, the stop branch stays quiet and the automatic waiver is recorded with its reason. A glyph or page change with an identical manifest goes back to the GM, who reads one changed map faster than the agent does; feature 228 spent 18.7 of its 32 minutes proving that. Where a review IS owed, the changed maps are snapshotted from the clone and from main into `<clone>/.git/review-snapshot/` before the gate starts (the roll cache evicts a pool map's renders mid-run), and the guard resolves THIS SESSION'S CLONE through `clone-sync-hooks.sh resolve` rather than the shell's cwd - reading the mirror's state is what refused feature 228's review and then fired half-open on it. **And since feature 248 (GM 2026-09-14) the tooling makes the correct DISPATCH SHAPE happen** - feature 247's review was one agent over four maps, serialized, 11 of the feature's 36 minutes, with "one map per agent" already written in the reviewer's contract and `dev/reviews.md` (*"the point of the tooling is to make the correct thing happen automatically without you or I needing to remember the precise ways to not get it subtly wrong in a costly way"*): a `settlement-review` dispatch naming MORE THAN ONE pool map (owed or not) is refused before the agent starts, with NO escape; `make verify` and the permit branch write one prompt per owed map (`<map>/dispatch.md` in the snapshot) and say to dispatch N agents in one message; the stop hook refuses a turn while an owed map has no review dispatched, running or recorded (once per missing set, after every waiver); and whether the dispatches shared a message is RECORDED (`reviews-parallel` / `reviews-serialized`, a sixty-second span by definition), never refused, because a hook cannot launch an agent or force parallel calls. **A rendering-only feature owes no settlement-review** (the week's ledger: a catch on every layout pass, none on a map for any rendering one) - `_review_owed.py` reads the DERIVED active features (the pointer plus every touched feature directory with a `tasks.md`, ticked or not, because at push no box is open) and waives only when every one is all `research: rendering`; a `procedure` feature is not waived. **And a changed map ships on its reviewer's verdict record** (PASS or NEEDS-WORK at the pushed tree's engine key), the rendering waiver, or - only for a map with no record at all - the notes-file touch; a stale-key record refuses whether or not the notes are touched | [`scripts/pair-hooks.sh`](scripts/pair-hooks.sh) `pretool` (Bash + Agent) / `stop`, keyed on `ci engine-key worktree`; `scripts/test-pair-hooks.sh` (93 cases since feature 248, including the MENTION-is-not-an-INVOCATION shape that blocked feature 151's own time audit four times, the two-map refusal, the missing-map stop and the span); `scripts/_review_owed.py` + `tests/tooling/test_review_owed.py` (the waiver's forms); `scripts/review-gate.sh` + `scripts/test-review-gate.sh` (the three passes and three refusals) |
| **FINDINGS DO NOT REACH THE GM UNFILTERED** (GM 2026-09-12). A session sent the GM five "judgment calls" off two settlement reviews and three came back as corrections: two rested on NO project norm at all - a dwelling count measured against a scoring radius no rule uses as a bar, and a distance measured against a maximum that does not exist - and one had already been answered by a measurement taken an hour earlier on a layout that no longer existed. The cost is not the wasted minute: *"I'm concerned that I will be ignoring something bad"* - an escalation that turns out to be nothing teaches the GM to doubt their reading of the ones that matter. **The rule already existed in TWO places** (the review agent's own contract, *"QUESTIONABLE ... needs a RESEARCH PASS - never 'a GM ruling'"*, and constitution XII) **and was disregarded anyway**, which is the GM's whole point: *"If we already had a rule in two places and it was still disregarded, then it sounds like we need another process change. Like maybe when you have findings to escalate to me ... you do a final subagent check on that writeup."* So a `settlement-review`, `building-review` or `size-audit` dispatch ARMS a requirement and an `escalation-check` dispatch DISARMS it; the turn may not close in between. The `pair` shape deliberately - it keys on an Agent dispatch, which the harness can see, rather than on the session's prose, which no hook can read. A review that turns out to have nothing to relay costs one cheap filter pass, stated here as the accepted price of an enforcement that works; escape `ESCALATION_OK` with a reason | [`scripts/escalation-hooks.sh`](scripts/escalation-hooks.sh) `pretool` (Agent) / `stop` + [`.claude/agents/escalation-check.md`](.claude/agents/escalation-check.md) (the filter: it verifies each cited norm IN THE REPO, which is the part the session cannot do honestly about its own draft); `scripts/test-escalation-hooks.sh` (22 cases, including that a `source-reader` or `quote-check` dispatch must NOT arm it, and that the refusal fires once per review rather than looping) |
| **A SPEC-FIDELITY ROUND AFTER THE FIRST READS THE DIFF, BY THE TOOLING'S HAND** (feature 249, GM 2026-09-14). The reviewer's contract had said since feature 236 that a later round reads only the changed passages and that a reviewer not told which passages changed should ask; on 2026-09-14 two rounds of one small amendment each read every file end to end (165k and 110k tokens, over half of an eleven-minute change) because the session's dispatch handed over the whole spec and whole-spec questions. The GM: *"it is okay for subsequent rounds to essentially review the paragraphs that have changed"* and *"I want this all to be automatic since instructions are not reliably followed"*. So a `spec-fidelity` dispatch that is a SPEC review (a MODE 2/3 marker wins; MODE 4 / PLAN REVIEW and MODE 1 / EXCEPTION CHECK pass untouched, keyed on the contract's heading forms because a round's prompt says "plan review" and "exception" in ordinary sentences) of a feature the guard has seen before is REWRITTEN: a preamble is prepended carrying the round within the pass (restarting at a recovered FAITHFUL, the GM's 2026-09-12 cap ruling), the previous verdict verbatim from the session's own subagent transcript (else the Review history, marked as the session's summary), the diff of the feature directory against the snapshot taken at the previous dispatch, and the instruction to read that plus grep hits only; the session's prompt follows unchanged. The first dispatch snapshots silently; a spec with history and no snapshot is told once. Never a refusal but the reason floor; escape `REVIEW_ROUND_OK="<reason>"` for a deliberate full re-read. MODE 3's third step is a bounded grep rather than "scan the rest" | [`scripts/review-round-hooks.sh`](scripts/review-round-hooks.sh) + [`scripts/_hm_review_round.py`](scripts/_hm_review_round.py) (the decision, a function); state in `<clone>/.git/review-round/<feature>/`; `scripts/test-review-round-hooks.sh` (41 cases on a fixture clone with fixture transcripts: the pass-throughs, the silent first snapshot, the rewrite's preamble, diff, verbatim verdict and unchanged tail, the advancing snapshot, the pass restart, the summary fallback, the escape and its reason floor); the census rows in `tests/tooling/test_guard_firing_log.py` |
| `hooks-test` COSTS WHAT IT SHOULD (feature 172, GM 2026-08-30: *"could we not do something similar where if the hooks have not changed, then we do not run the hooks tests?"*). The skip already existed and costs 0 s - what did not was the GRANULARITY: every suite was declared to depend on all four shared helpers, so a one-line `_gatecost.py` fix re-ran 21 suites. The set is DERIVED now, transitively and from CODE rather than prose - measured on a real incremental run, a `_gatecost.py` change re-runs **4 of 21** rather than 21, and a make/rewrite change 5; the constant in both is the TWO whole-tree entries (`sync-with-main.sh`, which is held there because it resolves script paths at run time, and `gate-stamp.py`, which derives there because it reads the whole directory) - and the suites run in PARALLEL (**194 s -> 63 s** on identical content, 21 of 21 green both ways). `_hookmatch.py` is three leaves - shape, escape, make/rewrite - with each guard invoking the one it uses, which takes make/rewrite churn from 21 suites to 3. **What it does NOT fix, measured**: the escape family stays at 17 of 18 because every guard reaches its escape through `_guardlog.sh`, and no arrangement of files changes that | [`scripts/_hookdeps.py`](scripts/_hookdeps.py) (the derivation), [`scripts/_hookrun.sh`](scripts/_hookrun.sh) (one parallel job), `tests/tooling/test_hook_deps.py`. The derivation is checked rather than trusted: it was wrong three times first, each time plausibly - a hardcoded roster, a filename counted from a COMMENT, and a Python import that never writes the `.py` |
| an ESCAPE MUST SAY WHY, and every permitting branch records it (feature 170, GM 2026-08-30: *"we have no way to audit later when this workaround was taken and whether the stated reasons were valid use cases"*). A bare token is REFUSED - the missing thing is the session's reasoning, which no tool can supply - and the floor is two words and eight characters, which excludes `GATE_OK: ok` and admits `CI is down`. THREE branches were permitting silently and no longer are: `make-only` (via a distinct `classify()` verdict), `repo-safety`'s `HOST_GIT_OK` over the GM's own repository, and `sync-with-main`'s `GATE_STAMP_OK`, in a file that had no `guard_log` call at all. `make audit` shows the reasons | [`scripts/_hookmatch.py`](scripts/_hookmatch.py) `escape_reason`/`reason_is_enough` + `escape_or_refuse` in [`scripts/_guardlog.sh`](scripts/_guardlog.sh); `tests/tooling/test_guard_firing_log.py` derives the completeness over **(token, permitting site)** pairs, because two tokens have two sites each and a per-token check passes green while the target branch stays silent |
| a FINISHED background run cannot be reported as still going (feature 170, from a session's own report: *"I reported the gate as 'waiting' when it had actually failed four hours earlier because I never saw the notification"*) - surfaced at the next prompt AND at turn end, once, never again. It REPORTS and never blocks: the failure was a session not KNOWING | [`scripts/finished-run-hooks.sh`](scripts/finished-run-hooks.sh); `scripts/test-finished-run-hooks.sh` (22 cases) |
| **...AND A WAITER SPINNING ON A DEAD PRODUCER IS REPORTED** (GM 2026-09-12, who found THREE in their own status line). A waiter is a loop watching a file; when the thing writing that file dies, the loop spins forever. Three had been re-grepping every 15 s for EIGHT HOURS on logs from the `make placement-stages` runs the OOM killer ended that morning - the same incident that produced the proof-of-life clause, whose waiters predate it and so never got one. **Two census attempts by this session missed them and twice told the GM that nothing was running**, which is the reason the rule is mechanical: at any instant the visible process is the loop's own 15 s `sleep`, so a census filtered by process AGE cannot see the loop and one filtered by NAME sees only `sleep`. The honest question is asked of every process's cmdline and then of the file it names (`_writer-alive.sh`), with this hook's own ancestry excluded BY PID rather than by pattern - the command that searches for `until grep` contains those words. It REPORTS and does not block: the loop is harmless, not knowing about it is not | the same script's `stop` branch + its `stale` mode; the suite starts a REAL spinning waiter on a stale file and asserts it is found, reported at turn end, recorded under its own rule, and that a waiter whose file is still held open is NOT reported |
| **...AND A RUN STILL GOING MUST NOT BE ABANDONED** (GM 2026-09-12) - the mirror image of the rule above, and it happened the same afternoon the rule above was being relied on: a session started `make done` detached, wrote its report, ended the turn and never waited; the gate failed 58 s later on five uncovered lines and sat unread for 52 minutes, until the GM asked whether something had gone wrong. The finished-run rule could not catch it, because it fires at the NEXT PROMPT and by then the damage is done. The GM: *"if there is a makefile command running and your turn ends, then we have a hook that errors or triggers you or sends you context to say, hey. Hold on. You need to wait for that makefile command."* So the Stop hook REFUSES a turn that would close over a live `make` in this session's clone, and names it. Detection is by CWD out of `/proc` - a process whose `comm` is `make` and whose working directory lies inside the clone - never by a process pattern, because `pgrep -f "make done"` matches the searching shell, which is the 2026-07-25 fault `no-poll` exists for. ONCE per run, keyed on the ROOT make of the run, and there is NO token escape: a Stop payload carries no command, so there is nowhere to put one (tried; the suite records why). The second attempt closes the turn, which is the release valve a session answering the GM mid-gate needs. **And since feature 246 (GM 2026-09-13: *"the fact that we keep seeing this message over and over again makes me wonder whether we should take a different approach"*) the guard asks the SECOND question - will anything wake the session?** In its first day it refused 18 turn-ends on 13 runs, 5 of them repeats on a run already refused, every one in the measuring session landing on a run the harness was tracking with a waiter loop armed besides: the marker had held EVERY live make pid, and a four-phase gate is a new child pid per phase, so "once per run" was once per phase; and the rule could not tell the 2026-09-12 shape (detached with `setsid nohup`, nobody watching) from the two that need nothing. Now, from `/proc` by pid and never by pattern: a run with an ancestor that is the `claude` process (the harness - what every Bash-tool background-mode command looks like; the harness notifies at exit) is TRACKED, a run with a `until`/`while`-`grep`-`sleep` loop naming its stdout file is WATCHED, and both are let through with one line of context and recorded `permitted` (`tracked-run` / `waiter-armed`); only a detached run nobody watches is refused, once per root, and the refusal names that run's own log in the loop it prescribes and says a background-mode run needs no loop - the old text sent sessions to build a second wakeup for an event the harness already reports. A periodic "still waiting?" timer was priced and declined (`specs/246` research.md R4): polling with a period at a turn per tick, where the event wakeup exists twice over and did not fail; a single fallback for a LOST notification waits in `future-work/cross-cutting.md` for a lost notification to be observed | the same script's `stop` branch + its `live <clone>` and `judge <clone>` modes; `scripts/test-finished-run-hooks.sh` drives a REAL `make` in a fixture and asserts the refusal, its message, the once-per-run release and the recorded rule - and since 246 a `claude`-named stand-in harness, a detached run with a loop on its log, a loop on the wrong file, and a two-phase make refused once and not again when its second phase is the live child |
| a TARGET THAT GETS SLOWER FAILS (feature 171, GM 2026-08-30, on `make done`'s median going 35 s -> 148 s in three days with nothing failing: *"I could have sworn that we had some code in place that detected when we had our unit tests start to take longer relative to some previous baseline"*). `quick` fails at the GM's 15 s on the run itself; `done` at its ceiling on the median of green same-scope runs. The GM's numbers are HARD CEILINGS the mechanism sits under - an early derivation LOOSENED as the baseline drifted, so a 12 s baseline bought a 16 s ceiling - and the baseline is PINNED, never rolling, because a rolling median follows a twenty-small-steps regression upward. A new target is a row; the AWS row is present and OFF | [`scripts/_ratchet.py`](scripts/_ratchet.py); `tests/tooling/test_ratchets.py` (16 cases, including that the derivation REPRODUCES the GM's own two numbers - a two-point fit, which is the entire argument for trusting it elsewhere) |
| a write that would LAND in the mirror through the shell's directory is MOVED to the session's clone, and one that NAMES the mirror is refused (feature 169, reworked by feature 204, GM 2026-09-07: *"is it possible for us to intercept the cd command and rewrite it to change into the correct directory instead based on what session we are currently in? And then if we did that, we could add tool context"*). Feature 169's guard asked where the SHELL STOOD, and the census behind 204 found it refusing correct work almost every time: 173 refusals in a week, 2 of them `cd /diagram && git commit` (the shape it was built for), 146 with no `cd` at all (a subagent's shell starts in `/diagram` and it wrote to the scratchpad or a clone by absolute path), 127 from subagents, and NONE naming a write inside main by its path. Now `_hm_tree.py judge` walks the command's own `cd`s, `VAR=` assignments and subshells from the payload's cwd and classifies each write by where it lands: `cd /diagram && git commit`, or `git commit` while standing in `/diagram`, is REWRITTEN to `cd <this session's clone> && ...` with `additionalContext` saying why (the rewrite also moves the shell, which cures the leak for the next command); `git -C /diagram commit` and `> /diagram/x` stay refused; a write to `/tmp`, the scratchpad or a clone from a shell standing in main is allowed silently. Every rewrite is re-judged before it is emitted. Escape `MAIN_TREE_OK`. It does NOT enforce the 2026-08-17 read-only rule, which stays declined | [`scripts/main-tree-hooks.sh`](scripts/main-tree-hooks.sh) + [`scripts/_hm_tree.py`](scripts/_hm_tree.py) (the verdict) + `clone-sync-hooks.sh resolve` (this session's clone; a subagent resolves to its parent's); `scripts/test-main-tree-hooks.sh` replays the 173-command corpus `scripts/fixtures/main-tree-refusals-2026-09.json` and expects zero refusals; `make guard-log GUARD=main-tree` lists the firings with session name, cwd and the full command (feature 204 made every guard's record carry those) |
| an ESCAPE token is an invocation, not a mention (feature 169): all eleven go through `_hookmatch.py escape`, so a grep, a commit message or a heredoc no longer escapes a guard - and no longer resets `measure`'s counter or deletes `gate`'s state file. `pair`'s agent-prompt branch is the one stated exclusion | [`scripts/_hookmatch.py`](scripts/_hookmatch.py) `escape_used`; `tests/tooling/test_guard_firing_log.py` derives both the slug rule and the log-isolation rule over the whole guard tree, because the hand-lists they replace each missed a guard within a day |
| a checkout or restore that would DISCARD uncommitted work in a tracked file is refused (feature 133 T33, GM 2026-08-27): `git checkout -- <path>`, `git checkout <path>`, `git restore <path>`, `.`; a clean file, a branch switch, `--staged` and a mention pass; escape `DISCARD_OK` with a reason | [`scripts/discard-hooks.sh`](scripts/discard-hooks.sh) - asks git whether each named path is dirty in the tree the command names; `scripts/test-discard-hooks.sh` (21 cases). Added after a `git checkout -- ways.py` typed into a command as a NOTE discarded a task's working diff (5 min recovered from the transcript; the 10-minute task took 39) |
| an IDLE session runs the expensive tests unattended (feature 136, GM 2026-08-27): the Stop hook arms a detached timer, a prompt disarms it (and aborts a run in progress - nothing of the session's ever waits on an idle run); the wait is 60 + (hash(session name) mod 61) minutes of AWAKE time, restarted in full when the laptop resumes (a wall-clock jump past the ticks); one runner at a time host-wide (`~/.claude/idle-tests.lock`, losers defer 5-15 min); the run is `make idle-tests` (= the whole `done` gate, never FULL - the GM's ruling; the scope lock is RELAXED for it, and only for a process descending from the timer: `switches.idle_context`, unforgeable by a session; its record is `idle-done`, never a `done` a push could reuse); one run per idle, none on a clone unchanged since the last green idle run; the verdict is one line at the next prompt and a record in `dev/idle-log/`; never in main, never self re-arming | [`scripts/idle-tests-hooks.sh`](scripts/idle-tests-hooks.sh); `scripts/test-idle-tests-hooks.sh` (26 cases with injected clocks: the stagger band, the suspend restart, the lock, the abort, the never-in-main rule, the seams refused outside a fixture); decisions D1-D9 in `specs/136-idle-background-tests/` |
| a performance increase owes its records before the push (feature 129): an increase over that environment's band-1 line (0.0% local, 2.0% codebuild since feature 179) on the total or any seed needs a written explanation + a `perf-audit` confirmation; >5% total / >10% seed the escalated audit; >10% / >20% the GM's sign-off at a terminal | [`l7r/diagram/tools/perf_review.py`](.claude/skills/diagram/l7r/diagram/tools/perf_review.py) `--check`, run by `sync-with-main.sh` at PUSH time (`make perf-review`), printed by `make done` (`perf-gate`); `tests/tools/test_perf_bands.py` + `test_perf_review.py`; the main session cannot write the records - the commands prompt and decline without `AS=perf-audit` (research R1: nothing else distinguishes the shells) |
| the iteration switches (feature 132): with `remote off` nothing dispatches to CodeBuild and the gated push lands on a green local `make done` (LOCAL-GATED); the scope axis was RETIRED in feature 185 (it deferred multi-map rolls while the gate was slow; feature 174 made the gate run everything). `remote` is thrown and released by `make ci-off`/`ci-on`, which commit | [`l7r/diagram/switches.py`](.claude/skills/diagram/l7r/diagram/switches.py) read by the Makefile, the ci dispatcher and every multi-map entry point; `tests/test_switches.py` runs the real Makefile in a fixture and proves each refusal; `tests/tools/test_scope_lock.py` the Python layer; `scripts/test-sync-with-main.sh` the GATED-LOCAL route and the seams-only-in-a-fixture rule; doctrine in [`dev/switches.md`](.claude/skills/diagram/dev/switches.md) |
| a paid CodeBuild run starts only when five conditions pass (engine delta; complete feature on a merge; green local check against this code; no verified record - a green build OR a green local `make done` on the same engine content with nothing new from main; breaker up), and never after a red gate without a green local target (feature 130) | [`l7r/diagram/ci/`](.claude/skills/diagram/l7r/diagram/ci/CLAUDE.md), `tests/tooling/ci/` (99 cases against recorded AWS responses); the route is decided by `sync-with-main.sh` push, tested by `scripts/test-sync-with-main.sh`; the mirror refresh by the same; the build-side FULL door by `tests/tooling/ci/test_door.py` |
| the gate cannot finish below **100% coverage** (feature 174, GM 2026-08-31: *"we want to enforce one hundred percent coverage in whatever the relevant place is ... so that in the future, we literally cannot complete our `make done` in order to merge back into main, and there will no longer be any mechanism by which this can be accomplished"*). The floor had been real but DEFERRED: a plain `make done` ran its test phase with `COV_FLOORS` empty, printed *"coverage floors: deferred to `make done FULL=1`"*, and **stamped green anyway** - and the stamp is the whole of what the push demands, so a merge below the floor was available every day. `COV_FLOORS=1` is also the switch that turns every deselection off, and a deselected test takes its coverage with it, so there is no arrangement that holds the floor over a partial suite: the gate's test phase is `test-full` on BOTH branches now, and `FULL=1` is what still adds the perf bookends. The cost - the gate runs the whole sweep - was stated to the GM rather than used to decline; **`make quick` is untouched**, which the GM exempted themselves (*"I don't think we need to do this for our make quick"*). The tree was brought to **100.00%, 0 uncovered** - 20,646 statements on 2026-08-31, and **22,520** once the GM widened the measured surface to the whole engine on 2026-09-02 (the clause above). No `pragma: no cover` was added TO REACH A NUMBER: unreachable lines were DELETED and five hard-to-reach closures were LIFTED to module level under the GM's own feature-146 doctrine. One was added later and is disclosed - `pipeline/gencache.py`'s `sys.monitoring` callback, which runs and is asserted but is invisible to coverage because coverage uses the same machinery. `GATE_RECIPE` in `gate-stamp.py` salts the stamp key so every record taken before the floor existed is retired at once - a run that was ALLOWED to finish below the floor must not satisfy a push under it | the skill Makefile's `done` phase list + its `test` target's three floors; [`scripts/gate-stamp.py`](scripts/gate-stamp.py) `GATE_RECIPE`; `tests/tooling/test_coverage_floor.py` proves the wiring (the phase list, the default-off switch, the 100 itself, and that bumping the recipe moves the key) |
| a Python file past **1,000 raw lines** fails, on the gate AND on both routes to main (feature 173, GM 2026-08-31: *"if any of them are too large ... then we fail the gate with a message that explains that the clone responsible for this work must split up the file in the manner prescribed in our project guidelines"*). Constitution Principle X clause 13 had been an ask-the-question line since 2026-08-15 and the question stopped being asked - ten files had drifted past, the largest at 4,369. The refusal PRINTS the procedure: the directory-module shape, the `CLAUDE.md` "look here when" index, a named exemplar package, and both carve-outs, so a reader whose file is genuinely ordered data or a derived roster is not sent to do the wrong work. Escape is the clause's own carve-out, `FILE_SIZE_OK: <reason>` in the file's first 40 lines with a reason of 40+ characters, enumerated by `make audit`; it ships with **zero** files using it. Closes half of the constitution's own v1.6.1 deferred TODO - clause 12's expression count on FUNCTIONS is still owed | [`scripts/check-file-scale.py`](scripts/check-file-scale.py) `--selftest` then the scan, in the skill Makefile's `lint` (which the gate runs first, so it reports before the map roll is paid for) and in `sync-with-main.sh` at push time - the same two places as `check-duplicate-defs.py`, the guard it is modeled on; `tests/tooling/test_file_scale.py` proves each rule fires |
| **a modal whose research section moved underneath it does not land unexamined** (feature 234, GM 2026-09-12). What a modal says about a feature IS the docstring of its `Kind` class, written FROM a research section its `Entry:` names, and nothing noticed when that section's content moved - the GM, told to update the pigsty write-up: *"if I hadn't said that ... then would you have done it?"* **Two halves, enforced differently on purpose.** The STALENESS half is a judgment about prose, so `make page-check` reports it free and the PUSH refuses until each named pair is answered - the `entry-drift` agent dispatched and the prose rewritten, or one recorded `ENTRY_DRIFT_OK="<reason>"` covering a whole maintenance sweep, which `make audit` lists. It is enforced rather than expected because the GM ruled the alternative out of existence: *"I don't believe that we should have any such thing as an unenforced doctrine. If it is unenforced, then it is not a doctrine."* Narrowing the key so it would fire less was priced FIRST and is dead - firing only on what a reader SEES takes 28 of 30 research-only commits to 27 of 30. The HEADING half is decidable and never correct work, so it simply GATES: a class `Entry:` that resolves to no section fails, with the deliberately-silent form recognized explicitly and enumerated. `record-format` and `quote-check` are NOT this check - neither ever opens a `Kind` docstring | [`scripts/_entry_owed.py`](scripts/_entry_owed.py) (the report, derived from the engine's own `_TAGS` less `_DATA_TAGS`), [`scripts/entry-gate.sh`](scripts/entry-gate.sh) (the push refusal) and [`scripts/check-entry-headings.py`](scripts/check-entry-headings.py) `--selftest` then the scan - the last two in `sync-with-main.sh` at push, the same place as `check-file-scale.py` and for the same reason: the delta that breaks either is a research-only edit that runs no gate at all. [`.claude/agents/entry-drift.md`](.claude/agents/entry-drift.md) makes the judgment a script cannot |
| **a plan's decisions are reviewed before its tasks are ticked** (feature 243, GM 2026-09-13: *"Yes please implement the plan-stage gate as a feature, thanks."*). `review-gate.sh` held the SPEC, but a plan is written after that verdict and nothing read it, so feature 239's plan narrowed its accepted spec - framed as an open question, not as a narrowing - and the narrowing reached every ticked task and main with no Principle XVI check. So `make tick` refuses while the feature's `plan.md` has no current CLEAR `plan-review.json`, and the PUSH refuses any feature its delta touches that has a ticked task in the same state, read at HEAD - because a box can be ticked by hand. A feature with tasks and no `plan.md` is refused too (skipping the plan must not be the way around the gate); a feature with no ticked task, a plan still being drafted, passes. The reviewer (`spec-fidelity` MODE 4) is given `request.md` verbatim with the spec and the plan, FINDS the decisions itself rather than reading the author's labels, and rules on each narrowing one; the record is keyed on the whole plan's SHA-256, so any later edit owes a new review, and its verdict is DERIVED from the rulings. Only the subagent records it (`make plan-verdict ... AS=spec-fidelity`, a declaration rather than a proof, as for `perf-confirm`). Older features are not exempt, by the GM's ruling on older specs. Escape `PLAN_REVIEW_OK="<reason>"`, recorded to the guard log and `dev/bypass-log/` | [`scripts/_plan_gate.py`](scripts/_plan_gate.py) (every decision), [`scripts/tick-task.py`](scripts/tick-task.py) (the tick refusal) and [`scripts/plan-gate.sh`](scripts/plan-gate.sh) (the push refusal, beside `review-gate.sh` in `sync-with-main.sh`); `tests/tooling/test_plan_gate.py` and `scripts/test-plan-gate.sh` |
| **a Bash command that cannot work is refused BEFORE it runs** (feature 236, GM 2026-09-12: *"if we are running a command in bash, and that command has a syntax error such that it is not going to work ... can we not have a hook which checks the syntax of the command before we run it and rejects it early?"*). Four rules, each recording its own so the census can price them apart: it does not PARSE (with the options the tool's own shell can enable - `bash -O extglob -n`, because plain `bash -n` refuses `shopt -s extglob` on one line and `!(x)` on the next, which the tool RUNS); a backtick span bash would EXECUTE, which the parse cannot see because it is valid syntax (the GM: *"putting backticks in a place that they do not belong ... reject it early"*); a `git commit -m` carrying a double quote, a newline or a second `-m`, which is item 3's literal ban and a refusal rather than a rewrite because the broken form parses cleanly into the WRONG message; and a co-author trailer addressed to anything but `noreply@anthropic.com`, by any route (`-m`, `-F -`, `-F <file>`, `--trailer`), after a placeholder address reached main through an `||` fallback on a history that is never rewritten. **Measured before it was written**: the 238 real commands of the session that motivated it, replayed - 2 refused by the parse, both of which had also failed at run time, 0 false positives, and 23 more caught by the `-m` ban; and across the 60 most recent transcripts NOT ONE executing backtick span was a deliberate substitution. Escape `SHELL_CHECK_OK` with a reason | [`scripts/shell-check-hooks.sh`](scripts/shell-check-hooks.sh) + [`scripts/_hm_shell.py`](scripts/_hm_shell.py) (the verdicts; the backtick walk is `_hm_shape.executing_backticks`, shared with the Makefile recipe-comment guard rather than copied); `scripts/test-shell-check-hooks.sh`, which REPLAYS `scripts/fixtures/bash-parse-corpus-2026-09.json` through the hook on every run, so the false-positive rate is measured rather than remembered |
| **house style reaches the writes that do not go through `Edit`** (feature 236, item 4). The corrector only ever saw the Edit and Write tools, so a British spelling written through a Bash payload - an `echo >>`, a `sed -i`, a `python3 -c` - walked past it, which is how they arrived on the session that motivated this. **The decision is a function since feature 239** - `_hm_house.report(payload)`, lifted mechanically out of a 275-line program inside the hook's single-quoted shell string, where one apostrophe in a comment had silently broken it twice - so `make hookbench GUARD=house-style [AGAINST=<ref>]` replays a frozen window of 560 real commands through it in seconds, and an exemption change owes that verdict diff. Two halves: the hook reads the Bash payload and CORRECTS it (GM 2026-09-12, ruling on the departure spec D2 had flagged: *"warn when it is the sed shape, and for other shapes just correct it"*) - a command that is itself the fix is reported and left exactly as typed, which is the sed shape the GM named plus any command carrying BOTH spellings of a word, and what a command only NAMES is never touched: a searcher's segment however it is reached, a regex alternation, a dash in a character class, a string whose whole content is one word (a sweep's word list), a path token, and `~/.claude/projects/` as `/tmp` already was - every class of it measured on the real commands the hook had warned on rather than imagined (`specs/236-*/research.md` R10, `scripts/_hm_house.py`); and `make quick` FAILS on one in the delta. The check reads the hook's own `BRIT` table, scans the delta against the merge base plus every UNTRACKED file (where the motivating failures were), judges exemptions from the WHOLE FILE (a line inside a `<blockquote>` or a 「」 quotation carries no opening marker of its own) and ignores a line merely MOVED, so a 1,000-line-gate split does not become a spelling sweep. Not the whole tree: 192 pre-existing lines in 83 files are ledgered under Principle XIII | [`scripts/house-style-hooks.sh`](scripts/house-style-hooks.sh) (matcher widened to Bash) + [`scripts/check-house-style-delta.py`](scripts/check-house-style-delta.py) `--selftest` then the scan, a PHASE of the skill Makefile's `quick` rather than a pytest test - testmon selects by changed code, and a scan whose own code never changes would sit unexecuted through the edits it exists to catch; `tests/tooling/test_house_style_delta.py` |
| **a spec does not spend a review round on what a script can find** (feature 236, item 5). Twenty `spec-fidelity` rounds over two features cost 91 minutes and at least eight of their findings were mechanical. Four checks: a measured figure (a number with a UNIT) in Summary, Functional requirements, Success criteria or Decisions recorded with no `R<k>`/`research.md` pointer in its paragraph; text a `research.md` marked `WITHDRAWN:` still standing anywhere in the TREE outside the two sections that exist to narrate a reversal; an `FR-` no success criterion names, or an `SC-` that names no FR and is not `(spec-wide)`; and a `tasks.md` citing an id the spec no longer defines. **Check 5 (feature 239, the GM's ruling 2026-09-13 on a reviewer handed numbers)**: a figure with a unit in a feature numbered 239 or later carries an `m:<key>` into that feature's `measurements.json` - written by the harness that measured it - and the recorded value must appear in the paragraph, matched numerically; a one-shot observation passes with its date and method, a Review history figure with `on round N's own run`, a figure in a backtick span is named rather than asserted; `make figures` re-runs every recorded command and restores the file, FAILING a moved count and REPORTING a timing outside its band. It exists because ten of twenty findings over one amendment's five review rounds were figures, and `spec-fidelity` now returns NOT-REVIEWABLE on an unkeyed one before reading the substance. Checks 1, 3 and 4 need a `tasks.md`, so the number claim and the milestone push this file protects still pass - check 5 does not, because spec review happens before a tasks.md exists and that is where figures reach a reviewer (two scopings of it, a feature-number cutoff and a tasks.md condition, were each ruled NOT LEGITIMATE by an independent check); check 2 scans the WHOLE TREE (GM 2026-09-12: *"You should do the tree wide scan instead of leaving this half done"* - all five of the survivals that motivated it were outside `specs/`), every tracked and kept-untracked file that is not binary - an EXTENSIONLESS one included, because a `Makefile` is where this project states a rule - holding out only a VERBATIM RECORD (`scripts/fixtures/`, `dev/*-log/`) and the two sections that narrate a reversal, with the marker required to open a line | [`scripts/spec-lint.py`](scripts/spec-lint.py) `--selftest` then the scan, in the skill Makefile's `static` beside `check-file-scale.py` and in `sync-with-main.sh` at push - the same two places, and for the same reason: a `specs/` edit touches no Python and runs no gate at all. `tests/tooling/test_spec_lint.py` proves each check fires and each stays quiet on the correct form |
| **a merge is never committed with its markers in it** (feature 241, GM 2026-09-13: *"let's talk about a tooling fix for the `git add -A` issue that keeps happening"*). Twice in one day a conflicted merge was committed unresolved - once one file, caught by ACCIDENT when `make notes-census` could not parse it, and once **23 files** (six engine modules, five test modules, five manifests, five notes files, two research pages), caught by the gate's lint phase after the commit; the history here is never rewritten, so the merge had to be redone from its base, about an hour. Both times the command was `git add -A`. **The rule is on CONTENT, not on merge state**, and that is the whole design: "refuse `add -A` while MERGE_HEAD exists" would have caught both incidents and would also refuse the correct command, because the end of every RESOLVED merge is exactly `git add -A` with MERGE_HEAD present - and it would miss a marker arriving from a `git am`, a patch script or a typo. **It departs from the proposal the GM approved**, which was the state one - and the GM then approved the departure itself on 2026-09-13, told what it costs (*"I approve your implementation"*), so it is a ruling rather than a session's carve-out; the argument is `specs/241-no-conflict-markers/spec.md` D1 under Principle XVI, and what the content rule cannot see (a binary or delete/modify conflict leaves no triple, with the one declined recovery priced) is D5. So a `git add` or `git commit` that would stage a file holding the TRIPLE git leaves (seven `<`, a line of seven `=`, seven `>`, each at COLUMN 0) is refused, naming the files. Any ONE marker alone is not a conflict - a line of seven `=` is a Markdown underline and a session writing about merges types the others - and column 0 is what exempts prose, so an indented example or a backtick span passes. **A FENCE is not an exemption**: git writes its markers at column 0 even inside a fenced block, and 7 of the 23 files were Markdown or HTML, exactly the class a fence exemption would have hidden; a file that must SHOW a triple declares `CONFLICT_MARKERS_OK:` with a real two-word reason in its first 40 lines (a reason opening with `<` is refused, `<reason>` being what a document writes where a reason goes), the `FILE_SIZE_OK` shape, and `make audit` lists them - where a MENTION of that marker is not a declaration: it stands at the start of its line and states a real reason, because the first rule (the token anywhere in the head) exempted the DETECTOR from its own backstop, which the audit listing found on its first run. **What a command would stage is asked of GIT** (`add --dry-run --ignore-missing`) rather than enumerated, with the command's own `cd`s, `-C` and `VAR=` assignments expanded - the first implementation read neither and so missed the incident's own shape, `CL=<clone>; git -C $CL add -A`, while a hand-rolled pathspec reading would have fired on `git add .` in a clean subdirectory. It REFUSES rather than narrowing to the clean files, because the compliant command stages what is RESOLVED and only the session knows which those are (feature 164's ladder, re-judged per branch by 212; the same class as `make-only`'s guard-write). Escape `CONFLICT_MARKERS_OK` with a reason | [`scripts/conflict-marker-hooks.sh`](scripts/conflict-marker-hooks.sh) + the shared detector [`scripts/_hm_conflict.py`](scripts/_hm_conflict.py), whose BACKSTOP (`--tracked`) runs in TWO places for the two reasons the checks beside it do: the skill Makefile's `static` phase, so a finding arrives before the map roll is paid for, and `sync-with-main.sh` at push time, because the delta that lands a marker is a MERGE whose own content is often docs, tests and manifests - the DIRECT route, which runs no gate at all. `--selftest` first in both, like every static check in that block. `scripts/test-conflict-marker-hooks.sh` (28 cases, including the two a state-based rule gets wrong - the resolved merge's own `add -A` with MERGE_HEAD present, and `git add .` in a clean subdirectory - and the incident's own command shape) |


**Deliberately NOT enforced**, because a guard that fires on correct work teaches a session to bypass
every guard: the caste sense of "people" (correct in narrative and vow voice), gender-neutral
office-holders (named characters keep their pronouns), Principle XI's kanji triangle, and the
behavioral principles XII/XIV/XV.

**A GUARD THAT CAN PRODUCE THE COMPLIANT COMMAND SHOULD PRODUCE IT** (feature 164, GM 2026-08-30:
*"are there places where a makefile command is refusing to do something but a tool could do a rewrite
or return additional context or whatever?"*). A `PreToolUse` hook may return `updatedInput` - the
command REWRITTEN - and `additionalContext` - a line the model reads - both at exit 0 and both
costing nothing, while exit code 2 costs a model round trip. The audit that answered the GM's
question measured **280 refusals in six days across eleven guards**, and the ladder it produced:

- **REWRITE** where the guard already names the compliant command in its own refusal (`make-only`,
  `no-poll`, `pair`, `house-style`). Never guess: a shape the matcher cannot rebuild exactly keeps
  the refusal, because a wrong rewrite costs a session its command while a refusal costs one turn.
- **TEACH FIRST, FREE** where a block cannot be avoided but the lesson can arrive earlier
  (`guard-file` at Read time, `batching` one turn below its bar, `measure` on the first run).
- **STAY A REFUSAL** where the action is destructive or irreversible, or the refusal IS the content:
  `repo-safety`, `source-block`, `readme`, `discard`, `clone-sync`'s name-routing and live-claim,
  `no-branch`, and the blocks inside `measure` and `gate`. `specs/164-guards-that-correct/research.md`
  R3 records why for each, and R6 does the same for all ten of the Makefile's own refusals - nine ask
  for a DECISION (a written reason, an authorization at a terminal, a procedure), which no
  substitution can supply.

**THE SECOND PASS (feature 212, GM 2026-09-07: *"anytime we are able to say that a command was run
incorrectly and then supply the correct thing to do, then it is at least possible that what we should
do is simply do the correct thing and then inform the session through hook context that we have done
it"*).** Every refusal in the record was recovered from the transcripts - 1,143 across fourteen
guards, 2026-08-25 to 2026-09-07 - and classified PER REFUSAL BRANCH rather than per guard, because a
guard-level verdict is how 164 converted one make-only branch and left four unexamined
(`specs/212-guards-that-do-the-right-thing/research.md` R1, and the corpus is
`scripts/fixtures/guard-refusals-2026-09.json`, replayed by `tests/tooling/test_guard_corpus.py`).
Five more branches DO the right thing: the targeted pytest run and the wrapped engine entry point
(`make-only`), the gate shape `verify` cannot take (`pair`, recorded and permitted), the file-watching
wait (`no-poll`, its qualifier fixed and the foreground form backgrounded), and the clean clone that
fell behind mid-turn (`clone-sync`, synced in). Feature 164's *"clone-sync: a procedure, not a
substitution"* still holds for name-routing and a live claim; the stale-head rule turned out to be a
single idempotent command, which is the case that class does not cover. What stays refused, and why,
is written branch by branch in that R1: the largest family is `make-only`'s `guard-write` (41 - the
compliant form is an Edit carrying a REASON, which only the session has), then `guard-file`'s
no-marker (77, the same reason), `batching` (629 - the fix is to make fewer turns, which no rewrite
of one command can do), and the destructive or decision-shaped refusals above.

**AN ESCAPE IS AN INVOCATION TOO** (feature 169, 2026-08-30). Every guard's BLOCKING decision has
been anchored through [`scripts/_hookmatch.py`](scripts/_hookmatch.py) since 2026-08-25, and every
guard's ESCAPE was still a bare `case "$CMD" in *TOKEN*)` - so a `grep` for a token, a commit message
quoting it, or a heredoc editing a document about guards counted as an escape. Every token matched in a COMMAND now
goes through `_hookmatch.py escape <TOKEN>`, which blanks heredocs and quoted strings, drops
search-command segments and drops `for VAR in` word lists. **The census of which tokens those are is
DERIVED** (`tests/tooling/test_guard_firing_log.py`): it enumerates every `*_OK` in `scripts/` and the
skill Makefile and fails on any that is not classified as `command`, `content` (a marker inside edit
text), `environment` (read as `${TOKEN:-}`, which a mention cannot set), `make-variable` or
`not-an-escape` - because three drafts of that list written by hand were each short by one. A second
check fails the build if a guard ever decides a command escape by substring again. Two reasons it mattered: the escape RATE is
the statistic this project acts on, and every one of the six recorded `measure escaped` entries was a
mention - but also **`measure` clears its repeat-measurement counter on that branch and `gate` removes
its state file**, so a session grepping for a token to learn how the escape works thereby switched the
guard off for its next command. `HOST_GIT_OK` was the worst of them (it guards git writes against the
GM's own repository) and was found by this feature's own spec review, which noticed that its audit
command would have disarmed the guard it was auditing. The one deliberate exclusion, stated at the
point of change: `pair`'s AGENT-PROMPT branch, because a prompt is prose with no command grammar.

**When you add a guard**, three properties, each learned by getting it wrong: match INVOCATIONS not
mentions (seven false positives in one feature - a grep, a commit message, a docstring, a fixture
argument, a redirect, a test harness, and a hook that blocked its own repair); check the ESCAPE FIRST
or the guard cannot be repaired through the channel it guards; and prove it FIRES by deleting it and
watching a test go red.

