# L7R Diagram - the settlement and building map generator

<!-- container-mounts: ..:/host-l7r-repo -->
<!-- container-workdir: /diagram -->

This repository is the `/diagram` skill of the GM's L5R worldbuilding project, split out of
[`EliAndrewC/gm-assistant`](https://github.com/EliAndrewC/gm-assistant) on 2026-08-25 (feature 131)
because it had *"grown into a project into its own right"* with rules the rest of that repository
should not carry: **every change here is a spec-kit feature; everything runs through `make`; the
merge gate runs on AWS CodeBuild (feature 130, once implemented).** The skill still lives at
[`.claude/skills/diagram/`](.claude/skills/diagram/) - the SAME path as before the split, on
purpose, so nothing in the engine, the pool generators or the feature-127 guards moved.

**Read next**: [`.claude/skills/diagram/SKILL.md`](.claude/skills/diagram/SKILL.md) (usage) and
[`.claude/skills/diagram/CLAUDE.md`](.claude/skills/diagram/CLAUDE.md) (the dev loop - auto-loads
when you edit under it). Building plans (Mode A) and settlement maps (Mode B) are deliberately ONE
skill and one package - the decision, and what would change it, in
[`.claude/skills/diagram/dev/skill-boundary.md`](.claude/skills/diagram/dev/skill-boundary.md). The GM's setting notes that the research cites live in gm-assistant
(`setting/`, `cosmology/`, `campaigns/`) - mounted at `/host-l7r-repo/gm-assistant` in this container (the whole `l7r` checkout is
mounted at `/host-l7r-repo`, the same mount gm-assistant uses, and this repository and gm-assistant
are both subdirectories of it),
and on GitHub at <https://github.com/EliAndrewC/gm-assistant/tree/main/setting>. The canonical
campaign notes remain `/host-l7r-repo/setting/l7r.md` in the GM's `l7r` repo; this repository never
edits them.

**A feature is identified by REPOSITORY + number from 2026-08-25.** This repository continues from
132; gm-assistant restarts at 200. Features 001-131 that concern the diagram live here with their
numbers; the five that do not (001-004, 011) stayed in gm-assistant.

## Core Rules

### Protecting the GM's Writing

Content between `<!-- SOURCE: GM NOTES - DO NOT MODIFY -->` and `<!-- END SOURCE -->` markers is the GM's original writing. These sections must NEVER be modified, rephrased, summarized, reworded, or "improved" in any way. Only the GM may edit these sections, and only when they explicitly instruct you to do so.

These blocks are **frozen historical excerpts**: they capture what the GM wrote at the time the downstream file was created and are not kept in sync with subsequent edits to `/host-l7r-repo/setting/l7r.md`. Drift between a downstream `SOURCE` block and the current canonical is expected and intentional - the block is a point-in-time snapshot, not a live mirror. The canonical for any topic is always `l7r.md`.

AI-generated content (preferences, generation instructions, examples of liked/disliked output) lives outside these markers and can be updated as the GM's preferences evolve.

This convention applies to ALL files in the project - both skill files and reference directory files.

### Skills (invocable as /slash-commands)

Each skill lives in `/.claude/skills/<skill-name>/SKILL.md` with YAML frontmatter. Skills are for *generating* specific types of content.

#### Reference Directories (not invocable, indexed by CLAUDE.md)

Reference directories hold organized source material and context. Each directory has its own `CLAUDE.md` that indexes and explains its contents. Skills should reference these directories when they need shared context.

- `/setting/` - Demographics, castes, currency, samurai ranks, government structure (Six Ministries), geography, lineage system, merchant families, ashigaru, experience levels
- `/campaigns/` - Campaign-specific material: Karmic Inquisitors, First Toshi Ranbo, Hidden Way, Wasp Bounty Hunters, timelines, PC/NPC backstories, the Order of Lord Moon
- `/hooks/` - Adventure hooks organized by type (countryside, town, city, caravan, prison camp), grifts and scams
- `/cosmology/` - Lord Moon's heavenly court, mythological stories/fables, maho & bloodspeakers, "between places", gaijin religions (Uru, Burning Sands), Fortune theology, soothsaying
- `/notes/` - Miscellaneous source material not yet organized into a dedicated reference directory

### Generation Behavior

- Preference feedback from the GM is captured in memory and incorporated into skill files over time.
- Never invent setting details that contradict the GM's source notes.
- **Hyphens only - no em-dashes (U+2014) or en-dashes (U+2013) anywhere in the project**, including generated content, webapp templates, skill files, specs, docs, and tests. This applies project-wide, not just to `l7r.md`. **Except inside a quotation of someone else's text** (GM 2026-09-06: *"The house style should not normalize british spellings or em-dashes inside things we are quoting, because that requires us to edit other people's quotes, which I don't think we should do"*): a passage quoted from a source - a footnote's 「」, a “” or straight-quoted span in prose, an HTML `<q>`/`<blockquote>` - keeps the source's own dashes and spellings, and the house-style guard holds quoted spans out of its corrections.
- **American spellings, never British ones.** Always use: `color`, `center`/`centered`, `gray`, `honor`, `judgment`, `catalog`, `labeled`/`labeling`, `behavior`, `neighbor`/`neighborhood`, `analyze`/`organize`/`recognize` (the `-ize` forms), `artifact`, `defense`, `license`, `practice` (both noun and verb), `skeptic`, `story` (of a building), `while`, `traveled`, `modeled`, `program`, `meter`, `liter`, `mold`, `plow`, `curb`, `draft`, `aging`, `marvelous`, `jewelry`, `skillful`. Never their British counterparts (the `-our`, `-re`, `-ise`, `-ce`-noun, doubled-`l`, and `-ogue` forms). This applies project-wide and to **everything**: generated content, prose, docs, specs, skill files, webapp templates, tests, comments and docstrings, **and code identifiers** (variable names, parameters, dict keys, CSS class names). The exceptions are the GM's own writing and a verbatim quotation of another source (the same ruling as the dashes above, GM 2026-09-06: a quoted span keeps the source's spelling) - never "correct" text inside `<!-- SOURCE: GM NOTES -->` blocks or in `l7r.md`, and leave direct quotations of it alone.
- **Constitution Principle XI** (Japanese Authenticity): any kanji that surfaces in generated content - relic names, sword names, given names, temple titles, vow refrains, decorative stamps - must pass the kanji ↔ romaji ↔ meaning triangle. Real characters, plausible reading, English meaning that maps back. Stylized readings are allowed when explained in surrounding prose.
- **Record the "why" of every research-driven rule (REQUIRED).** When historical (or setting) research leads us to a concrete generation rule, automated check, or magic number - "every farmhouse had a work yard," "~30% of farms had a storehouse," "threshing was per-household, not communal" - we MUST capture the *reasoning* alongside the *rule*, not just the rule. Encoding the finding into a check or generator is necessary but **not sufficient**: a bare `count >= 0.3 * n` teaches a future reader nothing about why 0.3. So write the finding down where the rule lives - a "Historical grounding"/research section in the skill's `SKILL.md`, or a comment next to the check - covering what the research found, the decision it drove, and any deliberate departures from literal reality (e.g. features drawn larger than true scale for legibility while keeping *relative* sizes roughly honest). The *why* is mandatory, and since 2026-08-27 (constitution v2.10.0, GM) **so are the sources**: every research finding ends with a `**Sources:**` line, registered by key in `research/SOURCES.html` with what each was used for **and the URL where it can be read** (v2.13.0, GM 2026-08-28: the interactive map links its references to them; `URL: none - <why>` when there is none) - the interactive map will owe its reader the source behind each claim. Primary and scholarly work first, then serious references (and an encyclopedia article's own references over the article); **never an AI-generated encyclopedia such as Grokipedia** - machine-rewritten, no editorial community, no provenance a reader can follow; a web-search summary is a pointer to sources, never a source. **And read what you cite** (v2.11.0): a source is cited only after the page or paper itself has been fetched and read - a search summary or another page's paraphrase can state the opposite of what the source found - and a claim taken from a summary of a source that could not be read is NOT cited (v2.19.0, GM 2026-09-06, feature 195, superseding the 2026-08-27 SUMMARY-ONLY clause: *"If we are linking to online sources whose content which is quotable from public sources does not support our claims then we should not cite it. For example, even if a given source is "known" to support a point we are making, if we are not able to simultaneously quote a relevant passage with a quote which actually backs up our assertion and then link to a page on the public internet where that quote can be read, then we should NOT be claiming that the source supports us."*) - the claim may still be asserted, its footnote an ABSENCE note (no key, no link, what was searched and when), the registry entry kept as the record of the search; **and a foreign-language passage is quoted in English translation, marked as one** (v2.21.0, GM 2026-09-07, feature 202: *"for foreign language things we want to quote the English translation rather than the original text but we also want to note that it is a translation"* - the note names the language and the translator, the original follows the note as the checker's anchor); the one exception to the citation rule is the GM's own campaign notes, canon rather than evidence (GM 2026-09-07: *"it is correct to make L7R setting notes an exception to the citation rule, so that should indeed be a documented exception"*). **Dispatch the reading to the `source-reader` agent** (`.claude/agents/source-reader.md`, verification, not judgment; on Opus like every check agent since GM 2026-09-07): give it each claim verbatim with its pointer and it returns READ with a quote, SUMMARY-ONLY, CONTRADICTED or NOT-FOUND; the session then writes the entry from the quotes. Running it in the background keeps the page dumps out of the main context. This protects against having to redo the research when memory fades or the context window rolls over. Applies to any generator (skills, the webapp, future tools), not just `/diagram`. **And QUOTE what you cite** (v2.18.0, GM 2026-09-06, feature 194): *"we quote the passage or passages from the reference which support the assertion that we are making. There is no point in including a reference if it is not being quoted"* - a citation is a FOOTNOTE at the assertion carrying the key, its link and the quoted passage(s) verbatim, one per assertion, several in a sentence that makes several; the `quote-check` agent (`.claude/agents/quote-check.md`) confirms the quote is on the page, supports the assertion, and that every relevant assertion has one, before the entry lands; **the research record is HTML** (`research/*.html`, hand-authored, the footnotes shown on hover) and the maps link to it locally - the form and the rule are in [`.claude/skills/diagram/research/CLAUDE.md`](.claude/skills/diagram/research/CLAUDE.md). **And since feature 211 (GM 2026-09-07) the notes live on a CITATIONS PAGE beside each research page** (`research/citations/<name>.html`, the research page's hover reading a derived `citations/<name>.js` that `make citations` writes), **every cited work's registry entry says what it is and why it applies with its honest limits** - two write-ups, written once and derived into the works list at the top of every citations page that cites the work - **and a source is judged by the `source-applicability` agent** (`.claude/agents/source-applicability.md`) when its write-ups land and BEFORE a session integrates its numbers into a map: *"whatever subagent check we create in order to justify whether a source is applicable to be used in the creation of our diagrams, that subagent check should also be run when we first begin to make use of the source prior to integrating its numbers or claims or details into our maps."*
- **WHAT THE RECORD IS FOR - the reader who will click on it (GM 2026-08-26, constitution XII).** The long-term goal is an interactive HTML rendering of every map (the pipeline is already coordinates -> SVG -> PNG; HTML is one more target): a player hovers a feature, sees it highlighted, clicks it and learns what it is, why it is there, and whether that is **historically accurate**, a **deliberate deviation** (the SETTING differing from the history it is based on - Legend of the Five Rings canon; showing a feature type on the sheet - the near samurai estate; a priced trade-off), a **map drawing convention** (a glyph drawn at a different scale or color than the feature would have, so the map reads to a human eye - the oversized well, the dark bund beads; feature 183, GM 2026-09-05: *"we should distinguish in our descriptions between 'deviations' ... and 'map drawing conventions'"*), or a **guess** made because the record has no firm number. On a city map, highlight every tannery and learn that a tannery stands by water because hides are soaked. So every rendering decision - glyph, size, placement rule, distance, density - is recorded in one of those four classes, in `research/` (the finding), the operative doc (the rule) and at the point of change (the pointer), and listed in the feature's spec under "Decisions Recorded". An unlabeled guess is the one failure: the reader must never be told a guess is a finding. **And the reader reaches the record by QUESTION** (GM 2026-09-05, feature 180): the audience is *"casual RPG enthusiasts who might be interested to learn a little more about why these settlements are the way that they are"*, so a modal's "See references" lists the research headings the feature was written from - the questions we asked - each linking to its section on the public GitHub rendering of `research/`, where the sources are; the third-party works themselves are one click further out, never in the reader's face. A research heading is therefore written as the question a reader would ask from the map, and its anchor is stable. **And the entry itself is written for that reader** (GM 2026-09-07, feature 209): a term they would not know is a glossary tooltip exactly as on the map (one glossary, `interactive/glossary.py`, derived to `research/assets/glossary.js` by `make glossary`); anything addressed to a session - `Grounds:`, `Evidence:`, a feature number, a task id, an engine identifier, a fetch verdict - is an HTML comment; and nothing in the entry says what it used to say or when it was corrected (*"we can look it up in our version control history"*). The `record-format` agent checks all three on every changed entry beside `quote-check`; `tests/interactive/test_record_format.py` holds the mechanical shapes. The sensibility in full: [`.claude/skills/diagram/research/CLAUDE.md`](.claude/skills/diagram/research/CLAUDE.md), "Who the record is for".
- **Record a decision to ACCEPT a limitation, and the alternatives that were declined (REQUIRED)** (GM 2026-08-17: *"we should always document this kind of decision... that way if we look it up later, we'll know it was a deliberate decision"*). The rule above covers a decision that produced a rule; this covers the other kind - where we looked at something imperfect and deliberately chose to leave it. Undocumented, those are indistinguishable from bugs, and the next session "fixes" them. So write down **what was accepted, what it costs in observable terms, which alternatives were priced, and who chose** - the rejected options matter as much as the chosen one, because they are what stops the question being reopened from scratch. Worked example: [`research/water.html`](.claude/skills/diagram/research/water.html#what-drawing-at-true-size-left-open) "What drawing at true size left open" (the taper is sub-perceptual at true scale) - the GM asked why a channel did not visibly narrow, the honest answer was that at true scale it cannot, two legibility multipliers were priced against keeping true size, and the ruling plus both declined numbers are recorded where the next reader will meet the map.
- **RESEARCH BEFORE YOU ASK FOR A RULING (REQUIRED)** (GM 2026-08-18, constitution Principle XII). A question about how a place was actually built, farmed or lived in is a RESEARCH question. Run the search pass FIRST; the GM is asked only when the record turns out silent or contradictory, and the ask must say what was searched, what was found, and why it does not settle the matter. This binds the review loop hardest, because that is where these surface: a reviewer writing *"this wants a one-line ruling"* has identified a QUESTION, not delegated it. Two questions queued for the GM on 2026-08-17 - whether a byre may stand beside a wellhead, and whether a hamlet's back rank needs a way - were both answered by one research pass the next morning.
- **A GUESS IS THE LAST RESORT - RUN THE RESEARCH PASS FIRST, ALWAYS (REQUIRED)** (GM 2026-08-26, constitution XII). Not only before asking the GM: before making ANY decision about how a place was built, farmed, planted or lived in whose answer you do not know - from the GM, a reviewer, a test or your own doubt, however small. The "guess" label is for a record that was searched and found silent. *"That is the kind of project that this is."*
- **TWO SUPPORTABLE ANSWERS BECOME A KNOB, NOT A CHOICE (REQUIRED)** (GM 2026-08-18, constitution Principle XII). Where the research shows a thing was genuinely done more than one way, do NOT pick the reading you prefer: make it a **tunable knob with per-settlement variance**, rolled from the map's own seed like every other knob. The reason is a project goal rather than a historical one - these maps exist for players who must tell one settlement from another at a glance, so *"we want settlements which are within historical norms while being as different from one another as is justifiable by our historical research"*. Every place the record permits two forms is a place two maps can honestly differ, and picking one throws that away permanently. The ladder: research it; if decisive, implement what it says; if it supports two forms, add the knob; only if it is silent does the GM rule. (The older "calibrated liberty" clause still covers a DEGREE along a continuum - how large, how dense, how often - never a choice between distinct FORMS.)
- **"People" has caste meaning.** In the Celestial Order only samurai are "people"; heimen are "half-people", hinin "non-people". In demographic, statistical, or analytical writing use `humans` / `inhabitants` / `population` / a specific caste term - never "4 out of every 5 people work the land". `people` is fine for samurai specifically, and in narrative, lore, dialogue, vow and folktale voice. Full rule in [`docs/l7r-style.md`](docs/l7r-style.md) (copied from gm-assistant; the canonical copy is there).
- **"Domain", never "demesne"**, for territory administered at any tier, including compounds ("Imperial domain"). Silently fix `demesne` -> `domain` in any text you edit.
- **Gender-neutral office-holders.** Use `they`/`their`/`them` for a GENERIC daimyo, governor, magistrate, minister, or samurai; specific named characters keep their own pronouns.


## Development Workflow

This project uses spec-driven development governed by [`.specify/memory/constitution.md`](.specify/memory/constitution.md) (v2.2.0 since 2026-08-25 - this repository's edition; 18 numbered principles, I and II not applicable here, 11 NON-NEGOTIABLE). The constitution is the higher-level authority; this CLAUDE.md operationalizes it.

**When to use spec-kit:**

- **Feature work** (new sections of the webapp, new skills, new generators, the upcoming Python backend) → invoke `/speckit-specify` and follow through plan → tasks → implement. The Constitution Check section in `.specify/templates/plan-template.md` is the gate that enforces the constitution at plan-time; skipping spec-kit on feature work means skipping that gate.
- **Tweaks and iteration** (wording fixes, regenerating one item, fixing one bug) → just do the work directly. The constitution still applies, but the formal spec/plan/tasks flow is overkill. **EXCEPTION - diagram ENGINE code always has a spec-kit feature** (GM 2026-08-24, feature 130 FR-011: *"Anything involving the diagram skill is sufficiently complicated to require a spec kid feature"*): a push whose delta touches `l7r/**/*.py` or `pool/*.gen.py|*.json` - the code the tests exercise; **the interactive page's `.js`/`.css` assets are a TWEAK, not engine code** (feature 188, GM 2026-09-05: *"there's no actual reason to rerun all the tests for style sheet changes"*), **and so is a modal's explanation - it is a DOCSTRING** (feature 189: `interactive/classes/`, parsed at import; the gate key strips docstrings, so rewording one owes the same `make page-check`) - an asset-only delta owes a green `make page-check` (the interactive tests and the browser test, about a minute) at push, no spec-kit feature, no review, no task file, and the pages regenerate on landing through the render fingerprint (feature 187); not `tests/` or `l7r/diagram/ci/` (GM 2026-08-25, feature 132 FR-024/FR-025: a tests-only or ci-only change skips the BUILD - **and since feature 178 a ci-only change no longer skips the LOCAL GATE**, because the coverage floor MEASURES `l7r/diagram/ci/` and a surface that owes 100% cannot also be able to short-circuit the gate enforcing it; the money half of FR-025 stands, the local-gate half is superseded, GM 2026-09-03: *"I think a short-circuit for 'measured but engine' is best"*), and not the Makefile, pyproject, the lockfiles or the hooks, which shape how the gate runs - takes the GATED route, and the gated route refuses to dispatch unless a named feature is complete (no open task, spec FAITHFUL). A docs-only change under the skill, or anything outside it, needs none.
- **Ambiguous cases** → ask before chain-firing `/speckit-specify`.
- **Every task is classified `research: rendering` or `research: physical` when it is created, and a physical task carries its research as checkboxes** - `research pass`, `source-reader confirmed`, `recorded and cited` (GM 2026-08-27, constitution v2.12.0), `quote-check confirmed` (feature 194) and, from feature 211, `source-applicability confirmed` (GM 2026-09-07: the `source-applicability` agent judged each new source BEFORE its numbers, claims or details reached a map or a rule) - that must be ticked before the task is. A rendering task is a map convention with nothing physical behind it (a caption's wrap); a physical task is about how a place was built, farmed, planted or lived in (a house's distance from the paddy). "The record already answers it" ticks the research-pass box WITH the pointer; enforced by `tests/test_task_research_boxes.py` at the gate.
- **Run the chain end-to-end, unattended** (GM 2026-08-15). Spec-kit here is externalized working
  memory - the same discipline that keeps a handoff to a human from dropping work: break it down,
  write it down, mark things off only when verified. It is NOT stage-gate ceremony, and the
  constitution is maintained pragmatically rather than artisanally. When implementing a spec-kit
  feature, run specify -> plan -> tasks -> implement straight through without pausing for GM
  approval between stages, answer each stage's own questions yourself, and record every resolved
  decision in the artifact where it arose.
- **Stop-and-ask calculus** (GM 2026-08-15, narrowed 2026-08-23): interrupt the run ONLY when a
  wrong guess would be expensive to unwind - an hour-plus of implementation that might be thrown
  away buys a question first. When a choice is cheap to adjust after the fact, make the call, finish
  the task, report what was chosen, and offer the adjustment. Asking beats redoing; redoing beats
  blocking on trivia. **This covers AMBIGUITY about what is wanted, and nothing else.** Difficulty
  in delivering something already known to be wanted is governed by constitution **Principle XV
  (Keep Going)**, and there the answer is to keep working.
- **THE GM STARTS WORK AND LEAVES** (GM 2026-08-23, constitution XV, NON-NEGOTIABLE). Stopping to
  ask "which of these should I do?" does not cost seconds of their attention - it costs the whole
  span until they return, and they come back to find the work where they left it. When a path
  forward exists, TAKE it; if one avenue is blocked, work the parts that are not. The only reason to
  stop is a genuine belief the thing cannot be done at all. If the options list contains "fix it and
  make it work", that is the answer and it does not need confirming. Two limits: this is never a
  license to exceed ordinary authorized work, and persistence means continuing to make PROGRESS, not
  continuing to make CHANGES - when stuck, the next step is a MEASUREMENT, not another speculative
  edit.
- **DO THE LITERAL THING; AN EXCEPTION IS NOT YOURS TO APPROVE** (GM 2026-08-24,
  constitution Principle XVI, NON-NEGOTIABLE). Asked for X, build X - never "X except
  where Y". This is the exact tension the project's own answer to spec-kit creates:
  sessions are told to answer their own questions rather than stop constantly, and the
  failure mode of that instruction is a session quietly resolving a question the GM
  had already answered. **The resolution is that exceptions are presumed wrong.** If
  one still looks necessary, an independent Opus 5 subagent decides - given the GM's
  request VERBATIM - and the honest question to put to it is "am I carving out a case
  contrary to what I was told?". If it agrees, carry on and raise it with the GM once
  the implementation WORKS; the GM starts long work and comes back to a finished
  thing, so a mid-flight question costs them the whole span.
- **A SPEC IS REVIEWED AGAINST THE GM'S OWN WORDS, BY SOMEONE ELSE** (same principle).
  Before implementation, hand the finished `spec.md` and the GM's request AS WRITTEN to
  an independent subagent: does this specify what was asked, and does it add anything
  that was not? Not the plan - a spec graded against its own plan is being checked for
  self-consistency, which a wrong spec passes. Adjust and re-review up to **five**
  times (raised from three by the GM on 2026-08-30); if the FIFTH round still returns
  changes, STOP and escalate, because that many failures means a persistent
  misunderstanding another attempt will not find. The cap exists to end an argument
  going nowhere, NOT to cut off a review still finding new, smaller things each round -
  which is what it did on feature 169, where round 3's only item was one more missed
  token in a hand-written list. **Watch the new number**: if features start hitting
  five, the drafting is the problem, not the cap. **THE CAP COUNTS THE INITIAL REVIEW ONLY** (the GM (2026-09-12): *"we should probably clarify the five round cap on reviews. That is intended for the initial creation of the spec ... it is completely fine to go past five rounds because what you're really doing is you're resetting the counter every time you make a change ... once the spec is accepted, then we set the counter to zero the next time that any changes are made mid implementation or later"*): the five rounds are the spec's first acceptance; a spec amended AFTER it was accepted - mid-implementation, because the design changed under measurement, or later - is re-reviewed on a counter reset to zero, five rounds again from the amendment. Feature 226 is the case: four rounds and a FAITHFUL fifth before implementation, then three more on the text amended when its one-blob design became two outlines - which is not the persistent misunderstanding the cap exists to end. The
  motivating case is feature 126's FR-003, which said "farmhouses before lanes EXCEPT
  the connector and the field spur" when the GM had said "farmhouses before lanes".
- **One constitution for the whole repo, deliberately** (GM 2026-08-15): spec-kit's Constitution
  Check reads a single file, and the domain separation the project needs already exists in layers -
  the constitution carries universal principles; each domain's operational doctrine lives in its
  auto-loading directory CLAUDE.md (the diagram skill's is the exemplar); SKILL.md stays the
  usage-facing index. A rule that applies to only one domain belongs in that domain's CLAUDE.md,
  not in a forked constitution.
- **NO FEATURE BRANCHES - spec-kit work included** (GM 2026-07-27). Isolation already comes from the session clone; a branch on top of it is a second axis that buys nothing and broke the stop-work procedure for a whole session. Branch creation is off (`.specify/extensions.yml`, `before_specify`, `enabled: false`) and [`scripts/no-branch-hooks.sh`](scripts/no-branch-hooks.sh) blocks a hand-rolled `git checkout -b` (escape hatch: `NO_BRANCH_OK` in the command, with a reason). Spec-kit still needs to know which feature is active: **`export SPECIFY_FEATURE=NNN-slug`**, which `common.sh`'s `get_current_branch()` returns ahead of asking git, so `check_feature_branch()` in `setup-plan.sh` / `setup-tasks.sh` is satisfied with no branch at all. **Export `SPECIFY_FEATURE_DIRECTORY` alongside it** - `export SPECIFY_FEATURE_DIRECTORY=specs/NNN-slug`. `SPECIFY_FEATURE` alone is NOT enough and the difference is silent: `common.sh` resolves `FEATURE_DIR` from `SPECIFY_FEATURE_DIRECTORY` (priority 1), then `.specify/feature.json` (priority 2), and only then from the branch name that `SPECIFY_FEATURE` supplies (priority 3) - so with a stale pointer present, `SPECIFY_FEATURE=115` yields `CURRENT_BRANCH=115` and `FEATURE_DIR=.../116`, and spec-kit writes into a PEER's feature directory while every log line looks right. Measured 2026-08-16 on features 115/116.
- **Concurrent sessions: a spec number comes from `make claim SLUG=<slug>`, under a lock - never
  from a scan by hand** (GM 2026-09-07, feature 197; this supersedes the 2026-08-16 claim-in-main
  protocol). The GM runs several sessions at once, and each used to allocate its own `specs/NNN` by
  reading "highest `NNN` under `specs/` + 1" after `sync-in` - which two sessions did at the same
  moment often enough that fourteen numbers were claimed twice, eight renumber commits landed in
  one week, and main carried two features numbered 195 until the GM had one moved to 198 on
  2026-09-07 (the census is `specs/197-claim-feature-numbers/research.md` R2). The old protocol's blind spot was structural: a
  sibling's claim lives in `<mirror>/.clones/<other>/specs/` until that session pushes, and nothing
  read it. `make claim` (`scripts/claim-feature.py`) takes one `flock` on the mirror
  (`<mirror>/.specify/feature-numbers.lock`), reads every place a claim can be - main's `specs/`, its
  fetched `origin/main`, EVERY clone's `specs/`, and its own ledger
  (`<mirror>/.specify/feature-numbers.jsonl`; gitignored host state, derivable from the directories it
  records, so a lost one is recreated rather than restored) - creates `specs/NNN-slug/` in YOUR clone
  before releasing the lock, writes `.specify/feature.json` and prints the two exports below. `PEEK=1`
  looks without claiming. It refuses from the mirror, a slug that is not lower-case kebab, a slug
  already in use anywhere, and a lock held past 30 s; it WARNS about any number main holds twice and
  never renumbers on a claim (that is the GM's call - and when they make it, `make claim
  RENUMBER=specs/NNN-slug` moves the directory to the next number under the same lock, which is how
  the 195 pair was resolved). Then write `spec.md` there, commit the directory
  and push it (`scripts/sync-with-main.sh push`, a mid-feature milestone push): the claim is already
  visible to every session on this host through the clone scan, and the push makes it visible to the
  GM's laptop and to GitHub. The one window the lock cannot see is a spec pushed to GitHub from
  OUTSIDE the container between the last `sync-in` fetch and the claim; if a pull ever surfaces one,
  renumber yours before pushing, exactly as before. Do NOT coordinate numbering by messaging peer
  sessions: a busy session replies late or never, while the lock serializes with zero cooperation.
  Full protocol (and what peer messaging IS for) in
  [`docs/session-clones.md`](docs/session-clones.md).

**FIXING IT IS THE EXIT.** Principle XIII's three ways out are not a menu (GM 2026-08-23): FIX is
the expected outcome, REVERT requires a WRITTEN impossibility investigation rather than a
preference, and a waiver is the GM's to grant after a fix has genuinely been attempted. A session
that has found a path forward takes it.

**NO KNOWN REGRESSIONS - and nothing merges to main carrying one** (constitution Principle XIII, NON-NEGOTIABLE, GM 2026-08-17: *"never count our work as being done when there are known regressions. Nothing should EVER be merged back into main if even one single new regression was added."*). Two separate bars, and work routinely clears the first and fails the second:

- **A regression is measured, not remembered.** It is anything that passed before your change and fails after it - a test, a pool artifact, a cohort seed, a rate. Take the baseline on unmodified code in a **detached worktree** (`git worktree add --detach /tmp/base HEAD`), never by stashing - a stash mutates the tree under any review agent currently reading it. **And check each failure the worktree reports against your clone before calling it pre-existing**: a fresh worktree carries no GITIGNORED artifacts, so tests that read them fail there for reasons that have nothing to do with the code (measured 2026-08-24: 2 such failures, 20 pool PNGs in the worktree against the clone's 28). The reverse is the dangerous half - the same gap can make a test pass ONLY in the worktree, hiding a real regression from the moment the baseline is taken.
- **Pre-existing failures are NOT regressions.** They stay ledgered and are not fixed under someone else's feature. That distinction is exactly why the baseline is mandatory.
- **None of these excuses one**: it is small; it is "only" a cohort seed or a fixture; you documented it (a ledger entry tracks a regression, it does not permit one); the change fixes more than it breaks; or the residue "ROTATED" because a re-roll moved which seeds fail. On rotation specifically - where per-seed comparison survives, a check that passed on a seed and now fails is a regression; where the re-roll makes per-seed comparison meaningless, the pass RATE must not drop **and** every newly-failing check must be individually diagnosed.
- **Three exits only: fix it, revert it, or get an explicit GM waiver** for that specific regression. If you cannot fix it, STOP and say so - the work stays in the clone, **unpushed**. `sync-with-main.sh done` is not run on a regressed state; committing inside your own clone is still fine and correct (mid-task work is sacred).

**FIX DEFECTS WHERE YOU FIND THEM - bugs before new code** (constitution Principle XIV, NON-NEGOTIABLE, GM 2026-08-17: *"anytime we are working on the diagram skill and you in the course of implementing a feature come across some new defect - even if it is a defect that did not have anything to do with what you were working on - I would like you to fix it as part of that work ... in general, we should fix bugs before writing new code."*). A defect you find while doing something else is fixed IN that work, with the same verification anything else gets - not filed, not deferred to "its own pass". The **only** exception is a fix that would be a complete overhaul or a giant architectural change (a stage reordering, a new subsystem, a placement engine rewritten), and deferring one is a deliverable rather than a shrug: it carries the MEASUREMENT that establishes the defect, the mechanism, and an implementation sketch.

- **Do NOT reach for Principle XIII's "pre-existing failures stay ledgered."** That clause is about what BLOCKS a push. This one is about what you owe a defect you have actually seen. A pre-existing failure you never touched does not stop you shipping; one you found gets fixed.
- **Most of these arrive from the review subagents**, which are pointed at a DELTA and reliably find things outside it - that is an independent reviewer working, not scope creep. Same for a defect a diagnostic surfaces, a number that looks wrong while you were measuring something else, or a comment describing code that no longer exists.
- **The why is compounding** (the GM's own reasoning): the point of these generators is to expand them onto a foundation whose behavior is known-good. Every defect left in place is one the next tier inherits and builds over, entangled by then with work that assumed it. Keeping the floor level is what lets the building get taller.
- **Record a fix that FAILED, at the point of change.** A cheap wrong lever that measurably did nothing saves the next session from pulling it again - see the front-row lane cap's two recorded dead ends in [`hamletgen/homesteads.py`](.claude/skills/diagram/l7r/diagram/hamletgen/homesteads.py).

**Verification before reporting "done"** (per Principle VI of the constitution):

- **UI changes** (per Principle I, expanded in v1.3.0):
  - Run `webapp/tests/screenshot.py` to produce **multi-scroll contact sheets** at GM-100 / GM-200 / tablet / mobile. For pages taller than 1.3× viewport, the script captures 0%/33%/66%/100% scroll positions and stitches them horizontally - so layout asymmetry, dead space, and below-fold problems are visible at a glance.
  - Run `webapp/tests/dom_audit.py`. It must report **zero issues** across all pages × viewports. The audit now covers BOTH clipping (overflow, ellipsis, line-clamp) AND layout balance (sibling-height ratio inside flex/grid containers must not exceed 2.5×).
  - **Persona-driven review pass**: before declaring done, examine at least one contact sheet at GM-200 with the user's task in mind (not the implementer's: "Eli is opening this page; what is he trying to do here?"). If the same agent both implemented and reviewed, **invoke the `frontend-review` subagent** (`.claude/agents/frontend-review.md`) to get an independent pass. Author ≠ reliable reviewer.
- **Python changes**: `ruff check` + `ruff format --check` + `pyrefly check` (the mypy-strict rule set, feature 142) + `pytest`, and the coverage floor - `coverage report --fail-under=100` over the whole engine, run by a plain `make done` (feature 174). It is NOT a pytest flag: `pytest-cov` is never given one and `[tool.coverage.report]` deliberately omits `fail_under`, so the floor runs LAST and a run's own failures are reported before the coverage table.
- **100% COVERAGE IS OWED BY EVERYTHING, THE DAY IT LANDS - there is no opt-in (GM 2026-09-02, constitution Principle X clause 5, NON-NEGOTIABLE).** *"A new tool absolutely should silently owe one hundred percent coverage the day it lands. Going forward, we want one hundred percent code coverage, period. That was not previously the case. We now want that to be the case always. For tools, for our settlement generation, for the automated checks on our hand drawn diagrams, for everything."* Enforced **in the standard manner** - a floor on the gate (`coverage report --fail-under=100` in the skill Makefile) with the measured surface DERIVED (`source = ["l7r"]`) - never by a ratchet and never by a roster somebody maintains. **Do NOT put `fail_under` in `[tool.coverage.report]`**: `pytest-cov` reads it there, so it would fire on every partial run (`make test-file`, `make quick`) and would break the deliberate ordering the floor depends on - it runs LAST, after a run's own failures are reported. The GM's *"for example ... and such"* names the shape, not the file. **The failure this replaces**: `[tool.coverage.run] source` in the skill's `pyproject.toml` named the measured modules one by one, so 19 engine files / 1,843 statements owed nothing, and its own comment argued FOR that - a new tool must not "silently owe 100% coverage the day it lands". The GM reversed that sentence verbatim. So a module's coverage duty follows from it being engine code; if you add a file under `l7r/`, it is measured, and the way to not owe coverage on something is to not ship it. A boundary that genuinely cannot be reached is stated at the point of change, like any other exclusion, and is argued rather than assumed.
- **AN INNER FUNCTION THAT IS HARD TO TEST GETS LIFTED OUT (GM 2026-08-28, feature 146).** *"If something is only available as an inner function in a closure, then you can move it out into its own function to make it more unit testable. That is a common unit testing paradigm ... you can generally have your unit tests be much simpler if you're just calling functions that take simple inputs and outputs without needing to create a lot of very complicated setup."* The failure this replaces is real and was in this repository's own commits: a session that met a closure it could not reach wrote *"dropped (nested closure)"* and moved on, leaving the branch untested. **Dropping the test is not one of the options.** Lift the closure to module level with its captured values as parameters, have the inner one delegate so there is ONE body, and test the lifted function with plain dicts and tuples - `web_pieces`, `web_rejoinable` and `fan_rival` are the worked examples, each of which previously needed a whole settlement, a fabric list and a water list to ask a yes/no question. The exception is narrow and must be stated where it is taken: an inner function that genuinely simplifies the implementation AND whose extraction would cost measured performance. *"There usually is not"* such a benefit.
- **AN OVERLAP CHECK WITHIN A MAP IS PERFORMED IN ITS EFFICIENT FORM (GM 2026-09-08, feature 218, constitution Principle X clause 15).** *"We will need to take care to make sure that We do not have literally every item on the map checking for overlap with literally every other item. or anything silly like that."* Any test of a candidate against the features already on the map - a tree against the crops, a scatter mark against the treads and the water, a parcel against the houses - builds the geometry that does not change during the scan into an index ONCE (`settlement/_geom/indexes.py`: a bounding-box prefilter, a `PointGrid`, a `RingIndex`, a `KeepoutGrid` holding every keep-out of one scatter) and asks the index per candidate; the index prunes, the exact test decides, so nothing is coarsened and the maps do not move. The failure this names was measured the day the rule was written: the windbreak tested each of 37,490 candidate clumps against every edge of every crop polygon (7.2 million segment distances, 77% of a 7.3 s stage) beside an `Indexed` registry the farmhouse placer already queried, and the marsh scatter rebuilt its whole watercourse list per point; both stages went to under 2 s with byte-identical maps. It governs new code and code a profile shows costing time (`make map PROFILE=1`, then `make perf-profile`); a plan that adds an overlap check says how it is indexed. Not mechanically enforced and no subagent reviews it today, by the GM's ruling; the future review of overlap-checking code is recorded in `specs/218-efficient-overlap-checks/spec.md` D4.
- **Files stay at human scale - GATED since feature 173** (constitution Principle X clause 13, GM 2026-08-15; tests included per v1.6.1, GM 2026-08-16; **enforced** per v2.14.0, GM 2026-08-31): a source OR TEST Python file past 1,000 raw lines FAILS `make lint`, and so `make done` and both routes to main - `scripts/check-file-scale.py`, which prints the split procedure with its refusal. It was an ask-the-question line for two weeks and the question stopped being asked: the GM found ten files past the bar from their own laptop, `hamletgen/ways.py` at 4,369, and ruled *"we fail the gate with a message that explains that the clone responsible for this work must split up the file in the manner prescribed in our project guidelines"*. The answer to the question is still the same one - "should this become a package of subfiles with its own CLAUDE.md index?" - it is just no longer optional. The cost being managed is context-window tokens - loading a huge file to use one part of it - and a test file is loaded under the same conditions as source (you load a test file to modify one test the way you load a source file to use one function), so tests get no exemption. Exemplar: `.claude/skills/diagram/l7r/diagram/settlement/`. Ordered-data files (registries) may stay large with an inline justification - the only carve-out, and it protects only rows stating real DECISIONS (execution order, curation, hand-written metadata that exists nowhere else). **The justification now lives IN the file** as `FILE_SIZE_OK: <reason>` in its first 40 lines, at least 40 characters because a whole-file exemption is argued rather than tokenized, and `make audit` lists every file taking it - a carve-out nobody can enumerate is one nobody revisits. It shipped with zero files using it, which is the point: `spec-fidelity` rejected the one exemption feature 173 proposed for itself (a hand-authored map draw script - "execution order is a contract" is true of nearly every imperative module here, so admitting it would let the carve-out swallow the rule). **A roster that merely restates what code elsewhere already declares is DERIVED, not maintained or split** (clause 14, v1.7.0, GM 2026-08-16): census who actually consumes each name (most of a grown roster has zero consumers), move the roster's safety property into a guard test proven to fire, then derive the surface - star imports for re-export `__init__`s, introspection for derivable registry rows. Exemplar: feature 027 collapsed the check battery's `__init__.py` from 3,148 lines to 63 with zero consumer changes; full method in `specs/027-init-star-imports/`.
- **Delegated work**: spot-check actual artifacts before relaying success to the user. "The subagent said it was done" is not sufficient.
- **A HAMLET IS ROLLED ONCE PER GATE, AND ROLLING ANOTHER IS A RECORDED DECISION (feature 213, GM 2026-09-07).** The packing record had measured 11 rolls and a floor of 8-9; a week later a census counted 37 rolls of 14 specs, because nothing enforced it (*"we definitely had this solved at one point, and then the problem just came back on its own ... program our unit tests to never allow the same hamlet to be rolled twice within the tests and also to have some required process around adding another hamlet that gets rolled"*). So `tests/rolls.py` is the ROSTER of specs the gate may roll, each with the unique coverage or emergent condition it carries, and the gate's test phase writes a roll census at the one chokepoint every roll crosses (`driver.roll_scope`) and judges it afterward: a spec rolled twice, a spec not in the roster, a roster row nothing rolled, a render from a test not marked `renders`, an in-process roll from an unexcepted module - each fails the gate and names the tests. A rolling test gets its roll from the roll cache (`rollcache.hamlet` / `report` / `keyed_to(..., child=)`), in a child, shared across workers behind a lock. The rule and the mechanics: [`.claude/skills/diagram/tests/CLAUDE.md`](.claude/skills/diagram/tests/CLAUDE.md).
- **THE GATE ROLLS ONLY WHAT THE FLOOR NEEDS (feature 216, GM 2026-09-08, constitution VI v2.23.0).** *"the make done tests are trying to minimize the number of map rolls and are doing only what is strictly necessary in order to reach one hundred percent code coverage. the correct place for the kind of test in which we make more map rolls than are strictly necessary is in the AWS tests. or the CI tests if we want to speak of it generically. Currently, that is not a type of test that we actually run, but that is the correct place for that type of test if we ever decide to create it."* A gate roll's reason is a set of engine lines nothing else reaches - `make roll-audit` lists them per roll off the gate's coverage baseline (`--min-lines` is the tier knob: a hamlet roll is ~7,000 lines, a town's more), and a roll whose set is empty is packed onto a roll already made, or its assertion becomes a unit test of the placer, or the test moves to `tests/soak/` (the tier above the gate, collected by no ordinary run; `make soak` names it). Three days of this - features 213 to 216 - took the gate from 37 rolls of 14 hamlets and a 541 s median to 3 rolls of 3 and a gate under three minutes, and the audit found on the way that most rolls justified by "the test needs its own map" reached no line the shipped maps' own rolls did not. **And since feature 217 (GM 2026-09-08) the gate MEASURES it**: the census verdict fails a rostered roll whose coverage context reaches no engine line no other context reaches, and prints every roll's count and lines on every gate, green or red; `tests/rolls.py` is a GUARD file (an edit needs `GUARD_EDIT_OK` with a reason, its Read-time context quotes the doctrine, and a change to it makes the next gate FULL), and every row points at the research section recording the `make roll-audit` run that justified it (`tests/test_rolls.py` checks it). The pool sweep's roll of a shipped map is printed and never failed: the pool's membership is the GM's exhibit decision. The push-time `roll-review` agent (an independent check only when a row is added) is deferred - `future-work/cross-cutting.md`. **And since feature 219 (GM 2026-09-08) the gate rolls NO map of its own**: the GM retired the 2026-08-08 requirement that an upstream change in the number of random draws must not move a map (*"I am actually okay with an upstream change in the number of random draws moving a map"*), which retired the immune experiment's roll, its test and its machinery; the audit the GM asked for found one other roll, Polder 12, whose three engine lines are unit tests and whose behavior is the soak tier's. The roster has zero `Roll` rows; a warm gate's census reads 0 rolls of 0 specs, a cold gate's the five shipped generators.
- **THE HTML MAP'S SPEED IS JUDGED BY HAND, NEVER BY A GATED TEST (GM 2026-09-07).** A request to make a page faster is measured in a browser while it is worked (the instruments are in `specs/199` and `specs/200` research.md) and the numbers recorded in that feature's research.md; the measurement is retired with the feature. No browser test loads a rolled page or times anything - *"the performance tests are never really going to be good enough to detect whether a human feels that the page is too sluggish ... the juice is not worth the squeeze"* - and the reason is the container: the thirteen such tests cost 3.9 GiB at 8 workers under an 8 GiB cap and crashed it twice. The synthetic-page browser tests (one Chromium, fifteen elements, the behavior rulings) stay. Full ruling in [`.claude/skills/diagram/l7r/diagram/interactive/CLAUDE.md`](.claude/skills/diagram/l7r/diagram/interactive/CLAUDE.md), "Verifying".

**Iteration-loop efficiency - THE PROJECT GOAL every guard here serves** (GM 2026-08-25, constitution v2.3.0): *"iterations are expensive in terms of wall clock time. And if me asking for a simple change results in half an hour of work being done when it should have only taken five minutes, then that limits the number of changes that I can make in a single day ... Every future session should keep this in mind as a project goal. both for the commands that we choose to run and for the way in which we look for opportunities to improve our tooling and for how we interact with that tooling."* When a simple task ran long, name which of the GM's three causes applied - more complicated than expected; lengthier tests than needed; more cycles than needed (small change, long test, repeat) - and if it is the tooling, propose the tooling change. A transcript profile (2026-07-20, re-confirmed 2026-07-25) found **78% of wall time is model turn latency, not tool execution** - the number of sequential turns is the cost, not tool speed. The rules, shortest form; the incident evidence behind each is in [`docs/iteration-loop.md`](docs/iteration-loop.md):

- **Batch into fewer, bigger turns.** Independent recon goes in ONE turn as parallel calls; a planned multi-edit goes in one turn. Serialize only past a real decision point. **ENFORCED** by [`scripts/batching-hooks.sh`](scripts/batching-hooks.sh) - when **3 of the last 6 turns** each made a single quick read-only call it blocks the next RECON-SHAPED call (a rolling window since 2026-08-08: **147 of 162 round trips single-call, 22.7 min of latency for 4.0 min of work** under the old consecutive streak). Since 2026-08-10 substantive calls (heredocs, `&&`/`;` folds, pytest/make/git-commit) are never the blocked call, the bar re-arms higher after each firing and decays back as turns batch, and the block message itself carries the full playbook (fold the retry-patch into one asserted script, act-on-read in the same command, never pad with no-ops) - measured 2026-08-09: 49 of 52 blocks in a patch-grind session were landing on already-substantive or already-folded calls.
- **Edit files with `Edit`, not with heredoc'd Python that rewrites them.** This costs no extra round trips - several `Edit` calls batch into ONE turn exactly like one folded bash command does - and it removes a hazard the patch script carries by construction: Python-inside-a-heredoc that itself contains quoted Python has to be hand-escaped, and when it breaks it breaks as a `SyntaxError` in the PATCHER, so the anchor never even gets tested and a full model turn goes on rewriting the quoting. Measured 2026-08-16 (fan-toe pond fix): three failed patch scripts, ~4-5 min, ~10% of the task, every one a quoting slip rather than a wrong anchor. Keep heredocs for analysis, asserts, and glue that contains no quoted code - and when an edit genuinely must be scripted (a mechanical sweep over many files), **use [`scripts/_patch.py`](scripts/_patch.py) rather than hand-rolling the loop again** (feature 236): each (anchor, replacement) is its own write, so a stale anchor is REPORTED and skipped while every other edit in the batch still lands, and anchors match whitespace-insensitively because four of the six anchor misses in the record spanned a line wrap. The ad-hoc form - accumulate every edit, write once at the end - discarded the substantive edits beside one cosmetic miss three times in a single day, and reported success each time. **The tension this leaves, stated rather than hidden** (feature 236 FR-009): this project's own rule prefers `Edit`, and the house-style guard used to see only `Edit`/`Write`, so a session working through Bash payloads wrote past it - which is why that guard now reads Bash payloads too and corrects them exactly as it corrects an `Edit` (GM 2026-09-12) - leaving as typed the command that is itself the spelling fix (`sed -i 's/centre/center/g'`, or any command carrying both spellings) and anything the command merely NAMES rather than writes.
- **Docs-only diffs skip the gate - and since feature 132 the gate skips itself.** `make done` answers `already verified` in seconds when its engine content (every `.py` under the skill outside `tests/` - **`l7r/diagram/ci/` left this exclusion in feature 178**, since the coverage floor measures it - plus pool gens/manifests; NOT the interactive page's `.js`/`.css` assets - feature 188 took them back out, an asset edit owes `make page-check` instead) is unchanged since the last green run - the same two keys the remote gate uses to skip a build; docs, tests, the Makefile, config and `scripts/` do not count, exactly as they do not owe a build (GM 2026-08-25, FR-024 for tests). **Nor do comments, docstrings or formatting inside engine Python** (GM 2026-08-26): both keys hash each `.py` as its docstring-stripped AST (`gate-stamp.py` `semantic_bytes`), so the record-the-why comment this project requires at the point of change never costs a second run - only a token that executes re-opens the gate. So running it after a docs commit is now free; not running it is still fine. **Nor does PAGE CONTENT re-open it (feature 207, GM 2026-09-07)**: the glossary, the sibling texts, the place card's wording, the page's fixed phrases and the rulings record are `interactive/assets/*.json`, a class's `Name:`/`Covers:`/`Label:`/`Sources:`/`Entry:` are docstring tags beside its explanation, the placement-stages notes and the pool index's text and stylesheet are files beside their modules - prose a reader sees is DATA and owes `make page-check`; data the engine executes on (the overlap taxonomy, the hit-region keys) stays code. Feature 205 paid the whole gate for one glossary term, which is how the line was found. **And a gate that does run is INCREMENTAL** (feature 207): it re-runs only the tests the change can reach - from the last full run's per-test and per-fixture coverage contexts - and judges 100% over that run merged over the kept coverage; a full run (`INCREMENTAL=0`, or any of six fallbacks) records the baseline. `l7r/diagram/ci/incremental.py` carries the argument. MEASURED the day it landed (specs/207 research R8, against the old 589 s gate): an edit to a tool no roll executes, 32 s; a polder-only edit, 219 s; an edit to the core placer, 595 s, because every map re-rolls and the rolls are the cost; a full run 664 s. The gate's cost follows the change's REACH now, not the suite's size. **And inside a gate that does run, the synthetic-page browser tests are skipped while nothing they read changed** (feature 206, GM 2026-09-07: *"this is about saving memory, not saving time"*): `gate-stamp.py`'s `browser` key covers the interactive modules, the assets, the test package, `research/*.html` and the installed browser; earned by `make page-check` or a `make done` that ran the package; never a push obligation. One Chromium (~430 MB, under 9 s) per gate, and the pattern for every browser test a later tier adds.
- **Before the gate, run the WHOLE affected test file - never a `-k` subset.** A filter selects the tests you were thinking about; a change breaks the ones you were not. **ENFORCED** by [`scripts/gate-hooks.sh`](scripts/gate-hooks.sh) - if the only local test run since your last `.py` edit used `-k`, it blocks `make done` once. (Escape: `GATE_OK` in the command, with a reason. Cost of getting this wrong, once: a 3.9-minute gate cycle lost to a test in the same file the filter did not select.)
- **Foreground-regenerate ONLY the motivating map - never a pre-gate pool sweep.** The gate verifies the pool itself, and render-sync regenerates main's renders from main's own tip, so a clone-side `pipeline/regen.py pool/*/*/*.gen.py` before `make done` is pure waste (38s measured 2026-08-16; evidence in [`docs/iteration-loop.md`](docs/iteration-loop.md)).
- **EVERYTHING RUNS THROUGH `make`, and it is enforced rather than requested** (feature 127). A bare
  interpreter reaching an engine entry point, a bare pytest, a make driven by a foreign makefile, or
  an inline override supplied to skip a prompt is refused BEFORE it runs by
  `scripts/make-only-hooks.sh`; the engine refuses in-process calls too. In the diagram skill the
  ladder, cheapest first, is `make quick`, `make done` (NOT the quick one),
  `make done FULL=1` (prompts, cancels by default, logs a reason). **What each costs is asked of the
  recorded runs, never written down here** (feature 162, GM 2026-08-30: *"I think those numbers for
  `make quick` are wrong and outdated"* - they were: the guard message said the gate cost ~70 s with
  the scope locked, while the scope had been unlocked for three days and the run log's median was
  111 s). `make audit` prints the history; `scripts/_gatecost.py <target>` prints the median a guard
  message would quote. Every refusal names the target that
  does the job, because a guard that blocks a legitimate question without giving the route is a guard
  that gets worked around.
- **`make quick` selects by CHANGE** (pytest-testmon, GM 2026-08-26): it runs only the tests whose executed code changed since the last run - nothing changed means "no tests ran" in ~3 s, a one-line engine edit runs its ~10 tests in ~7 s. `make quick ALL=1` runs every quick test; the gate never selects. A test that reads a DATA file (a fixture, a manifest) re-runs only when its code changes - the gate covers that.
- **`make quick` is the UNIT suite; `make done` is the INTEGRATION suite (GM 2026-08-26, constitution "quick runs the unit form; the gate runs the integration form").** A test with a quick form runs it in quick and its full form at the gate (`tests/_scope.py`: `full_or`, `subset`; the gate always sets `EXHAUSTIVE`). Tests that RUN the tooling (`make` in a fixture, git repos, coverage subprocesses) carry `tooling` and run in quick only when the tooling changed since the last green gate. The bad-map corpus replays at the gate, not in quick.
- **A slow test must earn its time (GM 2026-08-26, constitution "the phase sets the standard").** Sweeps run a documented subset by default and the full form under `EXHAUSTIVE=1` (`tests/_scope.py`; the docstring records the last exhaustive green). Cutoff ~0.5 s in `make quick`; ask what the time buys before keeping it.
- **`make quick` while iterating, `make done` ONCE at the end - and if you name both in one command the tooling COMBINES them for you** (feature 162, GM 2026-08-30). From 2026-08-26 this was a refusal, and the refusal was measured and did not pay: 37 firings, 23 of them escaped with `GATE_OK` in the very next call, to prevent one warm `make quick` (4.1 s). A bounced command costs a model round trip, which is the thing this whole section exists to save. So [`scripts/gate-hooks.sh`](scripts/gate-hooks.sh) now REWRITES the command (`_hookmatch.py combine` -> the harness's `updatedInput`) and says so in one line, and it never guesses: a shape it cannot rebuild exactly goes through untouched. Nothing here is refused any more.
- **Never re-run what the gate just ran, and never run pytest without `-n auto`.** Serial pytest is ~7x slower here; a green `make done` is already the proof. (Cost of getting this wrong, once: 13.2 minutes, 19% of a feature.)
- **`make done` reports ALL failures together.** Fix everything it lists, then re-run once. On a coverage failure it also prints the lines you changed that no test reaches.
- **Background the final gate - and NEVER poll it.** Act on the completion notification. **ENFORCED** by [`scripts/no-poll-hooks.sh`](scripts/no-poll-hooks.sh), which blocks `pgrep -f`, sleep-loops, and the `command sleep` bypass. (A wait on genuinely EXTERNAL state passes by putting `POLL_OK` in the command with a note saying what it waits for.)
- **An idle session runs the expensive tests for free** (feature 136, GM 2026-08-27): when a turn ends and nothing arrives for 60-120 minutes (staggered per session, restarted on a laptop resume), the clone runs `make idle-tests` (the whole gate, scope relaxed for that run only; once per idle, never on unchanged content) detached and the next prompt opens with its verdict - ACT on a red there; it is a regression found on the night it was made. The reference-hamlet period showed the cost of not having this: two days locked, four regressions and a 20-minute bisect at the unlock. A prompt aborts a run in progress; nothing of yours ever waits on one.
- **WEB READING RUNS IN BACKGROUND AGENTS, AND A STALLED AGENT IS REPORTED, NEVER ASSUMED RUNNING** (GM 2026-08-28, feature 143). A `WebFetch` in the session's own turn is FOREGROUND: one hung fetch in a parallel batch blocks the whole turn, and the GM's next message queues behind it (measured 2026-08-28: an hour, ended by Ctrl-C). So every source read goes to a `source-reader` (or another background agent), one attempt per host, and the session keeps working. A background agent can also stall with NO completion notification (two did that day - a bad TLS certificate, then nothing, for ten hours); [`scripts/agent-stall-hooks.sh`](scripts/agent-stall-hooks.sh) catches that: the `prompt` hook lists any of this session's agents whose transcript ends on a tool_result and has not moved for 5 minutes, and a session that dispatches readers starts `Monitor` on `scripts/agent-stall-hooks.sh watch <its subagents dir>` so the stall wakes it mid-turn. On a stall: `TaskStop` it, relaunch the batch in a NEW background agent with the failed host excluded. Companion `scripts/test-agent-stall-hooks.sh`.
- **Read derived data from the recorded artifact, not by re-running the generator.** Regenerate when you need to change what a generator DRAWS; read its manifest/output when you need to know what it drew.
- **Iterate on ONE artifact; run the full test bed exactly once, at the end** - but that final sweep is MANDATORY whenever shared code changed. **This binds when BUILDING, not only when debugging** (GM 2026-08-23, constitution VI). The old wording said "the ONE motivating artifact", which a feature can read as not applying to it - a new capability has no artifact "exhibiting the defect", so the cohort looks like the natural first check. It is not: on feature 126 a 48-map cohort was launched mid-experiment, one seed near-hung, and 30 minutes returned no result, twice. Use **`make maps`**, which picks its own scope: after a failed run it does the reference hamlet alone (~1 min) and stops at the first problem; after a clean run it does the whole tier and reports every failure together. There is no second command on purpose - the earlier two-command version relied on the session choosing right, and it did not. **Every step of a generator feature is TWO steps** - reference settlement, then pool - and both are tasks (constitution VI). The bare commands are the cheap ones on purpose; `make done` re-checks the whole pool anyway, so forgetting the sweep costs time, never correctness. A feature adding a KNOB owes one map per knob VALUE - three, not forty-eight.
- **Before changing ORDERING or architecture, read every path involved in ONE batched pass and settle the sequence first.** The failure mode is discovering the ordering one gate failure at a time. Where you add ordering-critical code, leave a comment at the point of change - a rule in a document nobody re-reads does not hold.
- **Do NOT cut the procedure steps** (regression-fixture freeze, overlap-registry classification, record-the-why docs, the stop-work procedure). They cost ~2 minutes per feature and are why the regression rate stays near zero. Savings come from turn structure, never from skipping guardrails.

**WHAT MAY BE RAISED TO THE GM AS AN ITEM ABOUT THE WORK** (GM 2026-09-12, in their own words):
*"if you are flagging or raising something for me as an item about the work, it should either be
information summarizing what was done, or measurements / explanations of the current state of things,
etc. But just saying 'I made a mistake mid-run and then fixed the mistake' is pointless to flag unless
I've specifically asked about that kind of thing (which I sometimes do)."* So three kinds earn their
place - **what was done**, at the level of what it changes about the maps or the tooling;
**measurements and the state of things**, including one that came out boring; and **a genuine decision
of theirs**, which is a fork where the record supports two forms and the choice is taste or canon, a
cost only they can price, or the acceptance of a finished thing. One kind does not: **the session's own
process narrative** - a mistake made and then fixed, a test that went red and then green, a wrong guess
corrected, a conflict resolved, a tool that took two attempts. The finished state is the deliverable.
The exception is real and the GM named it themselves: they sometimes ASK (what went wrong, why it took
so long, a post-mortem), and then it is the answer to a question rather than an unprompted flag. The
`escalation-check` agent applies this to a draft before it is sent, and `dev/reviews.md` carries the
three tests a decision item passes first.

**The whole of this machinery, laid out as one picture** - the cost ladder, the four short-circuits, the selection, every guard and Makefile refusal, the caches and what invalidates them, and the records that make it auditable - is [`docs/efficiency-tooling.md`](docs/efficiency-tooling.md) (GM 2026-08-30).

Package-specific timings and skill-specific lessons live in that skill's dev-loop doc, e.g. [`.claude/skills/diagram/CLAUDE.md`](.claude/skills/diagram/CLAUDE.md) - an index over [`.claude/skills/diagram/dev/`](.claude/skills/diagram/dev/), where the DRAW ORDER map and the KEEP-CLEAR CONTRACT live ([`dev/placement.md`](.claude/skills/diagram/dev/placement.md)).

**Improving a review subagent** (`settlement-review`, `building-review`, `frontend-review`): every pass is a row in [`docs/review-ledger.md`](docs/review-ledger.md) - verdict, what it caught that the author missed, and what it missed that the GM then saw - so "is it pulling its weight" is a total, not an impression (GM 2026-08-26). Do NOT just apply the fix and write the rule in. The current artifacts contain the motivating defect - that is the failing test. Add the **general, category-level rule only**, run the agent against the unfixed artifact, and only once it FIRES do you fix the artifact and record the specific instance as a validated example. Full procedure, plus the harness gotcha that mid-session edits to `.claude/agents/*.md` do not reach agents launched by type, in [`docs/spec-kit-and-reviews.md`](docs/spec-kit-and-reviews.md).

**Session clones (REQUIRED for every session that modifies this repo).** Any session about to change a file here works in an isolated clone under `.clones/` - never in main's tree. The normal path:

- **Clone name = this session's name, kebab-cased.** Resolve it - do not guess: the hooks read it from the LAST `{"type":"custom-title"}` record in this session's transcript (`~/.claude/projects/<cwd-mangled>/<session_id>.jsonl`, the `session_id` being in your scratchpad path), falling back to `~/.claude/sessions/*.json`; do the same by hand. The transcript is the reliable source: `/clear` mints a NEW `session_id` and re-emits the rename there before your first prompt, while the sessions json is rewritten asynchronously - which is why sessions used to "lose" a name after `/clear` (fixed 2026-08-27). `diagram` (this repository's own name) and `gm-assistant` are FORBIDDEN clone names; an unnamed or auto-derived session has no valid workspace, so ask the GM to `/rename` it before doing repo work.
- **Create it with** `git clone /diagram .clones/<session-name>`. Repo-local config does not copy, but nothing is set by hand: `sync-with-main.sh` establishes the committer identity (from the GM's shared `~/.claude/gitconfig` - one name and address for every container, GM 2026-08-27; main's tip author is only the fallback when that file is absent) and main's `updateInstead` on every run, and `setup-dev-env.sh` does the same for main on a fresh container. Reuse an existing clone for a resumed session.
- **GITHUB `main` IS MAIN; `/diagram` IS A MIRROR** (feature 130, 2026-08-25). A clone's `origin` is GitHub (`https://github.com/EliAndrewC/diagram`, HTTPS + the PAT from `development-secrets.ini` via `scripts/git-askpass-token.sh` - the procedure re-points a stale origin itself). Nobody pushes to `/diagram`; it fast-forwards from GitHub and render-sync runs there. The GM's old "push main to GitHub" job is retired for this repository - a laptop push to GitHub main flows into the mirror and every clone like any other landing.
- **Sync in at the start of EVERY new piece of work** - `scripts/sync-with-main.sh sync-in` inside the clone (the prompt hook runs it every turn): fetch GitHub main -> fast-forward the mirror -> render-sync there -> merge into the clone. On a DIRTY clone the mirror half still runs (`sync-in --mirror-only`); only the clone merge waits, because mid-task work is sacred.
- **Stop-work procedure, EVERY time you stop** (a PROCEDURE, never a "ritual" - GM 2026-08-28: a ritual has religious or spiritual significance, the setting has real ones, and this is a real-world scripted process; "ritual" in this repository refers only to actual rituals, historical or fictional) (task done, milestone, or pausing for GM input): commit in the clone, then run [`scripts/sync-with-main.sh`](scripts/sync-with-main.sh) `done` from inside it. **A FEATURE IN PROGRESS LANDS NOTHING** (feature 133, GM 2026-08-25) - if any spec your delta touches, or your pointer names, has an open task, the push is refused on either route; the only exception is pushing that feature's `specs/` directory alone (the number claim). **TWO ROUTES TO MAIN, chosen by the delta, never by you**: no diagram engine code in YOUR commits -> DIRECT (a locked fast-forward push to GitHub main, free, today's guards); engine code (`l7r/**/*.py` outside `ci/`, `pool/*.gen.py|*.json` - never `tests/`, `ci/`, the Makefile, config or hooks; and not the interactive page's `.js`/`.css` assets since feature 188 - those owe a green `make page-check` at push and route DIRECT) -> GATED - **and a comment-only, docstring-only or formatting edit to engine Python is NOT engine code for the route** (GM 2026-08-28: `ci/delta.py` compares each changed `.py` by the same docstring-stripped AST id `gate-stamp` keys on, so the wording sweep that used to route GATED and then demand a spec-kit feature goes DIRECT) (`make ci-merge`: five conditions checked locally, then a CodeBuild build merges the LATEST main into the work, runs `make done`, and fast-forward-pushes the result to GitHub main; the clone and the mirror fast-forward to it). `done FULL=1` runs the full sweep on CodeBuild after the local prompt. **A green local `make done` IS the reference-scope verification when main adds no engine content to the merge** (GM 2026-08-25): the gated route then pushes directly with no build; a build runs only when main moved on engine paths since your merge base, or for FULL / an operation. There is NO local override of the gated route - a delta that cannot be dispatched stays in the clone. **With `remote off` (`make ci-off`, feature 132) the gated route is LOCAL-GATED**: nothing dispatches; a green local `make done` on the merged engine content pushes, anything else stays in the clone until you merge main in and run `make done` again. **Never `git push --force`** - it is the one thing that overwrites other sessions' work.
- **Main is the integration point, never a workspace.** The ONLY thing a session runs in main's tree is render-sync. No generators, no tests, no writes. Read-only commands are fine. A hand commit in `/diagram` stops the next sync-in ("mirror cannot fast-forward").
- **Git ownership:** the session does all commits/merges/pushes; the merge project's credential is the only other thing that writes GitHub main, and the `main` ruleset stops it (and everyone) from rewriting history. Never commit or push against `/host-l7r-repo`.
- **Paid, prompted targets** (`make ci-image`, any `FULL=1` dispatch) exist to be answered by a person; a session answering its own prompt through a pseudo-terminal was ruled an EXCEPTION by `spec-fidelity` on 2026-08-25, and the GM then authorized it in their own words the same day (*"all of them look like things that you should be capable of kicking off"*). So a session MAY answer, and the logged reason MUST say it did and quote that authorization - the bypass-log entry is how the audit tells a session's answer from the GM's. Never through an environment variable. `make ci-check` (reference scope) and the gated push need no prompt.
- **Commit on `main` inside the clone** - never on a branch. `sync-with-main.sh` pushes `HEAD:main`, so what you committed is what lands.
- **NEVER REWRITE HISTORY** (GM 2026-08-25, constitution VI): no `git rebase`, no `pull --rebase`, no `merge --squash`, no `commit --amend`, no force push. Every landing is a real merge commit with its parents intact. The history is the record, and the verified-tree records, review records and perf bookends are keyed by the commits and content that were actually tested - a rewrite would silently throw away a paid verification. Enforced by [`scripts/repo-safety-hooks.sh`](scripts/repo-safety-hooks.sh).
- Hooks enforce all of this ([`scripts/clone-sync-hooks.sh`](scripts/clone-sync-hooks.sh)): the forbidden name, name-routing to another session's clone, a live-session claim, a clean-but-stale HEAD (**since feature 212 the hook runs `sync-in` itself and lets the edit proceed, told** - every one of the 13 stale-head refusals in the record fired MID-TURN after main moved under a clone the prompt hook had synced at the turn's start; a clean clone's sync-in is a 1.4 s fast-forward, run under a 25 s bound, and a sync that cannot fast-forward falls back to the refusal), and - since feature 168 - a **STRAY COMMIT IN THE MIRROR**. That last one is the bare-`cd` trap two rows below landing on everyone at once: a session commits into `/diagram` instead of its clone, and from that moment the mirror's HEAD is a commit GitHub main does not have, so EVERY clean clone is refused as stale and sent at `sync-in` - which fetches GitHub, finds nothing new, reports success and changes nothing. The guard now checks the mirror's HEAD against its `origin/main` first and names the real problem. **The recovery belongs to the session that made it**, and it is not a reset: the mirror's working tree may be the only copy, so the content goes into THAT session's clone first (`format-patch` / `git am`), then `git -C /diagram status --porcelain` is checked for untracked files a reset would destroy, and only then `git -C /diagram reset --hard origin/main`. A dirty tree is never blocked - mid-task work is sacred. **If a hook blocks you, the full spec and every failure mode is in [`docs/session-clones.md`](docs/session-clones.md).**
- **Gotcha:** keep `pytest`/`ripgrep` scoped to the working dir - they do not read `.gitignore`, so a repo-root run double-collects every clone.
- **NAME THE TREE IN THE COMMAND; never carry a `cd` into it** (measured 2026-08-17). A bare `cd` persists for the REST of that command AND into the next Bash call, so a block opening `cd /gm-assistant && ...` silently answers about MAIN for everything after it - including the half you labeled CLONE. That cost one session two wrong readings of its own state in a single sitting, the second one immediately after writing up the first. Measured behavior: `( cd X && ... )` in a subshell leaks nothing, either way; a bare `cd X` leaks both ways. So:
  - **`git -C <abs-path> ...` for every git call.** No `cd` at all, and - the part that actually matters - the tree is named IN THE COMMAND TEXT, so a mislabeled section header cannot survive a re-read of what you ran.
  - **`( cd <abs> && ... )`** when something genuinely needs a cwd (`make`, `pytest`, `python3 -m`).
  - **One tree per command.** If you want main and a clone side by side, that is two `git -C` calls, not one `cd` and a header.
  Note what this is and is not for: actually WRITING in main is already caught three ways (`webapp/mainguard.py`, the Makefile's `guard`, `settlement._assert_not_main_tree`). This rule prevents the quieter failure those guards cannot see - a read-only diagnostic that confidently reports the wrong tree, which is worse than an error because it looks like an answer.
  **A hook was priced and DECLINED (GM 2026-08-17: "I can't think of a better enforcement mechanism either").** A `PreToolUse` hook is this project's usual answer to a recurring mistake, and it does not fit this one. A hook precise enough to catch the motivating case would have to demand `git -C` on EVERY git call - it fired on a command with one legitimate `cd` and no second path, so nothing structural marked it as wrong - and a hook that fires on nearly every correct command trains sessions to pattern-match past it, which degrades the guards that matter. The narrow alternative (fire only when ONE command contains both a `cd` to a non-clone path AND `.clones/`) is near-zero noise but would have caught neither instance, since neither named two trees. So this rule is deliberately unenforced and rests on habit; the cost is that it will be broken again. **Reopen only with a mechanism that would catch a single-`cd` command whose section header names the other tree** - that is the shape to beat, and neither candidate above does.

**WHAT IS ENFORCED, AND WHERE** (audit 2026-08-24). Twelve guards, each with a test companion that
`make hooks-test` runs as a gate phase - a guard without one turns the gate red (constitution XVIII).

| rule | mechanism |
|---|---|
| never `git push --force`; never rewrite history (rebase, pull --rebase, merge --squash, commit --amend) | [`scripts/repo-safety-hooks.sh`](scripts/repo-safety-hooks.sh) - no escape; "never" stops meaning never the moment one exists |
| no git writes to `/host-l7r-repo` | same script; EDITS there stay legal, the intake workflow needs them |
| the GM's SOURCE blocks are not editable (V) | [`scripts/source-block-hooks.sh`](scripts/source-block-hooks.sh) - checks containment against the file on disk |
| hyphens only; American spellings | [`scripts/house-style-hooks.sh`](scripts/house-style-hooks.sh) - **CORRECTS the text rather than refusing the edit** (feature 164): both rules are exact substitutions from this file's own table, so the fix is applied and the session is told. Two things it never does: correct a word inside a backtick span (a token being NAMED, not used - it refused feature 164's own plan for one) and correct the GM's verbatim `specs/*/request.md`, which stays on the refusal path so a person decides (Principle V). Still exempts the GM's writing and the files that must quote the rule |
| a README is the GM's to write (XVII) | [`scripts/readme-hooks.sh`](scripts/readme-hooks.sh) |
| everything runs through `make` | [`scripts/make-only-hooks.sh`](scripts/make-only-hooks.sh) + `l7r/diagram/_invocation.py`. **A TARGETED bare pytest is REWRITTEN to `make test-file FILE=<files, a dir, a node id> [K="<-k expr>"]`** (feature 164 took one file; feature 212, GM 2026-09-07, *"targeting a specific module or even a specific test case"*, takes the rest: `-k` becomes `K=`, output flags are dropped, and the `2>&1 \| tail` after the pytest segment - which every one of the eleven targeted runs in the record carried, and which 164 declined - is kept verbatim); a marker, a deselect, a collection, a coverage or plugin flag still refuses, and the refusal names the token that stopped it. **An engine entry point a make target wraps is rewritten to that target** (`python3 -m l7r.diagram.ci engine-key worktree` -> `make engine-key REF=worktree`), the table DERIVED from the Makefile's one-line `$(RUN).<module>` recipes at hook time. `gate-hooks` counts `K=` and a `::` node id as the subset a bare `-k` is. Its message also lost a false reason: the floors are held by the gate targets, not by `test-file`, which runs `--no-cov` |
| guard files are not edited casually | [`scripts/guard-file-hooks.sh`](scripts/guard-file-hooks.sh) - and it says so when you OPEN one (feature 164): a Read of a guard file carries the marker line, because 28 of its 29 recorded firings were the same edit again with the marker added. Since feature 212 the marker is judged only on a GUARD file (a spec that mentioned the token was being refused for a missing reason) |
| **a recipe comment must not RUN** (feature 212, the GM's ruling relayed 2026-09-07 after feature 207's `: "... \`make test-full\` ..."` comment ran `make test-full` from inside test-full and recursed 914 levels to the container's 2,048-process limit; feature 185 had met the same hazard: *"just commenting, saying not to do it again is not a good way to reliably make sure that the problem does not recur"*): an Edit or Write to ANY Makefile whose `: "..."` recipe comment holds an unescaped backtick or `$$(` is refused BEFORE the marker escape and has no escape token - a shell fact, not a policy; `\`` and `\$$(` pass | the same script, `recipe-comment-substitution`; the detector is `_hm_make.py recipe_comment_hazards`, shared with the gate-phase backstop `tests/tooling/test_makefile_recipe_comments.py`, which scans every Makefile in the tree and proves the detector fires; `scripts/test-guard-file-hooks.sh` section 5 |
| a feature in progress lands NOTHING, on either route (feature 133, GM 2026-08-25): the active feature is derived from any `specs/NNN-*/tasks.md` with an open task that the delta touches or the pointer names; the one exception is a push of that feature's `specs/` directory alone (the number claim); no flag | [`scripts/sync-with-main.sh`](scripts/sync-with-main.sh) at PUSH time; `scripts/test-sync-with-main.sh` 7d proves it fires on both routes and stays quiet on the claim and on a finished feature |
| a spec is reviewed before implementation (XVI); a Mode B map before it ships | [`scripts/review-gate.sh`](scripts/review-gate.sh), run by `sync-with-main.sh` at PUSH time |
| nothing is pushed that a green gate did not see (XIII): engine Python needs a green `make done`, a guard script (`scripts/*.sh`, `scripts/*.py`) a green `make hooks-test` | [`scripts/gate-stamp.py`](scripts/gate-stamp.py), stamped by each gate on success and checked by `sync-with-main.sh` at PUSH time; `scripts/test-gate-stamp.sh` proves both refusals. Added 2026-08-25 after a red `hooks-test` chained with `;` landed on main - the stamp only covered engine Python |
| both perf bookends exist; a seed >5% slower must be DIAGNOSED and a total >10% BLOCKS as a regression (VI) | `make perf-gate`, a phase of `make done FULL=1`; the two bands are in `tools/perf_snapshot.py` with `tests/tools/test_perf_snapshot.py` proving each fires |
| no `-k` subset before the gate; no branches; no polling; batching | the `gate`/`no-branch`/`no-poll`/`batching` hooks. Feature 164 converted the two that could be: **`no-poll` CORRECTS a self-matching process pattern** into the bracket form it used to recommend in prose (and runs that correction LAST, after the busy-wait refusals - in first position it rewrote the original 2026-07-25 command and let the 10.9-minute busy-wait through, which its own suite caught; **and since 2026-09-08 the correction runs on the ESCAPED path too** - a `POLL_OK` waiter on a detached page-check carried `! pgrep -f "make page-check"`, the token exited the guard before the correction, and the loop ran for hours on a gate that had finished in 7 s: the escape permits the wait, it does not make the pattern correct), and **`batching` warns one turn BEFORE it blocks**, free, because at 127 firings in six days it was the loudest guard in the repository and every firing spends the round trip it exists to save. **Feature 212 (GM 2026-09-07) fixed `no-poll`'s file-wait qualifier and backgrounds the foreground form**: the one wait the GM permitted in feature 165 - a backgrounded loop reading a file - was being REFUSED on seven of its eight real uses because the qualifier read the condition raw (a `\|` inside the quoted regex counted as a pipeline, `2>/dev/null` as an output file, `$S/done.log` as no path, `[ -s f ] && grep` as none of the three forms); it now reads the condition in five stated steps (`_hm_shape.py file_watching_loop`; the substitution test stays on the RAW condition, so nothing quoted can hide a `$(curl ...)`), and a FOREGROUND loop of the permitted shape is returned with `run_in_background` set and told, instead of refused. The boundary itself did not move. **Feature 227 (GM 2026-09-12) ADDS THE PROOF OF LIFE to a permitted wait that has none**, because a wait that never asks whether the thing writing the file is still there waits forever when it is not: a detached `make` run wrote everything it had to write and was then killed before flushing the line the waiter watched for - by the kernel's OOM killer, `oom_kill 36` in this container's `/proc/vmstat`, so yes, a `make` run here does get killed. The GM's rule, *"the hook should add the second proof of life check to what is being waited for"* and *"simply telling you to set a watch properly next time is bad engineering practice because that's just another version of making you remember to do something"*, is performed: the clause names the file the loop itself watches and asks the kernel's open-file table and the file's mtime (`scripts/_writer-alive.sh`), never a process pattern - a literal `pgrep -f` pattern matches the searching shell, which is this guard's own 2026-07-25 fault. And the same guard stops REFUSING the shape it now writes: a liveness clause was not one of the three permitted file forms, so a wait that asked both questions was blocked as a busy-wait. A liveness part can only end a loop sooner, a condition must still read a real file, and the `>/dev/null` forgiveness is per PART, so `until grep -q x /tmp/a.log >/dev/null` is refused exactly as before |
| a repeated EXPENSIVE MEASUREMENT with nothing changed between (GM 2026-08-28, feature 146: *"that seems really inefficient since you know what lines need to be covered"*) - the **SECOND** `make test-full` / `done FULL=1` in a row with no engine edit and no commit is refused (feature 162, GM 2026-08-30: *"should we make it so we start blocking at 2 in a row instead of 3 in a row?"*), and the FIRST one carries the reminder, so a session is told before it is ever refused - a `additionalContext` on an allowed call costs no round trip, a block costs one. The cheap loop is never blocked; an engine edit or a commit resets it; escape `MEASURE_OK` with a reason. Measured on the feature that prompted it: 20 measurements, about an hour, almost all redundant | [`scripts/measure-hooks.sh`](scripts/measure-hooks.sh); `scripts/test-measure-hooks.sh` (28 cases, including the cheap loop staying free, the known mention false-positive, and a log that cannot be written not taking the guard down) |
| what a guard actually COSTS is a total, not an impression (feature 162, GM 2026-08-30: *"I notice I've been seeing a lot of this in the output of my claude code sessions over time"*) - `measure-hooks.sh` and `gate-hooks.sh` record every block, rewrite, reminder and escape to `~/.claude/guard-log/`, one file per entry, and `make audit` prints the census with the escape rate. Answering that question before this existed took a replay of 715 MB of transcripts (`specs/162-guard-block-economics/measure/replay.py`), and the answer retired a refusal that was being escaped 62% of the time. **Since feature 168 (GM 2026-08-30: *"the firing log should record more data for us to be able to use to make improvements in the future"*) EVERY acting branch of every guard records, and an entry names the RULE** - `no-poll` distinguishes busy-wait from disguised-sleep, `clone-sync` its five refusals, `make-only` its five verdicts - because the unit a future improvement acts on is a rule, not a script, and "no-poll fired 32 times" cannot say which one is carrying the cost. **An escape is a branch, and the one that matters most**: all nine escape tokens record now, where only `MEASURE_OK` did before. `idle-tests` is deliberately out of class (it refuses a session nothing - it is a runner, and it keeps `dev/idle-log/`) | [`scripts/_guardlog.sh`](scripts/_guardlog.sh) + [`scripts/_gatecost.py`](scripts/_gatecost.py); every guard's own suite, plus `tests/tooling/test_guard_firing_log.py`, which drives each guard with a REAL payload and asserts the `(event, rule)` on the entry that lands (a grep proves a call site exists, not that it fires), and `tests/tooling/test_guard_message_durations.py`, which fails the gate if any guard message states how long a command takes |
| a background agent that stalls (transcript ends on a tool_result, unchanged 5 min) is REPORTED at the next prompt and, under a `Monitor`, mid-turn (feature 143, GM 2026-08-28) | [`scripts/agent-stall-hooks.sh`](scripts/agent-stall-hooks.sh) `prompt` / `watch`; `scripts/test-agent-stall-hooks.sh` (12 cases) |
| the gate and the independent settlement-review run TOGETHER, and neither alone - and since feature 164 a plain `make done` with a review owed is **REWRITTEN to `make verify`** rather than refused, carrying the line that says to dispatch the review in the same turn (7 of its 13 firings had been escaped in the very next call) (feature 151, GM 2026-08-29: *"is there a scripted way to have them both happen at the same time and that's the only way you can do either of them without some kind of override?"*): `make verify` starts both; **every OTHER gate shape** (`make maps`, a detached `make done`, `FULL=1`, flags) **is RECORDED AND PERMITTED with the DISPATCH NOW context** rather than refused (feature 212, GM 2026-09-07: the census found the rewrite fired once while every real refusal - eight `make maps`, three detached gates - was a shape `verify` cannot take, against 98 `PAIR_OK` waivers; a detached run overlaps the review as under verify, a FOREGROUND run is told the review will follow it and how to overlap next time - spec 212 D7, the one place this changes what 151 delivers; the GM accepted it on 2026-09-08 over restoring the refusal for that shape, with a detached `verify` variant for `maps` as the lever if sessions keep sweeping in the foreground); a `settlement-review` dispatch with no gate running or freshly green is refused, and a turn may not end on a half-open pairing. Escape `PAIR_OK` with a reason, logged to `dev/bypass-log/`. Measured cause: the review's 17 minutes sat on the critical path of feature 150's T55 because it is dispatched from memory rather than by the tooling. **And since feature 231 (GM 2026-09-12) a review is owed only when a pool map's LAYOUT moved** (*"if there are no changes to the actual way that the settlement is laid out, then we should not need to re review the settlement"*): `scripts/_review_owed.py` names every map whose MANIFEST differs from the merge base with main - the one scripted answer, asked FRESH at every decision point because the gate's own pool phase can move one - and with no map named the gate runs as typed, the stop branch stays quiet and the automatic waiver is recorded with its reason. A glyph or page change with an identical manifest goes back to the GM, who reads one changed map faster than the agent does; feature 228 spent 18.7 of its 32 minutes proving that. Where a review IS owed, the changed maps are snapshotted from the clone and from main into `<clone>/.git/review-snapshot/` before the gate starts (the roll cache evicts a pool map's renders mid-run), and the guard resolves THIS SESSION'S CLONE through `clone-sync-hooks.sh resolve` rather than the shell's cwd - reading the mirror's state is what refused feature 228's review and then fired half-open on it | [`scripts/pair-hooks.sh`](scripts/pair-hooks.sh) `pretool` (Bash + Agent) / `stop`, keyed on `ci engine-key worktree`; `scripts/test-pair-hooks.sh` (14 cases, including the MENTION-is-not-an-INVOCATION shape that blocked this feature's own time audit four times) |
| **FINDINGS DO NOT REACH THE GM UNFILTERED** (GM 2026-09-12). A session sent the GM five "judgment calls" off two settlement reviews and three came back as corrections: two rested on NO project norm at all - a dwelling count measured against a scoring radius no rule uses as a bar, and a distance measured against a maximum that does not exist - and one had already been answered by a measurement taken an hour earlier on a layout that no longer existed. The cost is not the wasted minute: *"I'm concerned that I will be ignoring something bad"* - an escalation that turns out to be nothing teaches the GM to doubt their reading of the ones that matter. **The rule already existed in TWO places** (the review agent's own contract, *"QUESTIONABLE ... needs a RESEARCH PASS - never 'a GM ruling'"*, and constitution XII) **and was disregarded anyway**, which is the GM's whole point: *"If we already had a rule in two places and it was still disregarded, then it sounds like we need another process change. Like maybe when you have findings to escalate to me ... you do a final subagent check on that writeup."* So a `settlement-review`, `building-review` or `size-audit` dispatch ARMS a requirement and an `escalation-check` dispatch DISARMS it; the turn may not close in between. The `pair` shape deliberately - it keys on an Agent dispatch, which the harness can see, rather than on the session's prose, which no hook can read. A review that turns out to have nothing to relay costs one cheap filter pass, stated here as the accepted price of an enforcement that works; escape `ESCALATION_OK` with a reason | [`scripts/escalation-hooks.sh`](scripts/escalation-hooks.sh) `pretool` (Agent) / `stop` + [`.claude/agents/escalation-check.md`](.claude/agents/escalation-check.md) (the filter: it verifies each cited norm IN THE REPO, which is the part the session cannot do honestly about its own draft); `scripts/test-escalation-hooks.sh` (22 cases, including that a `source-reader` or `quote-check` dispatch must NOT arm it, and that the refusal fires once per review rather than looping) |
| `hooks-test` COSTS WHAT IT SHOULD (feature 172, GM 2026-08-30: *"could we not do something similar where if the hooks have not changed, then we do not run the hooks tests?"*). The skip already existed and costs 0 s - what did not was the GRANULARITY: every suite was declared to depend on all four shared helpers, so a one-line `_gatecost.py` fix re-ran 21 suites. The set is DERIVED now, transitively and from CODE rather than prose - measured on a real incremental run, a `_gatecost.py` change re-runs **4 of 21** rather than 21, and a make/rewrite change 5; the constant in both is the TWO whole-tree entries (`sync-with-main.sh`, which is held there because it resolves script paths at run time, and `gate-stamp.py`, which derives there because it reads the whole directory) - and the suites run in PARALLEL (**194 s -> 63 s** on identical content, 21 of 21 green both ways). `_hookmatch.py` is three leaves - shape, escape, make/rewrite - with each guard invoking the one it uses, which takes make/rewrite churn from 21 suites to 3. **What it does NOT fix, measured**: the escape family stays at 17 of 18 because every guard reaches its escape through `_guardlog.sh`, and no arrangement of files changes that | [`scripts/_hookdeps.py`](scripts/_hookdeps.py) (the derivation), [`scripts/_hookrun.sh`](scripts/_hookrun.sh) (one parallel job), `tests/tooling/test_hook_deps.py`. The derivation is checked rather than trusted: it was wrong three times first, each time plausibly - a hardcoded roster, a filename counted from a COMMENT, and a Python import that never writes the `.py` |
| an ESCAPE MUST SAY WHY, and every permitting branch records it (feature 170, GM 2026-08-30: *"we have no way to audit later when this workaround was taken and whether the stated reasons were valid use cases"*). A bare token is REFUSED - the missing thing is the session's reasoning, which no tool can supply - and the floor is two words and eight characters, which excludes `GATE_OK: ok` and admits `CI is down`. THREE branches were permitting silently and no longer are: `make-only` (via a distinct `classify()` verdict), `repo-safety`'s `HOST_GIT_OK` over the GM's own repository, and `sync-with-main`'s `GATE_STAMP_OK`, in a file that had no `guard_log` call at all. `make audit` shows the reasons | [`scripts/_hookmatch.py`](scripts/_hookmatch.py) `escape_reason`/`reason_is_enough` + `escape_or_refuse` in [`scripts/_guardlog.sh`](scripts/_guardlog.sh); `tests/tooling/test_guard_firing_log.py` derives the completeness over **(token, permitting site)** pairs, because two tokens have two sites each and a per-token check passes green while the target branch stays silent |
| a FINISHED background run cannot be reported as still going (feature 170, from a session's own report: *"I reported the gate as 'waiting' when it had actually failed four hours earlier because I never saw the notification"*) - surfaced at the next prompt AND at turn end, once, never again. It REPORTS and never blocks: the failure was a session not KNOWING | [`scripts/finished-run-hooks.sh`](scripts/finished-run-hooks.sh); `scripts/test-finished-run-hooks.sh` (22 cases) |
| **...AND A WAITER SPINNING ON A DEAD PRODUCER IS REPORTED** (GM 2026-09-12, who found THREE in their own status line). A waiter is a loop watching a file; when the thing writing that file dies, the loop spins forever. Three had been re-grepping every 15 s for EIGHT HOURS on logs from the `make placement-stages` runs the OOM killer ended that morning - the same incident that produced the proof-of-life clause, whose waiters predate it and so never got one. **Two census attempts by this session missed them and twice told the GM that nothing was running**, which is the reason the rule is mechanical: at any instant the visible process is the loop's own 15 s `sleep`, so a census filtered by process AGE cannot see the loop and one filtered by NAME sees only `sleep`. The honest question is asked of every process's cmdline and then of the file it names (`_writer-alive.sh`), with this hook's own ancestry excluded BY PID rather than by pattern - the command that searches for `until grep` contains those words. It REPORTS and does not block: the loop is harmless, not knowing about it is not | the same script's `stop` branch + its `stale` mode; the suite starts a REAL spinning waiter on a stale file and asserts it is found, reported at turn end, recorded under its own rule, and that a waiter whose file is still held open is NOT reported |
| **...AND A RUN STILL GOING MUST NOT BE ABANDONED** (GM 2026-09-12) - the mirror image of the rule above, and it happened the same afternoon the rule above was being relied on: a session started `make done` detached, wrote its report, ended the turn and never waited; the gate failed 58 s later on five uncovered lines and sat unread for 52 minutes, until the GM asked whether something had gone wrong. The finished-run rule could not catch it, because it fires at the NEXT PROMPT and by then the damage is done. The GM: *"if there is a makefile command running and your turn ends, then we have a hook that errors or triggers you or sends you context to say, hey. Hold on. You need to wait for that makefile command."* So the Stop hook REFUSES a turn that would close over a live `make` in this session's clone, and names it. Detection is by CWD out of `/proc` - a process whose `comm` is `make` and whose working directory lies inside the clone - never by a process pattern, because `pgrep -f "make done"` matches the searching shell, which is the 2026-07-25 fault `no-poll` exists for. ONCE per run, keyed on the make PIDs, and there is NO token escape: a Stop payload carries no command, so there is nowhere to put one (tried; the suite records why). The second attempt closes the turn, which is the release valve a session answering the GM mid-gate needs | the same script's `stop` branch + its `live <clone>` mode; `scripts/test-finished-run-hooks.sh` drives a REAL `make` in a fixture and asserts the refusal, its message, the once-per-run release and the recorded rule |
| a TARGET THAT GETS SLOWER FAILS (feature 171, GM 2026-08-30, on `make done`'s median going 35 s -> 148 s in three days with nothing failing: *"I could have sworn that we had some code in place that detected when we had our unit tests start to take longer relative to some previous baseline"*). `quick` fails at the GM's 15 s on the run itself; `done` at its ceiling on the median of green same-scope runs. The GM's numbers are HARD CEILINGS the mechanism sits under - an early derivation LOOSENED as the baseline drifted, so a 12 s baseline bought a 16 s ceiling - and the baseline is PINNED, never rolling, because a rolling median follows a twenty-small-steps regression upward. A new target is a row; the AWS row is present and OFF | [`scripts/_ratchet.py`](scripts/_ratchet.py); `tests/tooling/test_ratchets.py` (16 cases, including that the derivation REPRODUCES the GM's own two numbers - a two-point fit, which is the entire argument for trusting it elsewhere) |
| a write that would LAND in the mirror through the shell's directory is MOVED to the session's clone, and one that NAMES the mirror is refused (feature 169, reworked by feature 204, GM 2026-09-07: *"is it possible for us to intercept the cd command and rewrite it to change into the correct directory instead based on what session we are currently in? And then if we did that, we could add tool context"*). Feature 169's guard asked where the SHELL STOOD, and the census behind 204 found it refusing correct work almost every time: 173 refusals in a week, 2 of them `cd /diagram && git commit` (the shape it was built for), 146 with no `cd` at all (a subagent's shell starts in `/diagram` and it wrote to the scratchpad or a clone by absolute path), 127 from subagents, and NONE naming a write inside main by its path. Now `_hm_tree.py judge` walks the command's own `cd`s, `VAR=` assignments and subshells from the payload's cwd and classifies each write by where it lands: `cd /diagram && git commit`, or `git commit` while standing in `/diagram`, is REWRITTEN to `cd <this session's clone> && ...` with `additionalContext` saying why (the rewrite also moves the shell, which cures the leak for the next command); `git -C /diagram commit` and `> /diagram/x` stay refused; a write to `/tmp`, the scratchpad or a clone from a shell standing in main is allowed silently. Every rewrite is re-judged before it is emitted. Escape `MAIN_TREE_OK`. It does NOT enforce the 2026-08-17 read-only rule, which stays declined | [`scripts/main-tree-hooks.sh`](scripts/main-tree-hooks.sh) + [`scripts/_hm_tree.py`](scripts/_hm_tree.py) (the verdict) + `clone-sync-hooks.sh resolve` (this session's clone; a subagent resolves to its parent's); `scripts/test-main-tree-hooks.sh` replays the 173-command corpus `scripts/fixtures/main-tree-refusals-2026-09.json` and expects zero refusals; `make guard-log GUARD=main-tree` lists the firings with session name, cwd and the full command (feature 204 made every guard's record carry those) |
| an ESCAPE token is an invocation, not a mention (feature 169): all eleven go through `_hookmatch.py escape`, so a grep, a commit message or a heredoc no longer escapes a guard - and no longer resets `measure`'s counter or deletes `gate`'s state file. `pair`'s agent-prompt branch is the one stated exclusion | [`scripts/_hookmatch.py`](scripts/_hookmatch.py) `escape_used`; `tests/tooling/test_guard_firing_log.py` derives both the slug rule and the log-isolation rule over the whole guard tree, because the hand-lists they replace each missed a guard within a day |
| a checkout or restore that would DISCARD uncommitted work in a tracked file is refused (feature 133 T33, GM 2026-08-27): `git checkout -- <path>`, `git checkout <path>`, `git restore <path>`, `.`; a clean file, a branch switch, `--staged` and a mention pass; escape `DISCARD_OK` with a reason | [`scripts/discard-hooks.sh`](scripts/discard-hooks.sh) - asks git whether each named path is dirty in the tree the command names; `scripts/test-discard-hooks.sh` (21 cases). Added after a `git checkout -- ways.py` typed into a command as a NOTE discarded a task's working diff (5 min recovered from the transcript; the 10-minute task took 39) |
| an IDLE session runs the expensive tests unattended (feature 136, GM 2026-08-27): the Stop hook arms a detached timer, a prompt disarms it (and aborts a run in progress - nothing of the session's ever waits on an idle run); the wait is 60 + (hash(session name) mod 61) minutes of AWAKE time, restarted in full when the laptop resumes (a wall-clock jump past the ticks); one runner at a time host-wide (`~/.claude/idle-tests.lock`, losers defer 5-15 min); the run is `make idle-tests` (= the whole `done` gate, never FULL - the GM's ruling; the scope lock is RELAXED for it, and only for a process descending from the timer: `switches.idle_context`, unforgeable by a session; its record is `idle-done`, never a `done` a push could reuse); one run per idle, none on a clone unchanged since the last green idle run; the verdict is one line at the next prompt and a record in `dev/idle-log/`; never in main, never self re-arming | [`scripts/idle-tests-hooks.sh`](scripts/idle-tests-hooks.sh); `scripts/test-idle-tests-hooks.sh` (26 cases with injected clocks: the stagger band, the suspend restart, the lock, the abort, the never-in-main rule, the seams refused outside a fixture); decisions D1-D9 in `specs/136-idle-background-tests/` |
| the type check is one-shot pyrefly, no daemon (feature 142, GM 2026-08-28): mypy needed 12.7 s cold, so it ran through `dmypy` at 380-600 MB per clone that nothing stopped; pyrefly checks the whole engine cold in ~0.6 s, so there is nothing to keep resident and nothing to sweep - the `SessionEnd` daemon hook and `scripts/dmypy-hooks.sh` were retired with it; mypy itself is gone from the lockfiles (GM 2026-08-28) | `TYPECHECK` in the skill Makefile; `[tool.pyrefly]` in its `pyproject.toml`; `tests/tooling/test_typecheck.py` (the check can fail a target; the file list equals mypy's) |
| a performance increase owes its records before the push (feature 129): an increase over that environment's band-1 line (0.0% local, 2.0% codebuild since feature 179) on the total or any seed needs a written explanation + a `perf-audit` confirmation; >5% total / >10% seed the escalated audit; >10% / >20% the GM's sign-off at a terminal | [`l7r/diagram/tools/perf_review.py`](.claude/skills/diagram/l7r/diagram/tools/perf_review.py) `--check`, run by `sync-with-main.sh` at PUSH time (`make perf-review`), printed by `make done` (`perf-gate`); `tests/tools/test_perf_bands.py` + `test_perf_review.py`; the main session cannot write the records - the commands prompt and decline without `AS=perf-audit` (research R1: nothing else distinguishes the shells) |
| the iteration switches (feature 132): with `remote off` nothing dispatches to CodeBuild and the gated push lands on a green local `make done` (LOCAL-GATED); the scope axis was RETIRED in feature 185 (it deferred multi-map rolls while the gate was slow; feature 174 made the gate run everything). `remote` is thrown and released by `make ci-off`/`ci-on`, which commit | [`l7r/diagram/switches.py`](.claude/skills/diagram/l7r/diagram/switches.py) read by the Makefile, the ci dispatcher and every multi-map entry point; `tests/test_switches.py` runs the real Makefile in a fixture and proves each refusal; `tests/tools/test_scope_lock.py` the Python layer; `scripts/test-sync-with-main.sh` the GATED-LOCAL route and the seams-only-in-a-fixture rule; doctrine in [`dev/switches.md`](.claude/skills/diagram/dev/switches.md) |
| a paid CodeBuild run starts only when five conditions pass (engine delta; complete feature on a merge; green local check against this code; no verified record - a green build OR a green local `make done` on the same engine content with nothing new from main; breaker up), and never after a red gate without a green local target (feature 130) | [`l7r/diagram/ci/`](.claude/skills/diagram/l7r/diagram/ci/CLAUDE.md), `tests/tooling/ci/` (99 cases against recorded AWS responses); the route is decided by `sync-with-main.sh` push, tested by `scripts/test-sync-with-main.sh`; the mirror refresh by the same; the build-side FULL door by `tests/tooling/ci/test_door.py` |
| the gate cannot finish below **100% coverage** (feature 174, GM 2026-08-31: *"we want to enforce one hundred percent coverage in whatever the relevant place is ... so that in the future, we literally cannot complete our `make done` in order to merge back into main, and there will no longer be any mechanism by which this can be accomplished"*). The floor had been real but DEFERRED: a plain `make done` ran its test phase with `COV_FLOORS` empty, printed *"coverage floors: deferred to `make done FULL=1`"*, and **stamped green anyway** - and the stamp is the whole of what the push demands, so a merge below the floor was available every day. `COV_FLOORS=1` is also the switch that turns every deselection off, and a deselected test takes its coverage with it, so there is no arrangement that holds the floor over a partial suite: the gate's test phase is `test-full` on BOTH branches now, and `FULL=1` is what still adds the perf bookends. The cost - the gate runs the whole sweep - was stated to the GM rather than used to decline; **`make quick` is untouched**, which the GM exempted themselves (*"I don't think we need to do this for our make quick"*). The tree was brought to **100.00%, 0 uncovered** - 20,646 statements on 2026-08-31, and **22,520** once the GM widened the measured surface to the whole engine on 2026-09-02 (the clause above). No `pragma: no cover` was added TO REACH A NUMBER: unreachable lines were DELETED and five hard-to-reach closures were LIFTED to module level under the GM's own feature-146 doctrine. One was added later and is disclosed - `pipeline/gencache.py`'s `sys.monitoring` callback, which runs and is asserted but is invisible to coverage because coverage uses the same machinery. `GATE_RECIPE` in `gate-stamp.py` salts the stamp key so every record taken before the floor existed is retired at once - a run that was ALLOWED to finish below the floor must not satisfy a push under it | the skill Makefile's `done` phase list + its `test` target's three floors; [`scripts/gate-stamp.py`](scripts/gate-stamp.py) `GATE_RECIPE`; `tests/tooling/test_coverage_floor.py` proves the wiring (the phase list, the default-off switch, the 100 itself, and that bumping the recipe moves the key) |
| a Python file past **1,000 raw lines** fails, on the gate AND on both routes to main (feature 173, GM 2026-08-31: *"if any of them are too large ... then we fail the gate with a message that explains that the clone responsible for this work must split up the file in the manner prescribed in our project guidelines"*). Constitution Principle X clause 13 had been an ask-the-question line since 2026-08-15 and the question stopped being asked - ten files had drifted past, the largest at 4,369. The refusal PRINTS the procedure: the directory-module shape, the `CLAUDE.md` "look here when" index, a named exemplar package, and both carve-outs, so a reader whose file is genuinely ordered data or a derived roster is not sent to do the wrong work. Escape is the clause's own carve-out, `FILE_SIZE_OK: <reason>` in the file's first 40 lines with a reason of 40+ characters, enumerated by `make audit`; it ships with **zero** files using it. Closes half of the constitution's own v1.6.1 deferred TODO - clause 12's expression count on FUNCTIONS is still owed | [`scripts/check-file-scale.py`](scripts/check-file-scale.py) `--selftest` then the scan, in the skill Makefile's `lint` (which the gate runs first, so it reports before the map roll is paid for) and in `sync-with-main.sh` at push time - the same two places as `check-duplicate-defs.py`, the guard it is modeled on; `tests/tooling/test_file_scale.py` proves each rule fires |
| **a modal whose research section moved underneath it does not land unexamined** (feature 234, GM 2026-09-12). What a modal says about a feature IS the docstring of its `Kind` class, written FROM a research section its `Entry:` names, and nothing noticed when that section's content moved - the GM, told to update the pigsty write-up: *"if I hadn't said that ... then would you have done it?"* **Two halves, enforced differently on purpose.** The STALENESS half is a judgment about prose, so `make page-check` reports it free and the PUSH refuses until each named pair is answered - the `entry-drift` agent dispatched and the prose rewritten, or one recorded `ENTRY_DRIFT_OK="<reason>"` covering a whole maintenance sweep, which `make audit` lists. It is enforced rather than expected because the GM ruled the alternative out of existence: *"I don't believe that we should have any such thing as an unenforced doctrine. If it is unenforced, then it is not a doctrine."* Narrowing the key so it would fire less was priced FIRST and is dead - firing only on what a reader SEES takes 28 of 30 research-only commits to 27 of 30. The HEADING half is decidable and never correct work, so it simply GATES: a class `Entry:` that resolves to no section fails, with the deliberately-silent form recognized explicitly and enumerated. `record-format` and `quote-check` are NOT this check - neither ever opens a `Kind` docstring | [`scripts/_entry_owed.py`](scripts/_entry_owed.py) (the report, derived from the engine's own `_TAGS` less `_DATA_TAGS`), [`scripts/entry-gate.sh`](scripts/entry-gate.sh) (the push refusal) and [`scripts/check-entry-headings.py`](scripts/check-entry-headings.py) `--selftest` then the scan - the last two in `sync-with-main.sh` at push, the same place as `check-file-scale.py` and for the same reason: the delta that breaks either is a research-only edit that runs no gate at all. [`.claude/agents/entry-drift.md`](.claude/agents/entry-drift.md) makes the judgment a script cannot |
| **a Bash command that cannot work is refused BEFORE it runs** (feature 236, GM 2026-09-12: *"if we are running a command in bash, and that command has a syntax error such that it is not going to work ... can we not have a hook which checks the syntax of the command before we run it and rejects it early?"*). Four rules, each recording its own so the census can price them apart: it does not PARSE (with the options the tool's own shell can enable - `bash -O extglob -n`, because plain `bash -n` refuses `shopt -s extglob` on one line and `!(x)` on the next, which the tool RUNS); a backtick span bash would EXECUTE, which the parse cannot see because it is valid syntax (the GM: *"putting backticks in a place that they do not belong ... reject it early"*); a `git commit -m` carrying a double quote, a newline or a second `-m`, which is item 3's literal ban and a refusal rather than a rewrite because the broken form parses cleanly into the WRONG message; and a co-author trailer addressed to anything but `noreply@anthropic.com`, by any route (`-m`, `-F -`, `-F <file>`, `--trailer`), after a placeholder address reached main through an `||` fallback on a history that is never rewritten. **Measured before it was written**: the 238 real commands of the session that motivated it, replayed - 2 refused by the parse, both of which had also failed at run time, 0 false positives, and 23 more caught by the `-m` ban; and across the 60 most recent transcripts NOT ONE executing backtick span was a deliberate substitution. Escape `SHELL_CHECK_OK` with a reason | [`scripts/shell-check-hooks.sh`](scripts/shell-check-hooks.sh) + [`scripts/_hm_shell.py`](scripts/_hm_shell.py) (the verdicts; the backtick walk is `_hm_shape.executing_backticks`, shared with the Makefile recipe-comment guard rather than copied); `scripts/test-shell-check-hooks.sh`, which REPLAYS `scripts/fixtures/bash-parse-corpus-2026-09.json` through the hook on every run, so the false-positive rate is measured rather than remembered |
| **house style reaches the writes that do not go through `Edit`** (feature 236, item 4). The corrector only ever saw the Edit and Write tools, so a British spelling written through a Bash payload - an `echo >>`, a `sed -i`, a `python3 -c` - walked past it, which is how they arrived on the session that motivated this. Two halves: the hook reads the Bash payload and CORRECTS it (GM 2026-09-12, ruling on the departure spec D2 had flagged: *"warn when it is the sed shape, and for other shapes just correct it"*) - a command that is itself the fix is reported and left exactly as typed, which is the sed shape the GM named plus any command carrying BOTH spellings of a word, and what a command only NAMES is never touched: a searcher's segment however it is reached, a regex alternation, a dash in a character class, a string whose whole content is one word (a sweep's word list), a path token, and `~/.claude/projects/` as `/tmp` already was - every class of it measured on the real commands the hook had warned on rather than imagined (`specs/236-*/research.md` R10, `scripts/_hm_house.py`); and `make quick` FAILS on one in the delta. The check reads the hook's own `BRIT` table, scans the delta against the merge base plus every UNTRACKED file (where the motivating failures were), judges exemptions from the WHOLE FILE (a line inside a `<blockquote>` or a 「」 quotation carries no opening marker of its own) and ignores a line merely MOVED, so a 1,000-line-gate split does not become a spelling sweep. Not the whole tree: 192 pre-existing lines in 83 files are ledgered under Principle XIII | [`scripts/house-style-hooks.sh`](scripts/house-style-hooks.sh) (matcher widened to Bash) + [`scripts/check-house-style-delta.py`](scripts/check-house-style-delta.py) `--selftest` then the scan, a PHASE of the skill Makefile's `quick` rather than a pytest test - testmon selects by changed code, and a scan whose own code never changes would sit unexecuted through the edits it exists to catch; `tests/tooling/test_house_style_delta.py` |
| **a spec does not spend a review round on what a script can find** (feature 236, item 5). Twenty `spec-fidelity` rounds over two features cost 91 minutes and at least eight of their findings were mechanical. Four checks: a measured figure (a number with a UNIT) in Summary, Functional requirements, Success criteria or Decisions recorded with no `R<k>`/`research.md` pointer in its paragraph; text a `research.md` marked `WITHDRAWN:` still standing anywhere in the TREE outside the two sections that exist to narrate a reversal; an `FR-` no success criterion names, or an `SC-` that names no FR and is not `(spec-wide)`; and a `tasks.md` citing an id the spec no longer defines. Checks 1, 3 and 4 need a `tasks.md`, so the number claim and the milestone push this file protects still pass; check 2 scans the WHOLE TREE (GM 2026-09-12: *"You should do the tree wide scan instead of leaving this half done"* - all five of the survivals that motivated it were outside `specs/`), every tracked and kept-untracked file that is not binary - an EXTENSIONLESS one included, because a `Makefile` is where this project states a rule - holding out only a VERBATIM RECORD (`scripts/fixtures/`, `dev/*-log/`) and the two sections that narrate a reversal, with the marker required to open a line | [`scripts/spec-lint.py`](scripts/spec-lint.py) `--selftest` then the scan, in the skill Makefile's `static` beside `check-file-scale.py` and in `sync-with-main.sh` at push - the same two places, and for the same reason: a `specs/` edit touches no Python and runs no gate at all. `tests/tooling/test_spec_lint.py` proves each check fires and each stays quiet on the correct form |
| **a merge is never committed with its markers in it** (feature 241, GM 2026-09-13: *"let's talk about a tooling fix for the `git add -A` issue that keeps happening"*). Twice in one day a conflicted merge was committed unresolved - once one file, caught by ACCIDENT when `make notes-census` could not parse it, and once **23 files** (six engine modules, five test modules, five manifests, five notes files, two research pages), caught by the gate's lint phase after the commit; the history here is never rewritten, so the merge had to be redone from its base, about an hour. Both times the command was `git add -A`. **The rule is on CONTENT, not on merge state**, and that is the whole design: "refuse `add -A` while MERGE_HEAD exists" would have caught both incidents and would also refuse the correct command, because the end of every RESOLVED merge is exactly `git add -A` with MERGE_HEAD present - and it would miss a marker arriving from a `git am`, a patch script or a typo. **It departs from the proposal the GM approved**, which was the state one; the departure is argued in `specs/241-no-conflict-markers/spec.md` D1 under Principle XVI, with what the content rule cannot see (a binary or delete/modify conflict leaves no triple) recorded in D5. So a `git add` or `git commit` that would stage a file holding the TRIPLE git leaves (seven `<`, a line of seven `=`, seven `>`, each at COLUMN 0) is refused, naming the files. Any ONE marker alone is not a conflict - a line of seven `=` is a Markdown underline and a session writing about merges types the others - and column 0 is what exempts prose, so an indented example or a backtick span passes. **A FENCE is not an exemption**: git writes its markers at column 0 even inside a fenced block, and 7 of the 23 files were Markdown or HTML, exactly the class a fence exemption would have hidden; a file that must SHOW a triple declares `CONFLICT_MARKERS_OK: <reason>` in its first 40 lines, the `FILE_SIZE_OK` shape, and `make audit` lists them - where a MENTION of that marker is not a declaration: it stands at the start of its line and states a real reason, because the first rule (the token anywhere in the head) exempted the DETECTOR from its own backstop, which the audit listing found on its first run. **What a command would stage is asked of GIT** (`add --dry-run --ignore-missing`) rather than enumerated, with the command's own `cd`s, `-C` and `VAR=` assignments expanded - the first implementation read neither and so missed the incident's own shape, `CL=<clone>; git -C $CL add -A`, while a hand-rolled pathspec reading would have fired on `git add .` in a clean subdirectory. It REFUSES rather than narrowing to the clean files, because the compliant command stages what is RESOLVED and only the session knows which those are (feature 164's ladder, re-judged per branch by 212; the same class as `make-only`'s guard-write). Escape `CONFLICT_MARKERS_OK` with a reason | [`scripts/conflict-marker-hooks.sh`](scripts/conflict-marker-hooks.sh) + the shared detector [`scripts/_hm_conflict.py`](scripts/_hm_conflict.py), whose BACKSTOP (`--tracked`) runs in TWO places for the two reasons the checks beside it do: the skill Makefile's `static` phase, so a finding arrives before the map roll is paid for, and `sync-with-main.sh` at push time, because the delta that lands a marker is a MERGE whose own content is often docs, tests and manifests - the DIRECT route, which runs no gate at all. `--selftest` first in both, like every static check in that block. `scripts/test-conflict-marker-hooks.sh` (28 cases, including the two a state-based rule gets wrong - the resolved merge's own `add -A` with MERGE_HEAD present, and `git add .` in a clean subdirectory - and the incident's own command shape) |

**Deliberately NOT enforced**, because a guard that fires on correct work teaches a session to bypass
every guard: the caste sense of "people" (correct in narrative and vow voice), gender-neutral
office-holders (named characters keep their pronouns), Principle XI's kanji triangle, and the
behavioral principles XII/XIV/XV. **File size is no longer on this list** (feature 173, GM
2026-08-31): it read "REPORTED by `make audit`, never gated - the rule prompts a question rather than
forbidding a size", and the cost of that turned out to be measurable - ten files past the bar, the
largest at 4,369 lines, and `make audit`'s own blurb still saying "five files already exceed it"
while scanning a directory list that could not see two of them. It has its own row in the table
above. The lesson kept: this list is for rules a guard would fire on CORRECT work, and a line count
is not one - it fires on exactly the thing it names, which is why it could be gated and the caste
sense of "people" cannot.

**A GUARD THAT CAN PRODUCE THE COMPLIANT COMMAND SHOULD PRODUCE IT** (feature 164, GM 2026-08-30:
*"are there places where a makefile command is refusing to do something but a tool could do a rewrite
or return additional context or whatever?"*). A `PreToolUse` hook may return `updatedInput` - the
command REWRITTEN - and `additionalContext` - a line the model reads - both at exit 0 and both
costing nothing, while exit code 2 costs a model round trip. The audit that answered the GM's
question measured **280 refusals in six days across eleven guards**, and the ladder it produced:

- **REWRITE** where the guard already names the compliant command in its own refusal (`make-only`,
  `no-poll`, `pair`, `house-style`). Never guess: a shape the matcher cannot rebuild exactly keeps
  the refusal, because a wrong rewrite costs a session its command while a refusal costs one turn.
- **TEACH FIRST, FREE** where a block cannot be avoided but the lesson can arrive earlier
  (`guard-file` at Read time, `batching` one turn below its bar, `measure` on the first run).
- **STAY A REFUSAL** where the action is destructive or irreversible, or the refusal IS the content:
  `repo-safety`, `source-block`, `readme`, `discard`, `clone-sync`'s name-routing and live-claim,
  `no-branch`, and the blocks inside `measure` and `gate`. `specs/164-guards-that-correct/research.md`
  R3 records why for each, and R6 does the same for all ten of the Makefile's own refusals - nine ask
  for a DECISION (a written reason, an authorization at a terminal, a procedure), which no
  substitution can supply.

**THE SECOND PASS (feature 212, GM 2026-09-07: *"anytime we are able to say that a command was run
incorrectly and then supply the correct thing to do, then it is at least possible that what we should
do is simply do the correct thing and then inform the session through hook context that we have done
it"*).** Every refusal in the record was recovered from the transcripts - 1,143 across fourteen
guards, 2026-08-25 to 2026-09-07 - and classified PER REFUSAL BRANCH rather than per guard, because a
guard-level verdict is how 164 converted one make-only branch and left four unexamined
(`specs/212-guards-that-do-the-right-thing/research.md` R1, and the corpus is
`scripts/fixtures/guard-refusals-2026-09.json`, replayed by `tests/tooling/test_guard_corpus.py`).
Five more branches DO the right thing: the targeted pytest run and the wrapped engine entry point
(`make-only`), the gate shape `verify` cannot take (`pair`, recorded and permitted), the file-watching
wait (`no-poll`, its qualifier fixed and the foreground form backgrounded), and the clean clone that
fell behind mid-turn (`clone-sync`, synced in). Feature 164's *"clone-sync: a procedure, not a
substitution"* still holds for name-routing and a live claim; the stale-head rule turned out to be a
single idempotent command, which is the case that class does not cover. What stays refused, and why,
is written branch by branch in that R1: the largest family is `make-only`'s `guard-write` (41 - the
compliant form is an Edit carrying a REASON, which only the session has), then `guard-file`'s
no-marker (77, the same reason), `batching` (629 - the fix is to make fewer turns, which no rewrite
of one command can do), and the destructive or decision-shaped refusals above.

**AN ESCAPE IS AN INVOCATION TOO** (feature 169, 2026-08-30). Every guard's BLOCKING decision has
been anchored through [`scripts/_hookmatch.py`](scripts/_hookmatch.py) since 2026-08-25, and every
guard's ESCAPE was still a bare `case "$CMD" in *TOKEN*)` - so a `grep` for a token, a commit message
quoting it, or a heredoc editing a document about guards counted as an escape. Every token matched in a COMMAND now
goes through `_hookmatch.py escape <TOKEN>`, which blanks heredocs and quoted strings, drops
search-command segments and drops `for VAR in` word lists. **The census of which tokens those are is
DERIVED** (`tests/tooling/test_guard_firing_log.py`): it enumerates every `*_OK` in `scripts/` and the
skill Makefile and fails on any that is not classified as `command`, `content` (a marker inside edit
text), `environment` (read as `${TOKEN:-}`, which a mention cannot set), `make-variable` or
`not-an-escape` - because three drafts of that list written by hand were each short by one. A second
check fails the build if a guard ever decides a command escape by substring again. Two reasons it mattered: the escape RATE is
the statistic this project acts on, and every one of the six recorded `measure escaped` entries was a
mention - but also **`measure` clears its repeat-measurement counter on that branch and `gate` removes
its state file**, so a session grepping for a token to learn how the escape works thereby switched the
guard off for its next command. `HOST_GIT_OK` was the worst of them (it guards git writes against the
GM's own repository) and was found by this feature's own spec review, which noticed that its audit
command would have disarmed the guard it was auditing. The one deliberate exclusion, stated at the
point of change: `pair`'s AGENT-PROMPT branch, because a prompt is prose with no command grammar.

**A WRITE THAT WOULD LAND IN MAIN'S TREE IS MOVED TO YOUR CLONE, AND YOU ARE TOLD** (feature 169,
reworked by feature 204; [`scripts/main-tree-hooks.sh`](scripts/main-tree-hooks.sh), escape
`MAIN_TREE_OK`). Read the "NAME THE TREE IN THE COMMAND" section above first, and note what this is
NOT: that rule is about a read-only diagnostic reporting the wrong tree, the GM declined a hook for it
in 2026-08-17, and **it remains deliberately unenforced** - this guard excludes read-only commands by
construction. What it catches is the WRITE half, which nothing caught: `webapp/mainguard.py`, the
Makefile's `guard` and `settlement._assert_not_main_tree` are all in-process or `make`-time, and none
of them sees a `git commit`. On 2026-08-30 two sessions let a `cd /diagram` leak into the next command
and committed into the mirror; each commit existed on no branch anywhere, and each blocked every clean
clone in the container. **Since feature 204 the answer to that is a REWRITE, not a refusal** (GM
2026-09-07): a session's shell stands in `/diagram` until it cds out, and a subagent's starts wherever
its parent's is, so `git commit` from there - or `cd /diagram && git commit` - is rewritten to `cd
<your clone> && git commit`, the shell is left standing in the clone, and the `additionalContext` says
so. A write whose target is elsewhere (the scratchpad, `/tmp`, a clone named by `git -C` or by a
variable assigned in the same command) is allowed silently; only a write that NAMES a path inside main
(`git -C /diagram commit`) is still refused, because a guard never guesses at a command that says what
it means. The census that forced the rework - 173 refusals in a week, 2 of them the motivating shape,
146 with no `cd` at all - is `specs/204-guards-that-move-the-shell/research.md`, and the 173 commands
are the suite's replay fixture, expected to produce zero refusals.

**When you add a guard**, three properties, each learned by getting it wrong: match INVOCATIONS not
mentions (seven false positives in one feature - a grep, a commit message, a docstring, a fixture
argument, a redirect, a test harness, and a hook that blocked its own repair); check the ESCAPE FIRST
or the guard cannot be repaired through the channel it guards; and prove it FIRES by deleting it and
watching a test go red.

**Review subagents run at acceptance, in the background, never per task (GM 2026-08-26; the scope lock this was framed around was retired in feature 185)** - the GM looking at the one map on the sheet is the faster reviewer; the agent earns its time on the pool sweep. Doctrine and the ledger in [`.claude/skills/diagram/dev/reviews.md`](.claude/skills/diagram/dev/reviews.md).

**Every subagent check runs on Opus, whatever the session's model (GM 2026-09-07: *"our subagent checks should all specifically use the Opus model, regardless of what the main claude code session uses"*).** Every file under `.claude/agents/` carries `model: opus` - the reviewers that were `inherit` and the verification agents that were Sonnet since 2026-08-27 alike - and `tests/test_agent_models.py` fails the gate on one that does not; an ad-hoc reader or checker a session dispatches through the Agent tool is dispatched with `model: opus` too. The old split (Sonnet for checkable work, the session's model for judgment) is recorded in each agent file as history.

**Review subagents are pre-authorized (GM 2026-07-27).** Claude Code's default system prompt tells a session not to call the Agent tool unless the user asked - a sensible default that nonetheless sits ABOVE this file in the instruction hierarchy, so it silently outranked the mandate to run `settlement-review` before shipping a Mode B map, and three city maps went out unreviewed with nothing warning. The fix is [`container-scripts/append-system-prompt.md`](container-scripts/append-system-prompt.md), loaded via `--append-system-prompt` by the `claude()` wrapper that `setup-dev-env.sh` installs into `~/.bashrc`: it lands AFTER that line with the same authority and grants standing authorization for the four review agents only. **If a review agent ever gets skipped again, check `type claude` first** - the wrapper is per-container and dies with a rebuild. Broad fan-out, `Workflow`, and deep research still need an explicit request.

**Container.** Launch with gm-assistant's `scripts/launch-container.sh` from THIS repo root (`../gm-assistant/scripts/launch-container.sh`; it reads the current repo's `CLAUDE.md` for the mounts and workdir). The script lives in gm-assistant only, on purpose (GM 2026-08-25: one copy, one repository) - this repository carries no copy. On every fresh container run `container-scripts/setup-dev-env.sh` once (`--check` re-verifies in ~3s - run it the moment something that used to work fails with "command not found" / "No module named" / "resvg not found"). **INSTALL WHAT YOU NEED** - passwordless sudo exists precisely so a session can `apt-get install` or pip-install without asking; never reject a design *because* a dependency is not currently installed. Ports/mounts, the Python 3.14 pin, the two lockfiles and the server-binding logic are in [`docs/container.md`](docs/container.md).

**Key paths**:

- `.specify/memory/constitution.md` - the constitution (copied from gm-assistant at the split; the two diverge from here)
- `.specify/templates/plan-template.md` - Constitution Check gate lives here
- `.claude/skills/diagram/SKILL.md` - usage; `.claude/skills/diagram/CLAUDE.md` - the dev loop (auto-loads under that tree)
- `.claude/skills/diagram/migration-plan.md` - **standing project plan** for converting `/diagram` from hand-authored maps to scripted generation. **Read it before drawing or scripting a settlement map, and update its status table when a conversion lands.**
- `.claude/agents/settlement-review.md`, `building-review.md`, `size-audit.md` - the independent reviews of Mode B maps and Mode A plans (mandatory before a map ships); `.claude/agents/spec-fidelity.md` - the spec review (constitution XVI)
- `/host-l7r-repo/gm-assistant/` (inside the `/host-l7r-repo` mount of the GM's `l7r` checkout) - the setting notes the research cites (`setting/`, `cosmology/`), the webapp, the content skills. Nothing here writes there, and nothing here runs git against `/host-l7r-repo`.

Spec-kit features (the `specify` -> `plan` -> `tasks` -> `implement` flow) live under `specs/NNN-*/`. There is deliberately **no single "active plan" tracked here** - a hardcoded pointer just goes stale as features come and go. For current status, look at the highest-numbered `specs/` dir, its `tasks.md` checkboxes, and `git log`.

**Since the split (feature 131, 2026-08-25)**: this repository's main is `/diagram`; session
clones are `/diagram/.clones/<name>`; the procedure, the hooks, the guards and `gate-stamp` derive
that root from git rather than hardcoding it. The webapp is not here - `webapp/make done` and the
Playwright suite belong to gm-assistant. The Note Intake Workflow, the content skills
(`/chargen`, `/synthesize`, `/name`, ...) and Obsidian Portal are gm-assistant's; a session here
does none of that.
