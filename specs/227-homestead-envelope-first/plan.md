# Plan - 227 the homestead's envelope first, and the page from the code

Spec: [`spec.md`](spec.md). Engine code (`settlement/rolling/place.py`, `fit.py`, `bundle.py`,
`hamletgen/homesteads/stages.py`, `seats.py`, `tools/placement_stages.py`, `pipeline/render_cache.py`); GATED route.

## Constitution Check

VI: Inashiro first, then the pool, the cohort, the review; the GM's acceptance last. X clause 15: the envelope is
one rectangle against the boundary's index and the placed boxes. X clause 5: the placer, the seat standoff, the
page generator and its resolver have unit tests. XII: D1-D7. XVI: reviewed first. Research: rendering.

## Order

1. FR-001/FR-002 the envelope-first placer and the seat standoff; `make map` Inashiro; the counts.
2. FR-003 the counts recorded; the pool; the cohort.
3. FR-004 the page from the docstrings and the `Steps:` sections; `stage_web`'s docstring; the notes retired;
   the boundary on the homesteads plate; the tests.
4. FR-005 the landing re-plates the page.
5. R2; `make done`; the settlement-review; then the GM's rounds on the page in the clone; land on acceptance.
