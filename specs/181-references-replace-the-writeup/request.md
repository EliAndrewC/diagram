# The GM's request, verbatim

2026-09-05, session "Diagram html", after feature 180 landed (the same message was sent twice because
of an interruption; it is reproduced once):

> Okay. Thanks. I think that when we pull open the references modal, it should not appear over top of the previous modal. I think the previous modal, for example, the farmhouse modal, should disappear because right now it looks very awkward, but there is one modal over top of the other. When the references modal is larger than the original modal, then we don't notice this. But when it is smaller, it just looks really weird. So I think that the original modal should disappear. But then if I click on the "Return to Farmhouse writeup" button, then the current modal closes and the original modal reappears.
>
> Also, instead of the title of the references being e.g. "References - Farmhouse" then I want it to be "Farmhouse references" where the word "Farmhouse" is a link and Clicking on that link is the same as clicking the return to farmhouse write up button. And I'm sure it is clear from context that I mean this for all of the references modals, not just the specific one. I am merely using farmhouse as a example.
