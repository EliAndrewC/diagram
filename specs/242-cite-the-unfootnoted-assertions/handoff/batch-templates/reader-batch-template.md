# Feature 242 research pass - ways.html - batch 1 of 1

You are a `source-reader` on a RESEARCH PASS (constitution XII: read what you cite, quote what you cite).
The research page is `/diagram/.clones/diagram-research/.claude/skills/diagram/research/ways.html`; each item
names the line its sentence stands on - `Read` the page around that line when the sentence needs its
context. The items below are assertions about how a place was built, farmed, governed or lived in that
carry NO footnote. Your job, per item:

1. SEARCH for a publicly readable page that states it - primary and scholarly work first (a paper, a
   government or museum page, a gazetteer, a standards body), then serious references (an encyclopedia
   article's own references over the article); NEVER an AI-generated encyclopedia such as Grokipedia, and
   a search-engine summary is a pointer, not a source. Japanese- and Chinese-language pages are welcome.
2. FETCH it and READ it. A quote must be copied from the fetched page, never from a search snippet or
   from memory. ONE fetch attempt per host; if a host refuses (403, TLS, paywall stub), do not retry it.
3. Report, per item, exactly one verdict:
   - `READ` - the URL (a page a person can open without login or payment), the title, the author or
     publisher, the date and kind of work (one line - it becomes the registry write-up), the passage
     VERBATIM inside 「」 (a foreign-language passage: the original verbatim AND your English translation,
     marked as your translation), and SUPPORTS / PARTIAL (say which part is not supported).
   - `CONTRADICTED` - the URL and the verbatim passage that says otherwise, and what it says instead.
   - `NOT-FOUND` - the queries you ran (verbatim), the hosts tried, and any document that PROBABLY holds
     it but could not be fetched (paywall, 403, TLS, a book): its best URL and a Google search query that
     would uniquely identify it. Those go on the GM's download list.
   - `NOT-A-PHYSICAL-CLAIM` - only when the sentence is this project's own decision or a drawing
     convention with nothing about the real world in it. Be strict: a claim about how a real place was
     built or lived in is never this.
4. Keep the report compact and machine-readable: for each item a line `
**Search budget.** This session's web-search allowance is exhausted, so `WebSearch` will refuse. Go straight to
`WebFetch` of the pages most likely to carry each claim - the Japanese, English and Chinese Wikipedia articles on the
thing named, kotobank, Wikisource for a classical text, a museum's, a ministry's or a municipality's page, J-STAGE - and
in `- Searched:` list the pages you fetched (no query could be run). Fetch each page ONCE and use it for every item it bears on.

### item 1 [HIGH] line 34 (FOOTNOTED)
Section: What vehicle used a village lane, and where could the lane run?
Claim: Heavy freight went by boat.
Reader's note: homesteads-buildings-and-the-hamlet-pages.md

### item 2 [HIGH] line 36 (LOCATED)
Section: What vehicle used a village lane, and where could the lane run?
Claim: A trodden path never crosses row crops: historically it runs on the baulk between plots or along the edge of the hem ...
Reader's note: homesteads-buildings-and-the-hamlet-pages.md

### item 3 [MEDIUM-HIGH] line 59 (FOOTNOTED)
Section: Where does a village's freight go?
Claim: Land carriage was for what a barrow or a packhorse could take.
Reader's note: homesteads-buildings-and-the-hamlet-pages.md

### item 4 [MEDIUM] line 33 (FOOTNOTED)
Section: What vehicle used a village lane, and where could the lane run?
Claim: So the vehicle to picture on a village lane is the wheelbarrow and the shoulder-pole porter, and the track is one barrow or one packhorse wide.
Reader's note: homesteads-buildings-and-the-hamlet-pages.md

### item 5 [MEDIUM] line 36 (LOCATED)
Section: What vehicle used a village lane, and where could the lane run?
Claim: ... because past it farmers walk the plot baulks.
Reader's note: homesteads-buildings-and-the-hamlet-pages.md
