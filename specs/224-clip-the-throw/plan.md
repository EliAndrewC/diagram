# Plan - 224 clip the throw, the glossary scan, and the drop's last pass

Spec: [`spec.md`](spec.md). Engine code (`land/cover.py`, `land/wet.py`, `hinterland/frame.py`,
`hinterland/stages.py`, `settlement/finish.py`, `interactive/page.py`, `interactive/raster.py`); GATED route.

## Constitution Check

VI: the motivating map first (Inashiro), then the pool; a settlement-review since the texture re-rolls. X clause
15: the throw's frame test is one comparison per point, ahead of the index. X clause 5: every new line covered
(the prediction and the breach by a unit test and the pool test, the prefilter and the direct parse by tests
against the general forms). XII: D1-D3 recorded; rendering only. XVI: the spec reviewed first.

## Order

1. FR-003 (glossary prefilter) and FR-004 (the direct parse) - small, with equivalence tests.
2. FR-001/FR-002: `scatter_frame` in `hinterland/frame.py`, the stage sets it on the settlement, the two throw
   loops test it, `finish()` records a breach, the pool test; Inashiro; the pool.
3. R2 in the phase-marked worktree; the 48-map cohort's overhangs; `make done`; the review; land.
