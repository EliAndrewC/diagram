# Feature 224 - the GM's request, verbatim (2026-09-11)

After feature 223 landed, the session's report ended: *"Left for later, in order: the scatter still throws its
off-map blades (only no longer writes them), the page's explanations blob at 0.3 s, and the remaining pass of
`drop_offmap` over every classed string."* The GM:

> Go ahead and do the left for later items now. Thanks.
