# Plan - 215 the floor itself

Tooling with one small engine change (D3). Constitution: XIII/XIV (nothing regresses; the two pool maps and
the reference brief change is the GM's), XVI (the spec reviewed against the request), X (100% holds; the
unique lines become unit tests).

1. `generate()` carries failures/attempt in the manifest meta (D3); the reference spec aligned (D1).
2. The gate's readers of Inashiro and Kuwabata read the pool: a `tests/gate/_pool.py` helper (`gate_obtain`,
   the manifest, the plan); the fifteen gate modules, the ratchet, the lane rules, the floor's subjects.
3. The three duplicates (FR-002); the immune test on the reference (FR-003); the re-roll loop on stand-ins
   (FR-004); seed 43's lines and the synthetic kink attempt (FR-005).
4. The roster, `make done`, the census, R2, the record.
