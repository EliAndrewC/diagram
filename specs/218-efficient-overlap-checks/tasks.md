# Tasks - 218 efficient overlap checks

Spec: [`spec.md`](spec.md). Plan: [`plan.md`](plan.md). All `research: rendering` - nothing here
changes how a place is built; the maps are byte-identical by requirement.

- [ ] T01 spec-fidelity review of `spec.md` against `request.md` - FAITHFUL before any code; the
      `218-start` bookend taken on unmodified code (FR-004 b)
      research: rendering
- [ ] T02 the windbreak's keep-outs indexed once (`GroveBlocks`, `Seats`), verdict-equivalence unit
      test, reference hamlet regenerated byte-identical (FR-001, FR-006)
      research: rendering
- [ ] T03 the marsh scatter's watercourse grid per pad; the hinterland re-profiled; the census of the
      stage's other per-candidate scans, each converted or listed with its share (FR-002, FR-006)
      research: rendering
- [ ] T04 the pool regenerated byte-identical (`make maps`, `git status` clean on `pool/`) (FR-003)
      research: rendering
- [ ] T05 the doctrine: constitution 2.24.0 Principle X clause 15, root `CLAUDE.md`, skill
      `CLAUDE.md`, `dev/performance.md`, the plan template (FR-005)
      research: rendering
- [ ] T06 `make done` green; `218-end` bookend and `perf-report`; the timings the GM asked for in
      research R3/R4 (the stage profile after, the from-scratch roll, the gate's time beside its
      predecessor); spec IMPLEMENTED; land (FR-004, SC-001..004)
      research: rendering
