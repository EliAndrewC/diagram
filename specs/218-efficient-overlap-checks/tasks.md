# Tasks - 218 efficient overlap checks

Spec: [`spec.md`](spec.md). Plan: [`plan.md`](plan.md). All `research: rendering` - nothing here
changes how a place is built; the maps are byte-identical by requirement.

- [x] T01 spec-fidelity review of `spec.md` against `request.md` - FAITHFUL before any code; the
      `218-start` bookend taken on unmodified code (FR-004 b)
      research: rendering
      verify: DONE. FAITHFUL at round 2 of 5 (round 1: the clause in the GM's terms, a numeric census bar, the GM's own target in SC-002); 218-start bookend median 25.0 s across seeds 4/25/39/47 on commit bbb2ac9f
- [x] T02 the windbreak's keep-outs indexed once (`GroveBlocks`, `Seats`), verdict-equivalence unit
      test, reference hamlet regenerated byte-identical (FR-001, FR-006)
      research: rendering
      verify: DONE. GroveBlocks + Seats in homestead_parts/grove_blocks.py; village_grove asks the index; tests/settlement/test_grove_blocks.py proves verdict equivalence on a lattice; Inashiro byte-identical; stage_windbreak 7.27 s -> 0.50 s
- [x] T03 the marsh scatter's watercourse grid per pad; the hinterland re-profiled; the census of the
      stage's other per-candidate scans, each converted or listed with its share (FR-002, FR-006)
      research: rendering
      verify: DONE. the marsh's watercourse grid per pad, then KeepoutGrid (one grid per scatter) for commons and marsh, the halo indexed, Footing for strip/trunk, _crop_refuses for parcels; census in research R2 (13 converted, 3 listed under the 0.10 s bar); stage_hinterland 8.09 s -> 1.27 s, above the 1 s target - reported as a shortfall in R3
- [x] T04 the pool regenerated byte-identical (`make maps`, `git status` clean on `pool/`) (FR-003)
      research: rendering
      verify: DONE. make maps: reference, tripwire seeds and all five hamlets regenerated clean; git status clean on pool/ - every map byte-identical
- [x] T05 the doctrine: constitution 2.24.0 Principle X clause 15, root `CLAUDE.md`, skill
      `CLAUDE.md`, `dev/performance.md`, the plan template (FR-005)
      research: rendering
      verify: DONE. constitution 2.24.0 Principle X clause 15 + log + footer; plan-template X entry; root CLAUDE.md bullet; skill CLAUDE.md Performance bullet; dev/performance.md third shape with the measured table
- [x] T06 `make done` green; `218-end` bookend and `perf-report`; the timings the GM asked for in
      research R3/R4 (the stage profile after, the from-scratch roll, the gate's time beside its
      predecessor); spec IMPLEMENTED; land (FR-004, SC-001..004)
      research: rendering
      verify: DONE. make done green 215 s (FULL, cold rolls, 21 guard suites, a concurrent gate; pytest 131 s, 3,487 passed, both floors 100%; roll census 7 of 6, green); 218-end bookend total 97.3 s -> 48.2 s (-50.5%), band 0; R4 written; spec IMPLEMENTED
