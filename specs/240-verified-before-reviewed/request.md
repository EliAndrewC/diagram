# The GM's request, verbatim (2026-09-13)

Asked after feature 230 landed, when the session's own time breakdown showed that its last three
review rounds were largely spent finding defects the previous round's fix had introduced.

<!-- SOURCE: GM NOTES - DO NOT MODIFY -->
Okay, the "Diagram (Kuwabata)" session is working on something similar. So you should probably coordinate with that session. as part of this work to avoid duplicating effort. However, Shipping fixes faster than you are verifying them. Sounds like a problem that we literally cannot fix with better instructions. because procedures which rely on someone, whether it's a human or an LLM, remembering to do something are flawed, and we should have our tooling enforce this when possible. So with this in mind, how do we either stop an unverified fix from going out for a full review round or make it so that the review round detects whether a fix has been verified and short circuits and exits immediately if it is not. For example, is there some kind of hook that is run when sending something out for review that checks whether the verification has been done in an automated way? like, I'm not one hundred percent sure what you mean when you say that you were sending out fixes for reviews without verifying them. But if the verification involves something like running the unit tests, or some other makefile command, then we could have a hook or even have the first stage of the review process be checking whether or not that was done. And then if it wasn't, then the entire review is blocked, and then we don't lose twenty minutes. measuring a fix against the drawn artifact before dispatching is another example of something that it would be good to enforce at the tooling level. I believe that the other session is working on something that relates to this, which involves performance measurements where I think it made a similar mistake. And if memory serves, was asking for a review on something where it had guessed at performance numbers instead of measuring them. So I believe I have made a similar request to that session that has to you with requiring that those things are measured and having the review short circuit early if they are not or even having it enforced at the tooling level where when possible, you could have those numbers generated by the reviewer or have the reviewer read the auto generated file, which includes the command which generated it or something. And in that fashion, confirm that the review is not a waste of time. And if it looks like those prerequisites have not been met, then the review should exit early. So you should definitely coordinate with that other session to see how far along they are with that work and whether you would be duplicating effort in anything that you are doing. You can, for instance, pull their branch into yours if that is appropriate, or you might simply be working on different hooks and different things, in which case you will not need to spend much time or effort coordinating with them.
<!-- END SOURCE -->

## What prompted it, in this session's own numbers

Feature 230 ran 14 h 18 m from claim to landing across 14 `settlement-review` passes. The breakdown
the GM asked for is in `research.md` R1. Two failures are the motivating cases, and both are the same
shape - a fix reasoned from a MODEL and dispatched for review without being measured against the
artifact:

- the notice board's canopy keep-out filed grove CLUMP BASES with one nominal radius (14 ft) while the
  drawn crowns reach a median 16.3 ft and a p90 26.7 ft past that base, so a board "passed" by one inch
  with a crown over its own center. A full review round found it.
- the perf explanation attributed a stage's growth to the map because none of the feature's new passes
  appeared in a cumulative-time profile. A `perf-audit` round refuted it by A/B in one measurement.

## The coordination the GM asked for, and its answer

`Diagram (Kuwabata)` holds feature 239 (measurable guards, and figures that are derived). Its section D
specifies the same contract shape for `spec-fidelity` and figures in spec documents: NOT-REVIEWABLE
returned before anything is read, not consuming one of the five rounds, with a `measurements.json`
carrying each figure's re-runnable command. Asked directly, that session's answer was: take the
settlement-review half; 239 touches neither `.claude/agents/settlement-review.md` nor
`scripts/pair-hooks.sh`; nothing of 239 is implemented yet, so whichever lands first sets the shape;
build on `measurements.json` rather than defining a parallel format, and add fields if needed. Two
warnings from its own rounds were carried into this feature's first draft: that a timing taken while
another session is rolling a map is corruption that looks like a result, and that a recorded command
reading a corpus under `/tmp` is not recorded at all. The second stands. The first was WITHDRAWN by that
session after two re-runs showed its only evidence to be a sampling bug in its own harness, with no
contention measured at all; the load-average rule survives in 239 as a precaution, not a finding, and
`research.md` R4 records the correction.

## The split, as the peer session stated it in full (2026-09-13)

Asked directly whether feature 239 touches any of the performance machinery, that session answered that
it does not: `make perf-explain`, `make perf-audit`, `l7r/diagram/tools/perf_review.py` and
`.claude/agents/perf-audit.md` are all this feature's. Its complete list of what 239 edits is
`.claude/agents/spec-fidelity.md`, `.specify/templates/tasks-template.md`, `scripts/spec-lint.py` (a new
check 5, keyed on check 1's existing `_FIGURE` and `_UNIT_ALT`, both unchanged), a new `make figures` and
`make hookbench`, `scripts/house-style-hooks.sh` with `scripts/_hm_house.py`, and one fixture under
`scripts/fixtures/` - nothing under perf, nothing in `settlement-review.md`, nothing in `pair-hooks.sh`.

It accepted both handbacks (the timing-record rule is 239's FR-011c, where it now records that its own
quiet threshold was chosen rather than measured; the run-from-repository rule is 239's `make figures`), and
it will record this feature's finding-keyed trigger in 239 as a considered extension pointing at this
feature's findings record, so that there is one findings format rather than two.
