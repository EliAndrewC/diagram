# Tasks: The Cold Gate - Faster Rolls, and a Cache Warmed While Idle (feature 138)

Verification: `make quick` while iterating; the byte-identity sweep (`scratch baseline vs after`) after every
router change; ONE `make done` per batch; `make maps`; the perf bookends.

## Phase 0 - baseline (FR-001)
- [x] T01 per-stage timings of the seed-19 polder and the reference; the manifest of every gate roll spec and every live pool map on unmodified code (the byte-identity oracle); `make perf LABEL=138-start`
- [x] T02 the cold gate's baseline wall clock: 5 m 42 s (feature 135 R11, the run after main's merge re-keyed every roll) - taken as the baseline; the after-figure is R4

## Phase 1 - the router (FR-002, FR-004)
- [x] T10 `FabricIndex` in `hamletgen/ways.py` (or a sibling module): per-polygon bounds once, a cell grid over a box, `candidates(q)` returning the obstacle / tight / line entries whose inflated bounds cover q's cell; a property test that on random fabric the index's candidates are a superset of the bbox scan's, and that `fouled` agrees on every sample
- [x] T11 `clear_runs` builds the index once per call and tests each sample against its cell; `_route` builds ONE index for the routing box and derives the free lattice from it (no per-cell `clear_runs`)
- [x] T12 byte identity: the seed-19 polder's manifest equals the baseline's; then every gate roll and live pool map (T01's oracle); stage timings recorded

## Phase 2 - the connector (FR-003, FR-004)
- [x] T20 `path_violations`: the pairwise crossing count becomes a sorted sweep; a test with a hand-made hit list proves the count identical to the pairwise form on random inputs
- [x] T21 byte identity again, all maps

## Phase 3 - the numbers (FR-005, FR-006)
- [x] T30 the cold gate re-measured (every cached roll re-made); `make perf LABEL=138-end`; `make perf-report AGAINST=138-start`; any slower seed diagnosed in writing
- [x] T31 the idle run warms the interactive gate: after a sync that moved the rolls' keys, `make idle-tests` (run by hand for the measurement, as the hook would) leaves the next `make done` reporting every roll served; anything the gate reads that the idle run did not fill is added to it
- [x] T32 `GEN_TIME_BUDGETS` and `dev/performance.md`: the measured cause and the fixed figures; the "bisection" explanation corrected (FR-008); the budget entries left at ~4x the OLD solo times - lowering them buys nothing and a busy box would trip them (the guard is for pathological growth, not for drift)

## Phase 4 - close
- [x] T40 `make done` green (3,878 passed; cold 1 m 36 s after the alias fix re-keyed the rolls), the live pool regenerated byte-identical (`make map` x 4 stands in for `make maps`), the stop-work procedure
