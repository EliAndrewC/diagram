# Plan - 216 the floor as doctrine

Tooling and doctrine. Constitution: VI amended (D1); X (the tool owes 100%); XIII/XIV (nothing regresses; the
lines the cut rolls alone reached become unit tests); XVI (the spec reviewed against the GM's words).

1. `tools/roll_audit.py` + tests + the Makefile target + `make audit` (FR-001).
2. The four cuts (FR-002): the woodland band on the pool's map; the clamp decision; the polder tests on Polder 12;
   the seatings' partial roll in a child with the nine lines as unit tests.
3. The doctrine (FR-003): constitution 2.22.0, root CLAUDE.md, tests/CLAUDE.md, dev/loop.md.
4. The roster, `make done`, the census, R2, `make roll-audit` on the landed baseline, land (FR-004).
