# The GM's request, verbatim

Captured BEFORE `spec.md` was written, so the spec can be graded against the GM's own words rather
than against the author's summary of them (constitution XVI). Nothing here is edited - not the
punctuation, not the dictation artifacts. The house-style guard exempts this file for exactly that
reason.

## 2026-08-25, the period this feature serves (from the session "Reference testing", the request that produced feature 132)

> Okay. I have some changes that I want to make. And for the current period, I would like to focus on getting our reference hamlet to be exactly right. and address all known issues with it and to not bother to run the larger set of tests. Because we know that those tests fail. I mean, some of them succeed, but we know that it is the case that a large number of our full test suite where we run, I think, currently something like forty eight different maps, which is something like some number of different maps with some number of different seeds per map. And we know that a lot of that fails. And so I don't think that there's value in running that full test suite while we are still iterating on only the reference map. because if we fix one thing on the reference map, but we know that other things are still wrong on the reference map, then trying to get everything else working is going to waste a lot of time relative to just getting a baseline set of stuff running. However, I think that right now, we have some merge gates where things don't make their way back into the main branch until all of those tests pass. Is that right? So is there a way to do this development effort? where I don't know in advance all of the stuff that we need to fix because I can point out a couple of things on the map right now, which I know are wrong. So is the correct thing to do to use SpecKit to define a feature that basically says get the reference map up to the point where Eli accepts it as the developer, and then we update the task list as we go? What is the idiomatic way to do this? [...]

The session proposed feature 133 as *"the reference hamlet is accepted by the GM": tasks added as
defects are pointed out; milestone push whenever every task is closed; spec-fidelity re-reviews
each batch*, and the GM accepted (*"Yes. That sounds like exactly what I want."*).

## 2026-08-25, opening the feature (after feature 132 landed)

> Yes. That sounds good. However, rather than tell you the first task, let's go ahead and open 133 as what is essentially an empty skeleton where the goal is acceptance by the GM. However, I would like to provide the tasks one at a time. Part of my reasoning for this is that I am going to use the length of time which each task takes to implement in order to measure the effectiveness of our tooling and our approach. For instance, if I ask for something which seems relatively simple to do, and then it takes a very long time, which depending on the task itself could be five minutes, twenty minutes, forty five minutes, etcetera - Then that will be a good cause for asking ourselves, hey. Why did that take so long? Was it more complicated than I expected? That may sometimes be true. Or were we running lengthier tests than what we actually needed to run? or perhaps were we doing far more cycles than we needed, making one small change and then rerunning lengthy tests only to make another small change and then repeat when instead we could have made a batch of changes, which would have all been validated by the same slightly lengthy tests, that kind of thing. Therefore, I don't want these results thrown off by the time that it will take to generate the skeleton. So let's go ahead and create the feature. and then I expect that this feature will sit in the clone rather than merging back into main. because there will at least be one task incomplete where that task is the GM accepting the current state of the hamlet. So our automated tooling should stop it from going back into main. Right? I mean, I think that's how that works. though I'm not a hundred percent sure because I know there are some ways to get things into Maine outside the context of features. So this will also be a good test for seeing whether working on a feature through speckit triggers those dates successfully. To be honest, I'm not sure how the tooling can know whether we're working on a feature or not. So that might be something that we need to figure out as part of this. Or maybe there is already something in place for that I have not checked. Either way, please proceed with making the feature. And then once that is done, I will create a fresh session, and then I will attempt to add a task to it. And then we will see how long that takes and so forth. You should certainly incorporate the motivation that I am providing here into the feature itself so that we know that part of this feature is evaluating the performance and acceptability of our tests. Also, keep in mind that we have turned off AWS testing. However, I think it would be good if as part of doing this work, we know that AWS testing should not even be attempted in the first place, but maybe we should make it so that it is still some sort of error or auditable event if AWS testing would have been triggered while we are in a period where it should not be. Does that make sense? What I'm basically getting at is this is the dry run for how we will do this kind of feature iteration in the future. And so one of the failure cases is dispatching to AWS to run fifteen minutes worth of tasks at a time that it is not appropriate to do that. And so while we do have AWS turned off, then I would hate to have it be the case that turning off AWS suppresses the finding that we would have been dispatching to AWS to run many hours and many dollars of tests. So part of this feature should, at the very beginning, involve modifying our tooling so that when AWS testing is turned off, we still track when it would have run. And then afterwards, we are able to audit whether it should have been initiated during those periods. And if the answer turns out to be no, then that means that we need to make more tooling changes in order to be smarter about when we run the longer tests. This is very important for our development workflow because iterations are expensive in terms of wall clock time. And if me asking for a simple change results in half an hour of work being done when it should have only taken five minutes, then that limits the number of changes that I can make in a single day. Therefore, this understanding should be incorporated into this spec kit feature. I think our spec kit constitution and our project documentation generally for our Claude dot MD and our skill dot MD and such already contains this sensibility, but to the extent that it does not, then we should update that as well because this is not just a motivation of this specific feature. This is a core motivation behind everything that we have done in the tooling, and gating that exists for this repository. Every future session should keep this in mind as a project goal. both for the commands that we choose to run and for the way in which we look for opportunities to improve our tooling and for how we interact with that tooling.

## 2026-08-25, after the skeleton was pushed to main - the GM on the routes

> Okay. So explain the difference between the gated route and the direct route to me. do we actually want the director out anymore? I can see an argument for wanting it because I will probably always want to be able to make small changes for which it would add a lot of overhead to create a spec kit feature. However, I am concerned about things being pushed back to main in advance of me saying that I want that. For example, I did not want what you have just pushed back to main to be pushed back to main, and I thought that I had made that clear in my original request. but I guess I was too ambiguous or you had enough leeway in considering the results to be able to bypass it. Thus, even though we are literally working on a feature and that feature is not yet done, you still pushed back to main anyway. And that is the kind of thing that I am trying to prevent with this tooling. Therefore, what do you recommend we do in order to prevent this from happening?

The session recommended: (1) the active feature is DERIVED (any `specs/NNN-*/tasks.md` with an open
task that the delta touches, or that the pointer names), not declared; (2) while one exists, no push
lands on either route, no flag, with one mechanical exception - a delta that is solely that feature's
own `specs/` directory (the number claim); and asked whether to keep the exception and whether to
do it now under 133's unmeasured skeleton phase.

> Yes. That sounds good, please. implement that. I do think that keeping the spec directory only exception is good. And, yes, I do want it done now under the 133 skeleton.  Thanks
