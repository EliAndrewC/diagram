# The GM's request, verbatim (2026-08-29)

Feature 159 reported that the shipped `paddy` modal states "four to six inches of water" as fact,
citing `tabayashi-1986`, and that a `source-reader` pass could not source it. The GM:

> Yeah, we should definitely either do more research to confirm the number or label it as an
> educated guess.

The research pass was run first (constitution XII's ladder). It did not confirm the number and it
did not leave it a guess either: it CONTRADICTED both the number and the shape of the sentence.
