# Tasks - feature 189

Spec: [`spec.md`](spec.md). Request: [`request.md`](request.md).

Every task is `research: rendering` - a code form, nothing physical behind it; no explanation's text changes (FR-011).

- [ ] T01 FR-005: snapshot today's registry (every field of every class) to `tests/fixtures/classes_before_189.json` by parsing `classes.py` with `ast`
      research: rendering
      verify: 52 keys in the snapshot; the file is committed before any registry change
- [ ] T02 FR-001/FR-003/FR-004: the package - `_base.py` (dataclass, `Label`, `ANNOUNCED`, lead/phrase, the docstring parser), the five family modules generated by script, `siblings.py`, `__init__.py` re-exporting the old surface, the package `CLAUDE.md`
      research: rendering
      verify: every old importer resolves; `classes.py` deleted; no module past the bar
- [ ] T03 FR-006/FR-010: tests - the parser, the snapshot equality, the surface; `test_page.py`'s stub text
      research: rendering
      verify: `make quick` clean; the equality test compares all 52 classes field by field
- [ ] T04 FR-007/FR-008: `gate-stamp.py` - the `page` area is `interactive/` (`*.py *.js *.css`) hashed by bytes; `test-gate-stamp.sh` cases (a docstring edit owes `page` not `diagram`; a code edit owes both)
      research: rendering
      verify: `scripts/test-gate-stamp.sh` green; `make hooks-test` green
- [ ] T05 FR-009: `interactive/CLAUDE.md`, root `CLAUDE.md` tweak-lane bullet, 188 D4 noted closed
      research: rendering
      verify: read back
- [ ] T06 `make done` green (the registry's Python changed semantically - classes instead of calls), push; then PROVE the property: a docstring-only edit in the landed package makes `make done` short-circuit and `page-check` alone satisfy the push
      research: rendering
      verify: green gate; the push lands; the property demonstrated and its timings in the answer to the GM
