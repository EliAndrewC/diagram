# Tasks - feature 189

Spec: [`spec.md`](spec.md). Request: [`request.md`](request.md).

Every task is `research: rendering` - a code form, nothing physical behind it; no explanation's text changes (FR-011).

- [x] T01 FR-005: snapshot today's registry (every field of every class) to `tests/fixtures/classes_before_189.json` by parsing `classes.py` with `ast`
      research: rendering
      verify: DONE. `classes_before_189.json`: 51 classes, 98 sibling links, every field, taken from the SOURCE with `ast` (no engine import) and committed before the conversion
- [x] T02 FR-001/FR-003/FR-004: the class form generated by script; `_base.py` (dataclass, `Label`, `ANNOUNCED`, lead/phrase, the parser); the line count MEASURED and the package split taken only if the converted module passes 1,000 lines
      research: rendering
      verify: DONE. Generated by script from `_DEFS` (round-tripped against the snapshot before writing). MEASURED: the class bodies alone are 1,195 lines and a single module would be about 1,500, so the split is taken: `_base.py` 231, `homestead.py` 281, `greenery.py` 176, `fields.py` 222, `water_and_ways.py` 248, `dikepond.py` 313, `siblings.py` 164, `__init__.py` ~56 - none near the bar. `Kind` registers subclasses; `__init__.py` orders them by family in FR-007 order (the import sorter had alphabetized the modules - caught by the order test) and re-exports the old surface; `classes.py` deleted; the package CLAUDE.md carries the format with a worked entry
- [x] T03 FR-006/FR-010: tests - the parser, the snapshot equality, the surface; `test_page.py`'s stub text
      research: rendering
      verify: DONE. `test_classes_docstrings.py`: the snapshot equality over all 51 classes and every field including siblings; the order; the parser (four tags, wrapping, six malformed shapes each naming the class, newline-free ids); a Kind builds its FeatureClass; the surface. `test_classes.py` unknown-pair test calls `install_siblings`; `test_measured_surface.py` reads the widened page area; the stub text says `interactive/classes/`. `make quick ALL=1` clean (the selected form disagreed across workers after this restructure - testmon, not the tests)
- [x] T04 FR-007/FR-008: `gate-stamp.py` - the `page` area is the assets plus the registry files, the registry hashed by bytes; `test-gate-stamp.sh` cases (a docstring edit owes `page` not `diagram`; a code edit owes both)
      research: rendering
      verify: DONE. `AREAS["page"]` = `interactive` with `assets/*.js`, `assets/*.css`, `classes/*.py`; `RAW_AREAS = {page}` hashes those by bytes (`hash_files(..., raw=)`); `_matches` is fnmatch on the area-relative path. `test-gate-stamp.sh`: a registry file stamped in both areas is allowed; a DOCSTRING edit refuses page and not diagram; re-stamping page admits it; a CODE edit refuses both. Suite and `make hooks-test` green
- [x] T05 FR-009: `interactive/CLAUDE.md`, root `CLAUDE.md` tweak-lane bullet, 188 D4 noted closed
      research: rendering
      verify: DONE. `interactive/CLAUDE.md` classes row -> the package and its own CLAUDE.md (families, the format, a worked entry, the conversion record); root `CLAUDE.md` tweak-lane bullet gains the docstring clause and its page-check figure reads the measured 26 s; 188 D4 marked CLOSED by 189. Also fixed on the way: the root Makefile FORWARD line that feature 188 had broken by appending after its continuation backslash
- [ ] T06 `make done` green (the registry's Python changed semantically - classes instead of calls), push; then PROVE the property: a docstring-only edit in the landed package makes `make done` short-circuit and `page-check` alone satisfy the push
      research: rendering
      verify: green gate; the push lands; the property demonstrated and its timings in the answer to the GM
