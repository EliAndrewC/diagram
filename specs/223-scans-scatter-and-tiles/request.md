# Feature 223 - the GM's request, verbatim (2026-09-11)

After feature 222 landed, the session's report ended: *"Left for another feature, in the audit's order: the
bamboo seat scan (3 s on Sawada), the wells key, dropping the off-map scatter in the writer, and `RASTER_R`."*
The GM:

> Go ahead and do those items which you left for another feature. Thanks. And is there anything that we can
> do about that SVG render time?

"That SVG render time" is the resvg render of the page's picture at 3 px per map px, 2.1-3.6 s per hamlet in
feature 222's after-measurement (specs/222 research R2) - the largest step of the page once the JPEG replaced
the lossless encode - and the 2600 px PNG render of 1.2 s, which 222 already hid under the page's own work.
