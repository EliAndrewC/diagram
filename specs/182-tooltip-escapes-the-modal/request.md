# The GM's request, verbatim

2026-09-05, session "Diagram html", after feature 181 landed:

> I just noticed a visual display bug. When I move my mouse over a word that has a definition, then a tool tip will pop up. But if that word is at the edge of the modal, then it gets cut off, and the modal gains a horizontal scroll bar. But that is not how this should work because the tool tip itself should be outside of the modal, and it is okay for the tool tip to extend off the right or the left of the model. Of course, the tooltip should remain visible on the page, and so it should not extend off the right or left of the page itself. But it should not be trapped within the modal and bound by the edges of the modal itself. Please fix this. Thanks.
