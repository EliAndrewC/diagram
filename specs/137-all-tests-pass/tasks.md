# Tasks: One Hundred Percent of the Tests Pass (137)

Checked off only when verified on the whole tripwire, the gate cohort and the 24-seed cohort (no
rotation). Every task classified per constitution v2.12.0.

- [x] T01 spec from the GM's verbatim request; `spec-fidelity` review - verdict in spec.md Status
- [x] T00 the opening message to the "Diagram tests" session (FR-000): sent 2026-08-28 (msg 7b11f701) - the division of labor and the request for a safe sha; recorded in handoffs.md
- [x] T02 the efficiency session's safe work pulled (FR-001: ebbf29ef, then 135 on main) - `handoffs.md`; first full measurement on the merged tree (2026-08-28 04:10-05:00Z): tripwire 27/33/37/47 as pinned, 41 ok; gate cohort 41 OK, 42/43/44 as pinned; the pool hamlets Kashikawa/Mizuguchi/Sawada red (T10); the 48-seed audit **2 of 48 pass** - `lanes_form_one_network` 26, `lanes_bend_like_paths` 15, `title_clear_of_features` 8, `village_windbreak_is_continuous` 6, `farmhouses_reach_a_way` 6, `houses_clear_of_paddies` 5, `lanes_reach_something` 4, `fields_clear_of_road` 3, `lanes_clear_of_bamboo` 3, `features_do_not_overlap` 2, `captions_clear_the_ways_they_stand_on` 2, `paddy_bunds_do_not_stagger` 2, one each `village_trees_unshade_from_west`, `kosatsuba_by_the_road`, `long_ditches_have_a_footbridge`. The lane web is broken across the TIER, not on three seeds; the COHORT_BASELINE pin (22/24) is far stale (research: procedure)
- [ ] T03 the lane web across the tier: `lanes_form_one_network` (26 of 48), `lanes_reach_something`, `farmhouses_reach_a_way` - measured on seed 37: the web is in pieces BEFORE the smoothing (the first touch pass's orphan joiner reports 3 orphans it cannot link: a 12 ft slot between a house and a yard that the 7 ft fabric pad closes both sides); a cohort pass-rate bisect over the period's commits names the commit that broke the tier; the smoothing now refuses any rewrite that disconnects the web unless the touch pass can rejoin it (kept: Inashiro clean, seed 37's bends gone); the orphan joiner's ladder - a route at the junction margin, a detour round a yard (8x the gap), a house-safe drop - took the 48-cohort from 2 to 7 passing and `lanes_form_one_network` from 26 seeds to 9; seed 37 CLEAN (pin out), 27 and 47 shrunk; the cohort bisect (24 seeds per commit, a clean scratch clone): **18/24 at the lock (513c419b), 2/24 at T32 (0ecb352f)** - `houses_clear_of_lanes` 13, `features_do_not_overlap` 10, `lanes_form_one_network` 10, `title_clear_of_features` 7; T41 and T52 produced no verdict (the batch crashed there on the knot collapse, guarded only at T99). So the tier was broken by T31/T32's own passes: a junction link that 'may brush a fence' at a 4 ft footprints-only margin draws lanes the gate refuses. Variant A under measurement: every junction link held to the web's fabric margin (`_clear_link`, then the routed detour) (research: rendering - the web's own geometry; the historical rule "interconnected" is already read)
- [ ] T04 `lanes_bend_like_paths` (18 of 48 after T03's ladder): every web lane now records the pass that drew it (`role`: cut / skeleton / join-orphans / bridge-breaks / straggler / touch / smooth); seed 43's fold is a STRAGGLER (the door path) folding inside the pocket between a house corner and a neighbor's yard, and the touch pass's two links onto it make the GM's triangle; chording stragglers at the footpath's 4 ft margin straightened it and rotated a bend onto Inashiro (dead end, recorded in ways.py); the router (`_route`) is Dijkstra on a lattice with no turn price - the fix belongs at the straggler's DOOR seat / the router's turn cost (research: physical - the bend rule is READ, research/homesteads.md 'How does a village lane bend?')
- [ ] T05 seed 33: `village_windbreak_is_continuous` - the belt thins around a plot inside the band (research: physical - the yashikirin's relation to the plot; cite)
- [ ] T06 seed 44: `houses_clear_of_paddies`; seed 42: `farmhouses_reach_a_way`; seed 27: `lanes_clear_of_bamboo`; seed 43: `title_clear_of_features`; seed 47: `fields_clear_of_road`, `long_ditches_have_a_footbridge` (research: rendering / procedure per item, recorded in each)
- [ ] T07 cohort seeds 22 (`field_ringed`) and 24 (`paddy_bunds_clear_the_supply_channels`) (research: physical - cite the field rule each check encodes)
- [ ] T10 the pool hamlets (FULL-only, `tests/full/test_villages.py`; measured 2026-08-28 on the merged tree): Kashikawa seed 3 `features_do_not_overlap` + `lanes_form_one_network`; Mizuguchi seed 23 `village_windbreak_is_continuous`; Sawada seed 6 `lanes_form_one_network` + `title_clear_of_features` - the same mechanisms as T03-T06 (the web stubs, the belt hole, the title seat); verified by `make maps` after a clean run and by the FULL sweep at the end (research: as the mechanism tasks)
- [ ] T08 every pin table empty; the unlocked gate, the tripwire and the 24-seed cohort green; handoff to the efficiency session recorded
- [ ] T09 the result reported to the GM (FR-005: acceptance is not a condition of completion - "a specific background session")
