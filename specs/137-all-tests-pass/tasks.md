# Tasks: One Hundred Percent of the Tests Pass (137)

Checked off only when verified on the whole tripwire, the gate cohort and the 24-seed cohort (no
rotation). Every task classified per constitution v2.12.0.

- [x] T01 spec from the GM's verbatim request; `spec-fidelity` review - verdict in spec.md Status
- [x] T00 the opening message to the "Diagram tests" session (FR-000): sent 2026-08-28 (msg 7b11f701) - the division of labor and the request for a safe sha; recorded in handoffs.md
- [x] T02 the efficiency session's safe work pulled (FR-001: ebbf29ef, then 135 on main) - `handoffs.md`; first full measurement on the merged tree (2026-08-28 04:10-05:00Z): tripwire 27/33/37/47 as pinned, 41 ok; gate cohort 41 OK, 42/43/44 as pinned; the pool hamlets Kashikawa/Mizuguchi/Sawada red (T10); the 48-seed audit **2 of 48 pass** - `lanes_form_one_network` 26, `lanes_bend_like_paths` 15, `title_clear_of_features` 8, `village_windbreak_is_continuous` 6, `farmhouses_reach_a_way` 6, `houses_clear_of_paddies` 5, `lanes_reach_something` 4, `fields_clear_of_road` 3, `lanes_clear_of_bamboo` 3, `features_do_not_overlap` 2, `captions_clear_the_ways_they_stand_on` 2, `paddy_bunds_do_not_stagger` 2, one each `village_trees_unshade_from_west`, `kosatsuba_by_the_road`, `long_ditches_have_a_footbridge`. The lane web is broken across the TIER, not on three seeds; the COHORT_BASELINE pin (22/24) is far stale (research: procedure)
- [ ] T03 (48-cohort 2026-08-28 10:10Z: **18/48** - bends 17, network 9, reach 6, then a tail of 1-2: bund staggers, kosatsuba by the road, lanes_reach_something, captions on ways, one paddy, one overlap, one footbridge) the lane web across the tier: `lanes_form_one_network` (26 of 48), `lanes_reach_something`, `farmhouses_reach_a_way` - measured on seed 37: the web is in pieces BEFORE the smoothing (the first touch pass's orphan joiner reports 3 orphans it cannot link: a 12 ft slot between a house and a yard that the 7 ft fabric pad closes both sides); a cohort pass-rate bisect over the period's commits names the commit that broke the tier; the smoothing now refuses any rewrite that disconnects the web unless the touch pass can rejoin it (kept: Inashiro clean, seed 37's bends gone); the orphan joiner's ladder - a route at the junction margin, a detour round a yard (8x the gap), a house-safe drop - took the 48-cohort from 2 to 7 passing and `lanes_form_one_network` from 26 seeds to 9; seed 37 CLEAN (pin out), 27 and 47 shrunk; the cohort bisect (24 seeds per commit, a clean scratch clone): **18/24 at the lock (513c419b), 2/24 at T32 (0ecb352f)** - `houses_clear_of_lanes` 13, `features_do_not_overlap` 10, `lanes_form_one_network` 10, `title_clear_of_features` 7; T41 and T52 produced no verdict (the batch crashed there on the knot collapse, guarded only at T99). So the tier was broken by T31/T32's own passes: a junction link that 'may brush a fence' at a 4 ft footprints-only margin draws lanes the gate refuses. Variant A under measurement: every junction link held to the web's fabric margin (`_clear_link`, then the routed detour) (research: rendering - the web's own geometry; the historical rule "interconnected" is already read)
- [ ] T04 `lanes_bend_like_paths` (18 of 48 after T03's ladder): every web lane now records the pass that drew it (`role`: cut / skeleton / join-orphans / bridge-breaks / straggler / touch / smooth); seed 43's fold is a STRAGGLER (the door path) folding inside the pocket between a house corner and a neighbor's yard, and the touch pass's two links onto it make the GM's triangle; chording stragglers at the footpath's 4 ft margin straightened it and rotated a bend onto Inashiro (dead end, recorded in ways.py); the router (`_route`) is Dijkstra on a lattice with no turn price - the fix belongs at the straggler's DOOR seat / the router's turn cost (research: physical - the bend rule is READ, research/homesteads.md 'How does a village lane bend?') **Progress 2026-08-28 (splice fixes, all in `ways.py`)**: every bend in the sample sat on a straggler or touch lane AT A JUNCTION - the routed lane was fine, the link stitched onto it was not. Five fixes at the splice: (1) `_unretrace` - a join link prepended whole doubled back along the piece's own first segment (seed 07, 180 degrees); (2) an end already standing on a way is a junction, not a free end - the touch pass linked it onward to a second way over the first lane's tread (seed 07); (3) `_unjog` at the junction margin on every `_route` result and every splice - the lattice's 7 and 13 ft steps round a garden corner (seeds 14, 15); (4) the detour rung compares the routes of the four nearest ways' three best pairs by ROUTE length instead of drawing the first that routes - the nearest point by air sat across a garden and its only route was a U round a shed (seed 03); (5) `_stop_at_network` - a link ends at the first way it meets, not at a `q` a later trim removed (tripwire 27, pin dropped). Verified per step on the reference; tripwire 33's caption fixed in `fixtures.py` (the seat probe scored a one-line box; the record wraps to two). **Defects found on the way (Principle XIV, fixed in the same commits)**: (a) `generate()` re-rolled a stranded map on the SAME `SitePlan`, and the stages accumulate onto it (`plan.bamboo_polys +=`) - attempt 2 drew attempt 1's bamboo strips over a different web (seed 03 `lanes_clear_of_bamboo`); each attempt now builds on a deep copy and the report reads the kept roll's plan; (b) `_strip_blocked` sampled five points of a 22 by 16 ft strip against the lanes, so a lane could cross it between them - the tread is now tested as a segment against the strip's edges; (c) the notice board's caption seat scored a one-line 54 by 10 box while the record wraps 'notice board' to two lines (26 by 18, 2.2 px above the anchor) - the probe now builds the box the record will carry (tripwire 33). **48-cohort 2026-08-28 ~14:00Z on 41956b4f: 25/48** (18 before): bends 17 -> 7, network 9 -> 8, reach 6 -> 7, tail 10.
- [x] T05 `village_windbreak_is_continuous` (seed 33; 5-6 of 48): measured with an instrumented `village_grove` - the belt DID re-seat its clumps outward round the two garden beds at its edge, and the T10 face trim then dropped those outer clumps from the record as off-page, so the belt read as holed where it had wrapped the plot. The trim now cuts the ink, not the record (the trees stand, off the page); seeds 33 and 44 green, the reference clean; the tripwire pin for 33 is out. The face-dodge alternative (a plot-extent column face) is recorded as a dead end in hinterland.py - it rotated the hole to seed 41 (research: physical - the yashikirin keeps off a plot's sun lanes, READ in research/vegetation.md; a belt planted round a garden is the same doctrine)
- [ ] T06 the title seat (`title_clear_of_features`, 10 of 48): a ladder - blank ground first; then COVER (the belt, a wood; the placard is an opaque card and hides nothing a reader needs) - the check permits cover now; then the first clean corner; then a TITLE BAND above the map declared in `meta.title_band`, which `crop_hugs_content` allows on the north edge (a band above the frame - `crop_hugs_content` refused it undeclared; growing the view also reveals features that run off the frame, so the check still reports those). Seeds 2, 6, 13, 18 green on the title. `labels_align_with_their_referent`: the check derived the subject's angle without `aligned_tilt`'s square-rotation snap (a 90-degree board read -90 there, 0 in label()) - now the one function (seed 13). Remaining under T06: seed 44 `houses_clear_of_paddies`; seed 42 `farmhouses_reach_a_way`; seed 47 `fields_clear_of_road`, `long_ditches_have_a_footbridge` (research: rendering / procedure per item)
- [ ] T07 cohort seeds 22 (`field_ringed`) and 24 (`paddy_bunds_clear_the_supply_channels`) (research: physical - cite the field rule each check encodes)
- [ ] T10 the pool hamlets (FULL-only, `tests/full/test_villages.py`; measured 2026-08-28 on the merged tree): Kashikawa seed 3 `features_do_not_overlap` + `lanes_form_one_network`; Mizuguchi seed 23 `village_windbreak_is_continuous`; Sawada seed 6 `lanes_form_one_network` + `title_clear_of_features` - the same mechanisms as T03-T06 (the web stubs, the belt hole, the title seat); verified by `make maps` after a clean run and by the FULL sweep at the end (research: as the mechanism tasks)
- [ ] T08 every pin table empty; the unlocked gate, the tripwire and the 24-seed cohort green; handoff to the efficiency session recorded
- [ ] T09 the result reported to the GM (FR-005: acceptance is not a condition of completion - "a specific background session")
