# Plan - 225 the marks culled, the pad tightened, the opacity folded, the rows clipped

Spec: [`spec.md`](spec.md). Engine code (`land/cover.py`, `land/wet.py`, `settlement/finish.py`,
`hinterland/frame.py`, `fields/landuse.py`, `interactive/page.py`); GATED route.

## Constitution Check

VI: Inashiro first, then the pool, the cohort, the review. X clause 15: no new scan; the flush's test is one
box comparison per mark. X clause 5: the fold, the cut and the flush each have unit tests against their rules.
XII: D1-D4. XVI: reviewed first.

## Order

1. FR-003 the fold (finish), FR-005 the precheck - tests; `make map` Inashiro; the tile times.
2. FR-001 the marks deferred + one flush; FR-002 the pad; the cohort in the background.
3. FR-004 the rows cut; Inashiro; the pool.
4. R2 in the phase-marked worktree; `make done`; the review; land.
