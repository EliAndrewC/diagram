# Tasks - 203 The placard stays on top

Spec: [`spec.md`](spec.md). Plan: [`plan.md`](plan.md). All `research: rendering`.

- [ ] T01 spec-fidelity review of `spec.md` against `request.md` - FAITHFUL before any code
      research: rendering
- [ ] T02 `finish.py` marks the scale bar (FR-002); `page.css` exempts the placard and the bar from
      raster mode's hiding (FR-001); the why at both points of change
      research: rendering
- [ ] T03 the browser test (FR-004) and the marker's unit test; SC-002 shown once
      research: rendering
- [ ] T04 `make done` green; the page opened, the card hovered and left, the scrub lit (SC-003); land GATED
      research: rendering
