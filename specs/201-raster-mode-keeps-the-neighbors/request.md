# The GM's request, verbatim

2026-09-07, session "Diagram tooling", after feature 200 (raster mode below the vector) had landed and
the GM had opened the regenerated pages in Chrome:

> Thanks. That does look a lot better. However, the specific place where it makes interacting with the page harder is when I move my mouse over the rice paddy, then it just lights up the entire outline and then fills it in with a solid form. So I can no longer see where the earthen bunds are Or the bund beans or the field ponds, etc. I don't think that we need to necessarily recreate an exact lit up version of what the field looks like. with every single feature exactly as it is. Although if that is easy to do, then we should just do that. because then I can see where I would have to move my mouse in order to move it over any of those individual features.
>
> Also, the title card for each map is now messed up. It is no longer easy to read the map scale. And, in fact, it looks as if two different lines of text are overlapped on each other or something. Also, for some reason, the font and font size of the lit up version of the title card is different than it is for the regular title card that is not highlighted. I imagine this probably has something to do with the readability issue that I am describing. Please fix that as well. Thanks.
