 the directory # /diagram engine - dev loop
 the directory 
 the directory Guidance for *working on the diagram engine* (the `settlement/` package, the `check_village/` package, the pool
 the directory generators), as opposed to *invoking* `/diagram` to draw a map (that is `SKILL.md`). This file
 the directory auto-loads whenever a session edits files in this directory - which is exactly when it applies.
 the directory 
 the directory The project-wide iteration doctrine lives in the root [`CLAUDE.md`](../../../CLAUDE.md)
 the directory "Iteration-loop efficiency" section (batch recon into fewer bigger turns; iterate on the ONE
 the directory motivating artifact, then run the full test bed once at the end; background the final gate; never
 the directory cut the ritual/guardrail steps). Read that first; this file carries the concrete diagram numbers
 the directory and the DIAGRAM-SPECIFIC lessons that section does not cover - each earned by costing real
 the directory round-trips.
 the directory 
 the directory ## Where things live (read this first; load only the index you need)
 the directory 
 the directory The skill's Python lives under **`l7r/diagram/`** (feature 119) and is grouped by what a module is
 the directory FOR. Each group carries its own `CLAUDE.md` index, so a session can open the one directory its task
 the directory is in instead of paging this file.
 the directory 
 the directory **Why the extra two levels.** `l7r/` here is a PEP 420 *namespace portion* - it deliberately has no
 the directory `__init__.py` - and it shares the `l7r` parent package with the L7R Toolkit webapp's `l7r.app` /
 the directory `l7r.names` in `/host-l7r-repo/gm-assistant/webapp/l7r/`. Both directories contribute to one `l7r.__path__`, so
 the directory `import l7r.app` and `import l7r.diagram.settlement` work in the same interpreter and the webapp
 the directory can render a map without two colliding top-level packages named `l7r`. **Never create
 the directory `l7r/__init__.py`**: that makes it a regular package, terminates the import search, and makes the
 the directory webapp's portion silently stop existing. `tests/test_namespace_portion.py` guards it in both trees.
 the directory 
 the directory This directory - not `l7r/diagram/` - is still the `sys.path` root, and `pool/`, `tests/`, the
 the directory `Makefile` and `pyproject.toml` all stay here. That is why every pool generator's bootstrap block
 the directory is unchanged by the move: `SKILL = dirname(dirname(HERE))` from `pool/<tier>/x.gen.py` still lands
 the directory here. Engine modules that compute the skill root from their OWN location moved two levels deeper and
 the directory were adjusted to match (`gencache`, `pool_index`, `render_cache`, `cohort_audit`, `cache_audit`,
 the directory `make_regressions`, `timings`, `hamletgen`, `check_village/__main__`) - a test asserts three of them
 the directory still resolve here, because a wrong depth is silent and just lands one directory short of `pool/`.
 the directory 
 the directory | directory | what is in it | load its index when |
 the directory |---|---|---|
 the directory | [`l7r/diagram/settlement/`](l7r/diagram/settlement/CLAUDE.md) | the Mode B drawing engine (the `Settlement` class and its mixins) | you are changing what a settlement map DRAWS or where it places something |
 the directory | [`l7r/diagram/check_village/`](l7r/diagram/check_village/CLAUDE.md) | the gate: the whole check battery, as a registry of segments | you are adding, changing or running a check |
 the directory | [`l7r/diagram/waterfields/`](l7r/diagram/waterfields/CLAUDE.md) | the water-first field engine (v2 comb fields) | you are changing paddies, bunds, canals or the field frame |
 the directory | [`l7r/diagram/hamletgen/`](l7r/diagram/hamletgen/CLAUDE.md) | the scripted hamlet generator - a whole hamlet from a 9-line spec | you are working on scripted generation |
 the directory | [`l7r/diagram/sitegen/`](l7r/diagram/sitegen/CLAUDE.md) | tier-agnostic generation machinery the tiers SHARE (geometry, types, worker counts) | you are adding a tier generator, or moving a stage out of one |
 the directory | [`l7r/diagram/pipeline/`](l7r/diagram/pipeline/CLAUDE.md) | how a map gets regenerated, cached, rendered and indexed | the cache is behaving oddly, or you are changing how generation is DRIVEN |
 the directory | [`l7r/diagram/tools/`](l7r/diagram/tools/CLAUDE.md) | read-only diagnostics and audits you run by hand | a map came out wrong and you need to ask WHY, or a number needs measuring |
 the directory | [`l7r/diagram/ci/`](l7r/diagram/ci/CLAUDE.md) | the CodeBuild dispatcher: when a PAID remote run may start, and how it is driven (feature 130) | a remote run refused, or you are changing when money may be spent |
 the directory | [`tests/`](tests/CLAUDE.md) | every test, mirroring the source layout, plus the frozen fixtures | you need to find or add a test |
 the directory | `pool/` | the shipped maps: `<name>.gen.py`, its manifest, its render, its `.notes.md` design journal | - |
 the directory | `wip/` | maps staged outside the pool (not gated, not swept) | - |
 the directory 
 the directory Two engine modules are still single files rather than packages, and stay that way on purpose:
 the directory **`l7r/diagram/compound.py`** (the Mode A compound program and perimeter-first placer) and
 the directory **`l7r/diagram/citybudget.py`** (the space-budget city/capital planner). Both are peers of the
 the directory engine packages above - pool generators import them directly - and folding them into a package
 the directory would rewrite six frozen generator scripts for no navigational gain.
 the directory 
 the directory The prose reference (as opposed to the code) splits the same way: [`SKILL.md`](SKILL.md) is the
 the directory usage-facing index, and it indexes [`settlements/`](settlements/) and [`buildings/`](buildings/)
 the directory (the per-topic design doctrine) and [`research/`](research/) (the historical grounding). Read a
 the directory skill index, then load only the topics the subject calls for.
 the directory 
 the directory **Run the packaged modules as modules**, from this directory - `python3 -m l7r.diagram.pipeline.regen ...`,
 the directory `python3 -m l7r.diagram.tools.why_placed ...`. Running a package module as a loose script path puts its own
 the directory directory on `sys.path` instead of the skill root, which is how one file ends up imported twice
 the directory under two names.
 the directory 
 the directory ## Dev-loop doctrine (load on demand)
 the directory 
 the directory This file used to carry all of it inline, at 1,449 lines - roughly 28k tokens charged to **every**
 the directory session that edits anything in this tree, including sessions that only regenerate a map. The
 the directory doctrine itself is unchanged and verbatim; it now lives in [`dev/`](dev/), one file per topic, each
 the directory stating when to load it. Same pattern as the root [`CLAUDE.md`](../../../CLAUDE.md) -> `docs/`
 the directory split. **Load the one file your task is in.** The short always-on version of each rule is below the
 the directory table; the file is where the evidence, the measurements and the failure stories live, and you want
 the directory those before you argue with a rule.
 the directory 
 the directory | doc | load it when |
 the directory |---|---|
 the directory | [`dev/loop.md`](dev/loop.md) | You are about to run the gate or a pool sweep, you want the diagram timing numbers, or you are deciding how much to re-run after a change |
 the directory | [`dev/placement.md`](dev/placement.md) | You are adding a map feature, or changing where anything is placed or drawn. Carries the DRAW ORDER map (including the scripted `STAGES` table), CENTER vs FOOTPRINT, and the KEEP-CLEAR CONTRACT. Its companion `dev/placement-stages/hamlet-placement.html` SHOWS the order - Inashiro plated after each of the thirteen stages |
 the directory | [`dev/gate.md`](dev/gate.md) | You are adding or changing a check, writing a check test, or waiving a rule for one map |
 the directory | [`dev/diagnostics.md`](dev/diagnostics.md) | A map came out wrong and you need to know WHY - `open_seat`, `why_placed`, `site_justice`, `crop_map`, and how a probe lies to you |
 the directory | [`dev/performance.md`](dev/performance.md) | A gen or a check got slow (or "hangs"), or a `GEN_TIME_BUDGETS` entry tripped |
 the directory | [`dev/cache.md`](dev/cache.md) | The cache is behaving oddly, you changed how generation is DRIVEN, or a coverage floor breached for no reason you can see |
 the directory | [`dev/pool.md`](dev/pool.md) | You are about to touch a pool map, convert one to scripted generation, or work on `hamletgen/` |
 the directory | [`dev/perf-log/`](dev/perf-log/CLAUDE.md), [`dev/run-log/`](dev/run-log/CLAUDE.md), [`dev/bypass-log/`](dev/bypass-log/CLAUDE.md) | You are about to add an entry to one of the append-only histories, or wonder why they are DIRECTORIES rather than files |
 the directory | [`dev/lessons.md`](dev/lessons.md) | A fix is not working and you are about to try another one - dead ends already walked, claims that turned out wrong, and the SHAPES those failures take |
 the directory | [`dev/decisions.md`](dev/decisions.md) | You are about to build on a property of the engine nobody decided, or you are leaving a decision open for a later session |
 the directory | [`dev/reviews.md`](dev/reviews.md) | You are about to launch `settlement-review`, `building-review` or `backstory-review` |
 the directory | [`dev/skill-boundary.md`](dev/skill-boundary.md) | You are wondering whether building plans (Mode A) and settlement maps (Mode B) should be separate skills or packages, you are adding a new Mode A building type, or a Mode A `.gen.py` is about to appear - the 2026-08-27 decision to keep one skill, the prediction of what would change it (a generator, not a building count), and the order to split in when it does |
 the directory 
 the directory [`future-work/`](future-work/CLAUDE.md) is the deferred-engineering backlog, split by map type on
 the directory 2026-08-24 - load `farming-communities.md` for hamlet/village work, `cities.md` for towns and above,
 the directory `compounds.md` for Mode A plans, `cross-cutting.md` for anything spanning tiers. Its own CLAUDE.md
 the directory carries the rules that keep it from rotting back into one 3,453-line file.
 the directory 
 the directory Two more docs that were already separate: [`migration-plan.md`](migration-plan.md) (the standing
 the directory plan for converting the pool to scripted generation - **read it before drawing or scripting a
 the directory settlement map, and update its status table when a conversion lands**) and
 the directory [`timings.md`](timings.md) (the measured timing ledger; never write fresh timings into prose).
 the directory 
 the directory ## The always-on version
 the directory 
 the directory Each line below is the rule; the doc named after it is the evidence. Where the two ever disagree,
 the directory the doc is right - it is where the measurement lives.
 the directory 
 the directory **The goal all of this serves** (GM 2026-08-25, feature 133, constitution v2.3.0): *"if me asking
 the directory for a simple change results in half an hour of work being done when it should have only taken five
 the directory minutes, then that limits the number of changes that I can make in a single day."* Every command
 the directory below is chosen against that - the cheaper one that answers the question wins; one verification
 the directory covers a batch of changes; a simple task that ran long is diagnosed (more complicated than
 the directory expected / lengthier tests than needed / more cycles than needed) and the tooling is improved when
 the directory it is the tooling. With remote off, a paid run the tooling was about to start is still recorded
 the directory (`make ci-status` "Would have dispatched") and audited at the period's end.
 the directory 
 the directory **The loop** ([`dev/loop.md`](dev/loop.md))
 the directory 
 the directory **THE COMMAND MAP, with measured times** (feature 127; every number is a stopwatch, not an estimate):
 the directory 
 the directory | command | what it does | time |
 the directory |---|---|---|
 the directory | `make quick` | lint, types, and every test that does not roll a map; stops at the first failure, failed-first (`--ff`) so a fix that did not take fails in seconds | **~33 s** |
 the directory | `make sun-audit M=...` | the sun rules and the belt's page presence off the manifest - the numbers a record may quote | ~1 s |
 the directory | `make new-check NAME= FILE= TEST=` | scaffold a gate check: segment stub with the next key, sorted fixture entry, test stub with builders imported | ~1 s |
 the directory | `make reference` | one seed of the reference hamlet (Inashiro), alone | **~26 s** |
 the directory | `make durations` | where the suite's time goes - run this when a target feels slow | ~35 s |
 the directory | `make maps` | picks its own scope from how the last run went | 1 min - many |
 the directory | `make quick` (scope locked) | lint/format autofix + mypy + every test that neither rolls a map nor carries coverage only nor is tagged for other tiers only (`tiers` marker, T17): ~3,000 tests, ~17 s pytest, ~20 s wall (2026-08-26) |
 the directory | `make done` | reference + lint/types + the whole suite + coverage floors; measured 2026-08-26: ~75 s with scope LOCKED (map-rolling tests deferred to unlock, `hooks-test` skipped while its stamp is fresh), ~4.5 min unlocked (the 31 map-rolling tests are ~4 of them), ~5.5 FULL. **Short-circuits in seconds** (`already verified`; comments and docstrings in engine Python do not re-key it, GM 2026-08-26) when the last record is a green `make done` against exactly this engine content - the dispatcher's own keys: every `.py` under the skill outside `tests/` and `l7r/diagram/ci/`, plus pool gens/manifests (feature 132; [`dev/switches.md`](dev/switches.md)). Docs, tests, Makefile, config and `scripts/` edits never cost the gate | **~5.5 min - NOT the quick check** (or ~3 s already verified) |
 the directory | `make done FULL=1` | prompt (cancel by default), THEN the reference map, THEN every pool map + the seeds 41-44 ratchet + `perf-gate` | ~6 min |
 the directory | `make ci-status` | **free** - the delta, its route (DIRECT/GATED), the verification state, whether the would-be tree is already verified, month-to-date remote spend. The "why won't it dispatch" answer | ~2 s |
 the directory | `make ci-check` | **PAID** (~$0.40 est.) - the iteration check on CodeBuild: lint locally, build parked, reference locally, then `make done` against the merge with the latest main; `FULL=1` (prompts), `TARGET=cohort` etc. | measured in `timings.md` |
 the directory | `make ci-merge` | **PAID** - the push's gated route; called by `sync-with-main.sh`, never by hand | - |
 the directory | `make ci-image` | **PAID** (~$1), prompts - rebuild the build image from `Dockerfile.ci`; the GM's to run | - |
 the directory | `make switches` | the iteration switches (feature 132): `remote on\|off`, `scope unlocked\|reference`, with reason, who and when | ~1 s |
 the directory | `make ci-off REASON=...` / `ci-on` | **remote off**: nothing dispatches to CodeBuild, `ci-check`/`ci-image`/`FULL=1` refuse, the gated push lands on a green local `make done` (LOCAL-GATED). Commits the switch | ~1 s |
 the directory | `make scope-lock REASON=...` / `scope-unlock` | **scope locked**: no invocation rolls a map other than the reference settlement or more than one map - `cohort`, `tripwire`, `test-full`, `done FULL=1`, `cache-audit`, `regressions`, `perf`, `maps SCOPE=all` and any remote sweep refuse and name the unlock. Commits the switch. Doctrine: [`dev/switches.md`](dev/switches.md) | ~1 s |
 the directory | `make perf-report AGAINST=<NNN>-start` | the trend, then the **BAND** the newest pair reaches (feature 129): 1 any increase, 2 >5% total / >10% seed, 3 >10% / >20% - per environment, both measurements | ~1 s |
 the directory | `make perf-explain WHY="..."` / `perf-confirm` / `perf-audit` / `perf-signoff` | the review records a band owes; `perf-confirm` and `perf-audit` are the **`perf-audit` subagent's** (they decline without `AS=perf-audit`); `perf-signoff` is the GM's, at a terminal | ~1 s |
 the directory | `make perf-review` | does every environment's newest pair carry the records its band owes? The PUSH runs this | ~1 s |
 the directory | `make perf-profile SEED=25 STAGE=web` | tier-2 evidence: cProfile of ONE stage of ONE seed (+225% on that stage); the derived table is committed, the raw `.prof` is not | ~3x the stage |
 the directory 
 the directory **Nothing runs outside make.** A bare interpreter reaching an engine entry point, a bare pytest, or a
 the directory make driven by a foreign makefile is refused before it executes (`scripts/make-only-hooks.sh`), and
 the directory the engine refuses in-process calls too (`l7r/diagram/_invocation.py`). If a refusal fires on correct
 the directory work that is a BUG in the guard worth fixing, not something to work around - it did so five times
 the directory while being built, every one a MENTION mistaken for an INVOCATION.
 the directory 
 the directory **`quick` enforces its own 60 s budget and fails over it.** It was 254 s while every guard pointed at
 the directory it as the cheap option, because it deselected two FILES and could not see that three polder tests
 the directory rolled maps. Marking is `@pytest.mark.rolls_map`, guarded by `tests/test_markers.py`.
 the directory 
 the directory 
 the directory - **A performance increase is never silently absorbed** (feature 129, constitution VI): `make perf-report` names the band; any increase on any seed or the total - per environment - owes `make perf-explain WHY=...` from you and a confirmation from the **`perf-audit` subagent** (launch it; never pass `AS=perf-audit` yourself); above 5%/10% the subagent's audit; above 10%/20% the GM's sign-off. The push refuses without them.
 the directory - Iterate on the ONE motivating map; run the full test bed exactly **once**, at the end. That final
 the directory   sweep is MANDATORY whenever shared engine code changed (`settlement/`, `check_village/`,
 the directory   `waterfields/`, a scripted engine).
 the directory - `python3 -m l7r.diagram.pipeline.regen pool/<type>/<map>.gen.py` - the cache skips the work and
 the directory   prints `CACHED` / `REGENERATED` / `FROZEN` every time.
 the directory - Cheap linters BEFORE the gate: `python3 -m ruff format . && python3 -m ruff check . && python3 -m mypy`.
 the directory - Then the WHOLE affected test file with `-n auto`, never a `-k` subset. Then `make done`, **once**,
 the directory   backgrounded, and **never polled** - act on the notification.
 the directory - Never re-run what `make done` just ran, and never run pytest serially (~7x slower here).
 the directory - Never run a pytest BESIDE a running gate - two writers on the same pool maps is a source of false RED.
 the directory - Update the predictably-affected unit tests in the SAME edit as the engine change.
 the directory - **CYCLE DISCIPLINE** (GM 2026-08-26, constitution v2.4.0; measured on feature 133 T10, where ~30 of
 the directory   57 minutes were round trips): **re-read the WHOLE diff for convention misses before the first
 the directory   test run** - an unimported builder, an undeclared segment input, an unsorted fixture, a wrong test
 the directory   coordinate are ten-second fixes that cost a full model round trip each when a test finds them one
 the directory   at a time; fix everything a failing run lists before re-running; **scaffold a check with
 the directory   `make new-check`** so the conventions cannot be missed; and **never write a number into a record
 the directory   that was not measured on the artifact** (`make sun-audit` for the sun and the belt) - a guessed
 the directory   figure is a correction round the reviewer will make you pay.
 the directory 
 the directory **Placement** ([`dev/placement.md`](dev/placement.md))
 the directory 
 the directory - **Read the DRAW ORDER map before moving anything.** A drawing method sees only what is in `self.M`
 the directory   when it runs; a placer avoids only what is in the registries when it runs. Most "wrong geometry"
 the directory   is wrong ORDER.
 the directory - A new footprint feature MUST go in `_OVERLAP_STRUCTS` (or `_OVERLAP_EXEMPT`, with the reason) and
 the directory   get a caption group in `_LABEL_GROUP`. Membership alone gates it off fifteen hazards; nothing else
 the directory   has a hand-written key list to remember.
 the directory - **Record a footprint the extractor can read** - `x`+`w`/`vw`, a `poly`/`outline` ring, a stroked
 the directory   polyline, or `parts` of rotated quads. A record matching none of those is invisible to every
 the directory   matrix check in both directions and looks exactly like a feature with nothing wrong.
 the directory - **Gap verdicts read footprints, never centers** - `edge_gap` / `within_edge_gap` / `sat_overlap`.
 the directory   Classification, association-reach and prefilters may use centers, deliberately; say which family
 the directory   your rule is in, in a comment, at the test. Add a `test_gap_verdicts_read_footprints_not_centers`
 the directory   entry with every new gap rule.
 the directory - **Never let an aggregate (a centroid) stand in for the distributed thing a verdict is about.**
 the directory   Measure to the nearest member, or to the wall.
 the directory - **Randomness is POSITIONAL or SCOPED, never "wherever the stream happens to be":** `self._hjit(x, y, salt)`
 the directory   for a per-feature attribute, `with self.rng_scope(name, *key)` for a phase or region.
 the directory 
 the directory **The gate** ([`dev/gate.md`](dev/gate.md))
 the directory 
 the directory - Adding a check: write `_seg_<key>__<name>` in the `check_village/segments_*` file that covers its
 the directory   theme, body reading inputs as keyword params defaulting to `_UNBOUND` and returning
 the directory   `_kept(locals(), <literal tuple>)`; extend `tests/fixtures/gate_check_names.json`. There is **no
 the directory   registry row to write** - the row and the execution position both derive from the function itself.
 the directory - Run one check by itself with `gate(M, only={"check_name"})`. Do not go hunting for the segment by hand.
 the directory - **A check that never RUNS looks exactly like a check that passes.** Any rule behind
 the directory   `if meta.get(...)` needs a companion check that the DECLARATION EXISTS.
 the directory - Build check-test manifests with the fixture builders (`manifest`, `house`, `yard`, ...), not by hand.
 the directory - Placement and its check must read the SAME manifest source - and when you mirror a gate
 the directory   measurement, mirror its WINDOW, not just its formula.
 the directory - A map may break a rule, but only IN WRITING: `s.meta(waivers={...})` with 60+ characters of real
 the directory   reason, and freeze the pre-waiver manifest into `pool/regressions/`.
 the directory 
 the directory **Diagnostics** ([`dev/diagnostics.md`](dev/diagnostics.md))
 the directory 
 the directory - Ask the ENGINE where a feature fits (`s.open_seat(...)`) - do not guess coordinates and regenerate.
 the directory - Ask the GEN who placed it (`tools/why_placed.py --at` / `--refused`) - do not grep for the caller.
 the directory - Adjudicate a multi-rule siting against the GATE (`tools/site_justice.py`), never against a
 the directory   re-statement of the rules.
 the directory - Read derived geometry from the MANIFEST (0.2s), not by re-running the generators (minutes).
 the directory - Batch every crop you want to look at into ONE `tools/crop_map.py` call, then Read them together.
 the directory - **A diagnostic that restates what it observes will lie to you.** Print the value and its
 the directory   provenance from ONE expression, or do not print the provenance.
 the directory 
 the directory **Performance** ([`dev/performance.md`](dev/performance.md))
 the directory 
 the directory - Every slow gen ever profiled here was the same shape: *a per-candidate scan of geometry that does
 the directory   not change during the scan*. Hoist, prefilter, or index - and if a gen "hangs", suspect that shape
 the directory   and profile before bisecting.
 the directory - When a check is slow, **INDEX it - do not coarsen it.** The index prunes; it never decides.
 the directory - Trust the A/B against HEAD, not cProfile's seconds.
 the directory 
 the directory **The pool** ([`dev/pool.md`](dev/pool.md))
 the directory 
 the directory - **The legacy pool is FROZEN.** The 19 hand-authored maps are permanent exhibits: never regenerated,
 the directory   never re-gated, renders committed. The fix for a frozen map that breaks a post-freeze rule is
 the directory   CONVERSION, not retrofit - do not "fix" one, and do not treat its violations as bugs.
 the directory - New rules ship un-gated; engine changes no longer need byte-identity flags.
 the directory - A cohort of seeds is a much stronger test bed than a map - and **measure the cohort's baseline
 the directory   first**, in a detached worktree (`git worktree add --detach /tmp/base HEAD`), never by stashing.
 the directory - A seed that passed before your change and fails after it is a REGRESSION, and nothing merges to
 the directory   main carrying one (constitution Principle XIII). "It rotated" is not a defense.
 the directory 
 the directory **Reviews** ([`dev/reviews.md`](dev/reviews.md)) - `settlement-review` is mandatory before a Mode B
 the directory map ships - which since 2026-08-26 means at ACCEPTANCE and at UNLOCK, not per task: while `scope`
 the directory is locked the GM looks at every result and no per-task review runs; when one runs it runs in the
 the directory background after the map is handed back (or beside a LONG gate, never `make quick`) and is never
 the directory waited on. Say the SCOPE (`DELTA:` vs `FULL`), one map per agent in parallel. **A finding OUTSIDE the delta
 the directory is still yours to fix** (constitution Principle XIV) - a reviewer pointed at a delta reliably turns
 the directory up unrelated defects, and that is it working.
 the directory 
 the directory **Build what was asked** (constitution Principle XVI, NON-NEGOTIABLE) - and in this engine the
 the directory temptation is specific: almost every ordering or placement rule has a case where an exception looks
 the directory justified, because the geometry really is full of special cases. It is still not yours to approve.
 the directory Feature 126 was asked for as "farmhouses before lanes" and specified as farmhouses before lanes
 the directory EXCEPT the connector and the field spur; both of those register no-build corridors, so both kept
 the directory constraining the placement the feature existed to free. An exception goes to an independent Opus 5
 the directory subagent with the GM's request verbatim, and a finished `spec.md` gets the same treatment before
 the directory implementation starts. Three rounds, then escalate.
 the directory 
 the directory **Fix defects where you find them** (constitution Principle XIV, NON-NEGOTIABLE) - this engine is
 the directory where the rule bites hardest, because its reviewers and diagnostics surface defects constantly and
 the directory almost none of them belong to the feature in hand. Fix them in the work at hand; defer ONLY an
 the directory architectural fix, and then with its measurement, mechanism and sketch. Do not cite Principle XIII's
 the directory "pre-existing failures stay ledgered" - that governs what blocks a push, not what you owe a defect
 the directory you have seen. And when a fix attempt FAILS, record it at the point of change: `homesteads.py`
 the directory carries two dead ends for the front-row lane cap, either of which a later session would otherwise
 the directory re-try.
 the directory 
 the directory **Recording decisions** ([`dev/decisions.md`](dev/decisions.md)) - before you build on a property of
 the directory the engine, check whether anyone DECIDED it; a side effect is not a rule. And an open decision
 the directory carries the 2-3 line implementation sketch, not just the question.
 the directory 
 the directory - **Research it before you ask the GM, and if two forms are supportable make it a KNOB.** The
 the directory   ladder: research -> decisive means implement it -> two attested forms means roll between them per
 the directory   settlement -> only a silent record earns a GM ruling. Liberty covers a DEGREE along a continuum,
 the directory   never a choice between two distinct FORMS. Constitution Principle XII; evidence and the worked
 the directory   example in the doc.
