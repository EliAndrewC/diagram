#
# Locked for the diagram repository at the 2026-08-25 split, pinned to the versions gm-assistant's
# webapp/requirements.txt carried then (the set every recorded green gate ran on).
# Re-lock:  pip-compile --output-file=requirements.txt requirements.in
#
numpy==2.5.1
    # via shapely
pillow==12.3.0
    # via -r requirements.in
shapely==2.1.2
    # via -r requirements.in
